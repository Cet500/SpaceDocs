# 📚 SpaceDocs

Документация по программированию с примерами проектов и практическими руководствами.


## 📋 Описание

**SpaceDocs** — это проект документации, созданный с помощью [MkDocs](https://www.mkdocs.org/).
Сайт содержит учебные материалы по различным технологиям программирования.


## 🚀 Быстрый старт

### Требования

- Python >= 3.12
- [uv](https://github.com/astral-sh/uv) (менеджер пакетов)

### Установка

1. Клонируйте репозиторий или откройте проект:
   ```bash
   cd SpaceDocs
   ```

2. Установите зависимости:
   ```bash
   uv sync
   ```

3. Активируйте виртуальное окружение:
   ```bash
   uv venv

   .venv\Scripts\activate  # Windows
   # ИЛИ
   source .venv/bin/activate  # Linux/Mac
   ```

### Запуск локального сервера

Для просмотра документации в режиме разработки:

```bash
   mkdocs serve
```

Сайт будет доступен по адресу: `http://127.0.0.1:8000/`

### Сборка статического сайта

Для генерации статических файлов сайта:

```bash
   mkdocs build
```

Собранный сайт будет находиться в папке `site/`.


## 📁 Структура проекта

```
SpaceDocs/
├── docs/                 # Исходные файлы документации
├── site/                 # Сгенерированный сайт (не коммитится)
├── static/               # Статические файлы (favicon, logo)
├── mkdocs.yml            # Конфигурация MkDocs
├── pyproject.toml        # Конфигурация Python проекта
└── README.md             # Этот файл
```


## ⚙️ Конфигурация

Основные настройки находятся в файле `mkdocs.yml`:

- **Тема**: `readthedocs` с русской локализацией
- **Плагины**: `search` для полнотекстового поиска
- **Навигация**: определяется вручную в секции `nav`


## 📝 Добавление новой документации

1. Создайте новый `.md` файл в папке `docs/`
2. Добавьте его в секцию `nav:` файла `mkdocs.yml`
3. Перезапустите `mkdocs serve` для просмотра изменений


## 🛠️ Разработка

Для разработки рекомендуется использовать:

```bash
# Запуск сервера с автоперезагрузкой
mkdocs serve -a localhost:8000 -w docs/ --livereload

# Сборка для проверки
mkdocs build

# Проверка конфигурации
mkdocs build --strict
```


## 📄 Лицензия

Проект создан для образовательных целей.


---

*Создано с помощью [MkDocs](https://www.mkdocs.org/) и темы [Read the Docs](https://readthedocs.org/)*
