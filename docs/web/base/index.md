# 🌐 Создание сайтов: с нуля до первого проекта

## Что такое сайт, вообще?

Сайт — это, по сути, несколько файлов, которые твой браузер читает и показывает красиво. Три основных файла:

- **HTML** — это "скелет" сайта. Текст, заголовки, кнопки, картинки — всё это пишется на HTML. Браузер его читает и
  говорит: "О, тут заголовок, тут кнопка, тут картинка".
- **CSS** — это "одежда" и "макияж". HTML говорит, что там есть кнопка, а CSS говорит: "Эта кнопка синяя, и она
  находится справа, и у неё закруглённые края". Красота, одним словом.
- **JavaScript** — это "поведение". Когда ты кликнешь на кнопку, она что-то делает. При наведении мышки она меняет цвет.
  При загрузке страницы считаются какие-то числа. Это всё JavaScript.

## Как это работает

Ты создаёшь три файла (или больше): `index.html`, `style.css`, `script.js`. Открываешь `index.html` в браузере, и
браузер сам загружает `style.css` и `script.js`. Всё видно на экране.

Никакого сервера не нужно для начала. Просто файлики на диске, и браузер их читает.

## Что ты сможешь сделать после этого плана

- **Уровень 1 (HTML)**: Создавать простые страницы с текстом, заголовками, списками, картинками и ссылками. Писать
  правильно структурированный HTML.
- **Уровень 2 (CSS базовый)**: Раскрашивать и раскладывать элементы. Менять цвета, размеры шрифтов, расстояния между
  элементами. Делать элементы рядом друг с другом или один под другим.
- **Уровень 3 (CSS продвинутый)**: Делать красивые переходы, анимации, сложные раскладки (флексбокс, гриды). Делать
  сайт, который хорошо выглядит на телефоне.
- **Проект "Футбольная команда"**: Собрать всё вместе и создать настоящий сайт про команду — со страницами игроков,
  расписанием матчей, галереей фото.

## Как пользоваться этим планом

1. Начни с **Уровня 1 (HTML)**. Не пропускай — в HTML всё просто, и это база всего остального.
2. Потом иди в **Уровень 2 (CSS базовый)**. Здесь ты научишься красивому.
3. Потом **Уровень 3 (CSS продвинутый)**. Дополнительные фишки.
4. **Проект "Футбольная команда"** можешь начинать, как только закончишь Уровень 1. Он специально простой — не нужно
   быть гуру в CSS, чтобы его делать.

Каждое задание построено так:

- Сначала я объясняю в двух словах, что мы делаем.
- Потом я показываю, какой новый элемент или идею ты сегодня выучишь.
- Потом я объясняю эту идею, как взрослому человеку (то есть без сложных слов).
- Потом ты видишь пример кода.
- Потом я даю тебе четыре-пять идей, что попробовать: поменять тут, добавить там, посмотреть, что будет.

**Главное правило**: Не торопись. Если не понял — перечитай. Если всё ещё не понял — напиши код, посмотри, что
произойдёт. Код — лучший учитель.

Удачи! 🚀
