# 🎮 Уровень 4: JavaScript введение — оживляем сайт

## Что такое JavaScript и почему это крутая штука

HTML — это скелет, CSS — это красота, а JavaScript — это мозг сайта. Когда ты кликаешь на кнопку, открывается меню,
меняется текст, крутится анимация — это всё JavaScript.

Аналогия: HTML — это пачка бумаги с текстом, CSS — это красивая обложка, JavaScript — это магия, которая заставляет
текст менять цвет, когда ты на него смотришь.

На этом уровне ты выучишь самое-самое базовое: как хранить информацию (переменные), как её обрабатывать (операции), и
как показывать результат (alert).

---

## Модуль 1: Первое знакомство с переменными

### Задание 1.1: Алерт — окно с сообщением

**Что мы делаем:**
Самый простой способ увидеть результат JavaScript — это показать алерт. Когда ты нажимаешь на кнопку, всплывает окошко с
текстом.

**Новый элемент / новое понятие:**

- **`alert()`** — функция, которая показывает окно с сообщением.
- **Скрипт (script)** — код JavaScript.
- **Тег `<script>`** — тег, в который пишут JavaScript код.

**Простое объяснение:**
`alert()` — это как крик "Привет!" на весь мир. Браузер услышит и покажет окошко.

**Пример кода:**

HTML:

```html
<html>
  <head>
    <title>Мой первый скрипт</title>
  </head>
  <body>
    <h1>JavaScript для новичков</h1>
    <button onclick="alert('Привет, мир!')">Нажми на меня</button>
  </body>
</html>
```

Открой это в браузере, нажми на кнопку — появится окошко с текстом "Привет, мир!".

**Что попробовать:**

1. Измени текст в `alert()` на что-нибудь своё.
2. Нажми на кнопку несколько раз. Каждый раз ли появляется окошко?
3. Напиши `alert('Число: 42')`. Работает ли с числами?

---

### Задание 1.2: Переменные — контейнеры для информации

**Что мы делаем:**
Переменная — это как коробка, в которую ты складываешь информацию. Потом ты можешь достать эту информацию, прочитать её,
изменить её.

**Новый элемент / новое понятие:**

- **Переменная** — контейнер, в котором хранится значение.
- **`var`** — способ создать переменную (старый способ, но работает везде).
- **`let`** — способ создать переменную (новый способ, лучше).
- **`const`** — переменная, которая не меняется.
- **Присваивание** — положить значение в переменную (знак `=`).

**Простое объяснение:**

```javascript
let name = "Иван";  // Создали переменную name, положили в неё "Иван"
let age = 25;       // Создали переменную age, положили в неё 25
```

Потом ты можешь использовать `name` и `age` везде, где нужны их значения.

**Пример кода:**

HTML:

```html
<html>
  <head>
    <title>Переменные</title>
  </head>
  <body>
    <h1>Привет!</h1>
    <button onclick="greet()">Нажми для приветствия</button>

    <script>
      function greet() {
        let name = "Мария";
        let age = 20;
        alert("Меня зовут " + name + " и мне " + age + " лет");
      }
    </script>
  </body>
</html>
```

Открой в браузере, нажми на кнопку — увидишь алерт с информацией.

**Что попробовать:**

1. Измени значения переменных (name и age).
2. Добавь новую переменную, например `let city = "Москва";` и включи её в алерт.
3. Создай переменную с числом: `let number = 42;` и покажи её в алерте.

---

### Задание 1.3: Операции с числами

**Что мы делаем:**
Переменные могут содержать не только текст, но и числа. С числами можно делать операции: складывать, вычитать, умножать,
делить.

**Новый элемент / новое понятие:**

- **`+`** — сложение.
- **`-`** — вычитание.
- **`*`** — умножение.
- **`/`** — деление.
- **`%`** — остаток от деления (модуль).

**Простое объяснение:**
Как в школе: 2 + 2 = 4. В JavaScript: `2 + 2` = 4.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="calculate()">Посчитай</button>

    <script>
      function calculate() {
        let a = 10;
        let b = 5;

        let sum = a + b;           // 10 + 5 = 15
        let difference = a - b;    // 10 - 5 = 5
        let product = a * b;       // 10 * 5 = 50
        let quotient = a / b;      // 10 / 5 = 2
        let remainder = a % b;     // 10 % 5 = 0

        alert("Сумма: " + sum);
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай переменные `a` и `b` с другими числами.
2. Показывай в алертах не сумму, а разность, произведение, деление.
3. Напиши: `let result = 100 % 7;` и покажи результат. Что такое остаток?

---

## Модуль 2: Работа с текстом и конкатенация

### Задание 2.1: Склеиваем текст — конкатенация

**Что мы делаем:**
Часто нужно объединить несколько текстовых строк в одну. Это называется конкатенация (склеивание).

**Новый элемент / новое понятие:**

- **Конкатенация** — объединение строк.
- **`+` для текста** — склеивает строки в одну.

**Простое объяснение:**

```javascript
let greeting = "Привет, " + "мир!";  // "Привет, мир!"
```

Это как взять две ленточки и сплести их в одну.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="introduce()">Представься</button>

    <script>
      function introduce() {
        let firstName = "Иван";
        let lastName = "Петров";
        let age = 30;

        let message = "Меня зовут " + firstName + " " + lastName + ". " +
                      "Мне " + age + " лет.";

        alert(message);
      }
    </script>
  </body>
</html>
```

Открой в браузере, нажми на кнопку — увидишь полное представление.

**Что попробовать:**

1. Добавь новые переменные (город, профессия).
2. Объедини их в одно сообщение.
3. Создай сообщение с числами и текстом: `"У меня " + 5 + " яблок"`.

---

### Задание 2.2: Шаблонные строки — более красивый способ

**Что мы делаем:**
Вместо конкатенации с `+` можно использовать шаблонные строки. Это красивее и удобнее.

**Новый элемент / новое понятие:**

- **Шаблонная строка** — строка в обратных кавычках `` ` ``.
- **`${переменная}`** — вставить значение переменной.

**Простое объяснение:**
Вместо:

```javascript
"Привет, " + name + "! Тебе " + age + " лет."
```

Пишешь:

```javascript
`Привет, ${name}! Тебе ${age} лет.`
```

Второй вариант читается как обычный текст с дырками.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="greet()">Приветствие</button>

    <script>
      function greet() {
        let name = "Мария";
        let city = "Санкт-Петербург";
        let hobby = "рисование";

        let message = `Привет! Меня зовут ${name}. Я из ${city} и мне нравится ${hobby}.`;

        alert(message);
      }
    </script>
  </body>
</html>
```

Видишь? Это намного понятнее, чем конкатенация.

**Что попробовать:**

1. Создай сообщение про себя, используя шаблонные строки.
2. Объедини текст, числа и переменные в одной строке.
3. Напиши длинное сообщение (3-4 предложения) со множеством переменных.

---

## Модуль 3: Вывод информации

### Задание 3.1: Console.log — вывод в консоль

**Что мы делаем:**
`alert()` хороша, но она прерывает работу. Для отладки удобнее использовать консоль браузера. Это место, где пишется
вся "техническая" информация.

**Новый элемент / новое понятие:**

- **`console.log()`** — вывести в консоль.
- **Консоль браузера** — специальное окошко для отладки. Открывается F12 или Ctrl+Shift+J.

**Простое объяснение:**
`console.log()` — это как писать в тетрадь для себя. Никто не видит, но ты можешь посмотреть, если захочешь.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <h1>Открой консоль (F12)</h1>
    <button onclick="testLog()">Нажми</button>

    <script>
      function testLog() {
        let name = "Петр";
        let age = 25;
        let score = 150;

        console.log("Вывод в консоль:");
        console.log("Имя: " + name);
        console.log("Возраст: " + age);
        console.log("Очки: " + score);
        console.log(`Всё вместе: ${name}, ${age} лет, ${score} очков`);
      }
    </script>
  </body>
</html>
```

Открой в браузере, нажми F12 (откроется консоль), потом нажми на кнопку. В консоли увидишь все сообщения.

**Что попробовать:**

1. Напиши несколько `console.log()` с разной информацией.
2. Выведи в консоль результаты математических операций.
3. Создай переменные и выведи их всё в консоль.

---

### Задание 3.2: Document.write — вывод на страницу

**Что мы делаем:**
Можно вывести текст прямо на страницу, не используя alert и не открывая консоль.

**Новый элемент / новое понятие:**

- **`document.write()`** — вывести текст на страницу.

**Простое объяснение:**
`document` — это сама веб-страница. `write()` — пиши на неё. Результат появляется прямо на странице.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <h1>Вывод на страницу</h1>
    <button onclick="writeToPage()">Напиши что-нибудь</button>

    <script>
      function writeToPage() {
        let name = "Алекс";
        let message = `Привет, я ${name}!`;
        document.write(message);
      }
    </script>
  </body>
</html>
```

Нажми на кнопку — текст появится на странице.

**Внимание:** `document.write()` может быть опасна, если вызвать её после загрузки страницы — она сотрёт всю страницу.
Поэтому будь осторожна!

**Что попробовать:**

1. Выведи на страницу несколько строк текста.
2. Используй переменные в выводе.
3. Попробуй `document.write()` с разными данными.

---

## Модуль 4: Упражнения и проекты

### Задание 4.1: Калькулятор с переменными

**Что мы делаем:**
Создаём простой калькулятор, который складывает, вычитает, умножает и делит два числа.

**Пример кода:**

HTML:

```html
<html>
  <head>
    <title>Калькулятор</title>
    <style>
      button { padding: 10px 20px; font-size: 16px; margin: 5px; }
    </style>
  </head>
  <body>
    <h1>Простой калькулятор</h1>
    
    <button onclick="addNumbers()">Сложи 10 и 5</button>
    <button onclick="multiplyNumbers()">Умножь 10 и 5</button>
    <button onclick="divideNumbers()">Раздели 10 на 5</button>
    <button onclick="customCalculate()">Посчитай с своими числами</button>

    <script>
      function addNumbers() {
        let a = 10;
        let b = 5;
        let result = a + b;
        alert(`${a} + ${b} = ${result}`);
      }

      function multiplyNumbers() {
        let a = 10;
        let b = 5;
        let result = a * b;
        alert(`${a} × ${b} = ${result}`);
      }

      function divideNumbers() {
        let a = 10;
        let b = 5;
        let result = a / b;
        alert(`${a} ÷ ${b} = ${result}`);
      }

      function customCalculate() {
        let x = 20;
        let y = 8;
        let sum = x + y;
        let diff = x - y;
        let prod = x * y;
        let quot = x / y;

        console.log(`Числа: ${x} и ${y}`);
        console.log(`Сумма: ${sum}`);
        console.log(`Разность: ${diff}`);
        console.log(`Произведение: ${prod}`);
        console.log(`Частное: ${quot}`);

        alert(`Все операции выведены в консоль (F12)`);
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Добавь кнопку для вычитания.
2. Измени числа в переменных.
3. Создай функцию, которая считает площадь прямоугольника.

---

### Задание 4.2: История о тебе

**Что мы делаем:**
Создаём маленькую программу, которая собирает информацию о тебе в одну историю.

**Пример кода:**

HTML:

```html
<html>
  <head>
    <title>История про тебя</title>
    <style>
      body { font-family: Arial; max-width: 600px; margin: 50px auto; }
      button { padding: 10px 20px; font-size: 16px; }
      #story { background-color: #f5f5f5; padding: 20px; margin-top: 20px; border-radius: 8px; }
    </style>
  </head>
  <body>
    <h1>Расскажи о себе</h1>
    <button onclick="tellStory()">Создать историю</button>
    <div id="story"></div>

    <script>
      function tellStory() {
        let name = "Иван";
        let age = 25;
        let city = "Москва";
        let hobby = "программирование";
        let pet = "кот Васька";
        let dream = "создавать крутые сайты";

        let story = `
          <h2>История про ${name}</h2>
          <p>
            Меня зовут ${name}. Мне ${age} лет, и я живу в ${city}.
            Больше всего на свете мне нравится ${hobby}.
          </p>
          <p>
            У меня есть ${pet}, который часто сидит рядом, когда я работаю.
            Моя мечта — ${dream}.
          </p>
          <p>Это была история про ${name}. Конец.</p>
        `;

        document.getElementById("story").innerHTML = story;
      }
    </script>
  </body>
</html>
```

Открой в браузере, нажми на кнопку — история появится на странице.

**Что попробовать:**

1. Измени информацию о себе.
2. Добавь больше переменных (рост, любимый цвет, друг и так далее).
3. Сделай историю смешной или грустной.

---

## Итоговая проверка

**Задание:** Создай свою программу с переменными и выводом:

- [ ] Минимум 5 переменных с разными данными
- [ ] Минимум одна математическая операция
- [ ] Использование шаблонных строк
- [ ] Вывод через `alert()` или `console.log()` или оба
- [ ] Минимум одна кнопка, которая что-то запускает
- [ ] Понятный результат

Когда закончишь, покажи кому-нибудь — пусть нажмет на кнопку!

---

Отлично! Ты выучил основы JavaScript. Теперь переходи на следующий уровень, где мы разберём типы данных поглубже!
