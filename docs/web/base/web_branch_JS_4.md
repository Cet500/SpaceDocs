# 🎮 Уровень 7: JavaScript продвинутый — функции

## Что такое функции и почему они гениальны

Функция — это блок кода, который можешь использовать снова и снова. Вместо того, чтобы писать одно и то же по 10 раз, ты
пишешь один раз и вызываешь его, когда нужно.

Аналогия: функция — это рецепт. Ты один раз пишешь рецепт блинчиков, потом каждое утро просто следуешь ему, не
переписывая рецепт заново.

На этом уровне ты выучишь:

- Как создавать функции
- Как передавать параметры
- Как получать результаты (return)
- Область видимости переменных

---

## Модуль 1: Создание и вызов функций

### Задание 1.1: Простая функция

**Что мы делаем:**
Создаём простую функцию, которая выполняет какое-то действие.

**Новый элемент / новое понятие:**

- **`function`** — ключевое слово для создания функции.
- **Имя функции** — как её вызывать (должно быть описательным).
- **Скобки `()`** — входные данные (параметры).
- **Фигурные скобки `{}`** — тело функции (код, который выполняется).
- **Вызов функции** — `functionName()`.

**Простое объяснение:**

```javascript
// Создаём функцию
function sayHello() {
  console.log("Привет!");
}

// Вызываем функцию
sayHello();  // "Привет!"
sayHello();  // "Привет!" (можем вызвать снова)
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="myFunction()">Нажми</button>

    <script>
      // Функция без параметров
      function myFunction() {
        console.log("Функция была вызвана!");
        alert("Привет из функции!");
      }

      function printStars() {
        console.log("★ ★ ★ ★ ★");
      }

      // Вызываем функции
      printStars();  // ★ ★ ★ ★ ★
      printStars();  // ★ ★ ★ ★ ★
      printStars();  // ★ ★ ★ ★ ★
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай функцию, которая печатает твоё имя.
2. Вызови функцию несколько раз.
3. Создай функцию, которая выполняет математическую операцию.

---

### Задание 1.2: Функции с параметрами

**Что мы делаем:**
Функции могут принимать данные (параметры) и использовать их внутри.

**Новый элемент / новое понятие:**

- **Параметр** — входные данные функции.
- **Аргумент** — значение, которое ты передаёшь при вызове.
- **`function name(param1, param2)`** — функция с параметрами.
- **`name(value1, value2)`** — вызов с аргументами.

**Простое объяснение:**

```javascript
function greet(name) {           // name — параметр
  console.log("Привет, " + name);
}

greet("Иван");                   // Иван — аргумент
greet("Мария");
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testFunctions()">Функции с параметрами</button>

    <script>
      // Функция с одним параметром
      function sayName(name) {
        console.log("Меня зовут " + name);
      }

      sayName("Иван");      // Меня зовут Иван
      sayName("Мария");     // Меня зовут Мария

      // Функция с двумя параметрами
      function add(a, b) {
        console.log("Сумма: " + (a + b));
      }

      add(10, 5);           // Сумма: 15
      add(100, 50);         // Сумма: 150

      // Функция с тремя параметрами
      function describe(name, age, hobby) {
        let description = name + " в возрасте " + age + " любит " + hobby;
        console.log(description);
      }

      describe("Петр", 25, "рисование");
      describe("Анна", 30, "музыку");

      function testFunctions() {
        sayName("Из функции!");
        add(7, 8);
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай функцию с двумя параметрами.
2. Создай функцию с тремя параметрами.
3. Вызови функцию с разными значениями.

---

### Задание 1.3: Функции с return (возврат результата)

**Что мы делаем:**
Функции могут не только печатать результат, но и возвращать его. Тогда ты можешь использовать результат дальше в коде.

**Новый элемент / новое понятие:**

- **`return`** — вернуть значение из функции.
- **После return** — функция останавливается, дальнейший код не выполняется.
- **Результат функции** — можно сохранить в переменную или использовать дальше.

**Простое объяснение:**

```javascript
function multiply(a, b) {
  return a * b;  // Вернуть результат
}

let result = multiply(5, 3);  // result = 15
console.log(result);          // 15
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testReturn()">Return значения</button>

    <script>
      // Функция, которая возвращает результат
      function multiply(a, b) {
        return a * b;
      }

      let result1 = multiply(5, 3);
      console.log("5 * 3 = " + result1);  // 15

      let result2 = multiply(10, 7);
      console.log("10 * 7 = " + result2); // 70

      // Функция, которая возвращает текст
      function getGreeting(name) {
        return "Привет, " + name + "!";
      }

      let message = getGreeting("Иван");
      console.log(message);  // Привет, Иван!

      // Функция с условием и return
      function checkAge(age) {
        if (age >= 18) {
          return "Ты взрослый";
        } else {
          return "Ты ребёнок";
        }
      }

      console.log(checkAge(25));  // Ты взрослый
      console.log(checkAge(10));  // Ты ребёнок

      // Return останавливает функцию
      function earlyReturn() {
        console.log("Начало");
        return "Выход";
        console.log("Эта строка не выполнится!");  // Не будет выведена
      }

      console.log(earlyReturn());

      function testReturn() {
        let sum = multiply(15, 4);
        alert("Результат: " + sum);
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай функцию, которая возвращает сумму двух чисел.
2. Создай функцию, которая возвращает полное имя (объедини имя и фамилию).
3. Создай функцию, которая возвращает результат условия (true/false).

---

## Модуль 2: Области видимости переменных

### Задание 2.1: Глобальные и локальные переменные

**Что мы делаем:**
Переменные могут быть видны везде в коде (глобальные) или только внутри функции (локальные).

**Новый элемент / новое понятие:**

- **Глобальная переменная** — создана вне функции, видна везде.
- **Локальная переменная** — создана внутри функции, видна только в функции.

**Простое объяснение:**

```javascript
let global = "я везде видна";      // Глобальная

function myFunc() {
  let local = "я только в функции";  // Локальная
  console.log(global);              // Видна
  console.log(local);               // Видна
}

myFunc();
console.log(global);                // Видна
console.log(local);                 // Ошибка! local не определена
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testScope()">Область видимости</button>

    <script>
      let globalVar = "Я глобальная переменная";

      function scopeTest() {
        let localVar = "Я локальная переменная";

        console.log("Внутри функции:");
        console.log(globalVar);  // ✓ видна
        console.log(localVar);   // ✓ видна
      }

      console.log("Вне функции:");
      console.log(globalVar);    // ✓ видна
      // console.log(localVar);  // ✗ ошибка! не видна

      scopeTest();

      // Параметры — локальные переменные
      function withParam(param) {
        console.log("param: " + param);
      }

      withParam("hello");
      // console.log(param);    // ✗ ошибка! не видна вне функции

      function testScope() {
        scopeTest();
        alert("Посмотри консоль");
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай глобальную переменную.
2. Создай локальную переменную внутри функции.
3. Посмотри, когда переменная видна, а когда — нет.

---

## Модуль 3: Практические примеры

### Задание 3.1: Функции для расчётов

**Что мы делаем:**
Создаём набор функций для разных расчётов.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testCalculations()">Расчёты</button>

    <script>
      // Функция для расчёта площади прямоугольника
      function areaOfRectangle(width, height) {
        return width * height;
      }

      // Функция для расчёта площади круга
      function areaOfCircle(radius) {
        return 3.14 * radius * radius;
      }

      // Функция для расчёта расстояния
      function distance(x1, y1, x2, y2) {
        let dx = x2 - x1;
        let dy = y2 - y1;
        return Math.sqrt(dx * dx + dy * dy);
      }

      // Функция для расчёта средней оценки
      function averageGrade(grade1, grade2, grade3) {
        return (grade1 + grade2 + grade3) / 3;
      }

      // Функция для расчёта скидки
      function applyDiscount(price, discountPercent) {
        let discount = price * (discountPercent / 100);
        return price - discount;
      }

      function testCalculations() {
        console.log("Площадь прямоугольника (5x10): " + areaOfRectangle(5, 10));
        console.log("Площадь круга (радиус 5): " + areaOfCircle(5));
        console.log("Расстояние между (0,0) и (3,4): " + distance(0, 0, 3, 4));
        console.log("Средняя оценка (5, 4, 3): " + averageGrade(5, 4, 3));
        console.log("Цена с 20% скидкой (100): " + applyDiscount(100, 20));
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Добавь новую функцию для расчёта объёма куба.
2. Добавь функцию для расчёта числа Фибоначчи.
3. Создай функцию для расчёта НДС (налог).

---

### Задание 3.2: Функции с проверками

**Что мы делаем:**
Функции, которые проверяют условия и возвращают результаты.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testValidation()">Проверки</button>

    <script>
      // Проверка, является ли число чётным
      function isEven(num) {
        return num % 2 === 0;
      }

      // Проверка, является ли число простым
      function isPrime(num) {
        if (num <= 1) return false;
        if (num === 2) return true;
        if (num % 2 === 0) return false;
        
        for (let i = 3; i * i <= num; i += 2) {
          if (num % i === 0) return false;
        }
        return true;
      }

      // Проверка пароля на крепость
      function isStrongPassword(password) {
        if (password.length < 8) return false;
        let hasUppercase = /[A-Z]/.test(password);
        let hasLowercase = /[a-z]/.test(password);
        let hasNumber = /[0-9]/.test(password);
        
        return hasUppercase && hasLowercase && hasNumber;
      }

      // Проверка email
      function isValidEmail(email) {
        return email.includes("@") && email.includes(".");
      }

      // Проверка возраста
      function canVote(age) {
        return age >= 18;
      }

      function testValidation() {
        console.log("10 чётное? " + isEven(10));        // true
        console.log("7 чётное? " + isEven(7));          // false
        console.log("7 простое? " + isPrime(7));        // true
        console.log("4 простое? " + isPrime(4));        // false
        console.log("Пароль крепкий? " + isStrongPassword("MyPass123"));  // true
        console.log("Email правильный? " + isValidEmail("test@email.com"));  // true
        console.log("Можно голосовать (20)? " + canVote(20));  // true
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Добавь функцию для проверки мобильного номера.
2. Добавь функцию для проверки, является ли строка палиндромом.

---

### Задание 3.3: Функции для работы со строками

**Что мы делаем:**
Функции, которые работают с текстом.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testStringFunctions()">Строки</button>

    <script>
      // Поменять регистр
      function toggleCase(text) {
        let result = "";
        for (let i = 0; i < text.length; i++) {
          let char = text[i];
          if (char === char.toUpperCase()) {
            result += char.toLowerCase();
          } else {
            result += char.toUpperCase();
          }
        }
        return result;
      }

      // Перевернуть строку
      function reverseString(text) {
        let reversed = "";
        for (let i = text.length - 1; i >= 0; i--) {
          reversed += text[i];
        }
        return reversed;
      }

      // Повторить строку N раз
      function repeatString(text, times) {
        let result = "";
        for (let i = 0; i < times; i++) {
          result += text;
        }
        return result;
      }

      // Подсчитать количество слов
      function countWords(text) {
        return text.split(" ").length;
      }

      // Удалить пробелы
      function removeSpaces(text) {
        let result = "";
        for (let i = 0; i < text.length; i++) {
          if (text[i] !== " ") {
            result += text[i];
          }
        }
        return result;
      }

      function testStringFunctions() {
        console.log("Поменять регистр 'Hello': " + toggleCase("Hello"));
        console.log("Перевернуть 'Hello': " + reverseString("Hello"));
        console.log("Повторить 'Ha' 5 раз: " + repeatString("Ha", 5));
        console.log("Слов в 'Hello world': " + countWords("Hello world"));
        console.log("Без пробелов 'H e l l o': " + removeSpaces("H e l l o"));
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Добавь функцию для капитализации первой буквы.
2. Добавь функцию для замены букв.

---

## Модуль 4: Структура кода с функциями

### Задание 4.1: Полная программа с функциями

**Что мы делаем:**
Собираем всё вместе в одну полноценную программу.

**Пример кода:**

HTML:

```html
<html>
  <head>
    <title>Калькулятор</title>
    <style>
      body { font-family: Arial; max-width: 400px; margin: 50px auto; }
      input { padding: 8px; margin: 5px; width: 150px; }
      button { padding: 10px 20px; margin: 5px; }
      #result { font-size: 20px; font-weight: bold; margin-top: 20px; }
    </style>
  </head>
  <body>
    <h1>Простой калькулятор</h1>
    
    <input type="number" id="num1" placeholder="Число 1">
    <input type="number" id="num2" placeholder="Число 2">
    
    <button onclick="performCalculation('+')">+</button>
    <button onclick="performCalculation('-')">-</button>
    <button onclick="performCalculation('*')">×</button>
    <button onclick="performCalculation('/'">÷</button>
    
    <div id="result"></div>

    <script>
      // Получить число из инпута
      function getInputNumber(elementId) {
        let value = document.getElementById(elementId).value;
        return Number(value);
      }

      // Показать результат
      function showResult(value) {
        document.getElementById("result").innerHTML = "Результат: " + value;
      }

      // Выполнить операцию
      function performOperation(a, b, operation) {
        if (operation === "+") return a + b;
        if (operation === "-") return a - b;
        if (operation === "*") return a * b;
        if (operation === "/") {
          if (b === 0) return "Ошибка: деление на ноль";
          return a / b;
        }
      }

      // Главная функция
      function performCalculation(operation) {
        let num1 = getInputNumber("num1");
        let num2 = getInputNumber("num2");
        
        if (isNaN(num1) || isNaN(num2)) {
          showResult("Введи оба числа");
          return;
        }

        let result = performOperation(num1, num2, operation);
        showResult(result);
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Добавь операцию возведения в степень.
2. Добавь историю вычислений.
3. Сделай очистку полей.

---

## Итоговая проверка

**Задание:** Создай программу, которая демонстрирует всё, чему ты выучился:

- [ ] Минимум 5 функций
- [ ] Функции с параметрами
- [ ] Функции с return
- [ ] Глобальные и локальные переменные
- [ ] Функции, которые вызывают другие функции
- [ ] Условия внутри функций
- [ ] Циклы внутри функций

Примеры: калькулятор, проверка данных, обработка строк, работа со списками.

---

Отлично! Ты выучил основы функций. Это большой шаг к мастерству. Теперь ты готов к реальным проектам!
