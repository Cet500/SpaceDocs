# 🎨 Уровень 2: CSS базовый — делаем красиво

## Что такое CSS и почему это не HTML

HTML говорит браузеру, "что" есть на странице. CSS говорит "как" это выглядит.

Аналогия: HTML — это список ингредиентов в рецепте ("яйцо, мука, сахар"), а CSS — это инструкции, как их готовить и как
подать на стол ("яйцо взбить, муку просеять, выложить красиво на тарелку").

На HTML странице всё выглядит скучно: серый текст, синие ссылки, стандартный размер. CSS делает текст цветным,
раскладывает элементы по-другому, добавляет красивые края, тени и так далее.

---

## Модуль 1: Первое встреча с CSS

### Задание 1.1: Создаём файл CSS и подключаем его к HTML

**Что мы делаем:**
CSS обычно пишется в отдельном файле (например, `style.css`). Потом этот файл "подключают" к HTML через специальный тег
в head.

**Новый элемент / новое понятие:**

- **CSS-файл** — текстовый файл с расширением `.css`, в котором пишутся инструкции стиля.
- **Тег `<link>`** — тег в head, который говорит: "Используй этот CSS-файл для оформления этой страницы".
- **Атрибут `rel="stylesheet"`** — означает "это таблица стилей (CSS)".
- **Атрибут `href`** — путь к CSS-файлу.

**Простое объяснение:**
Представь: на столе лежит статуя (это HTML). Рядом стоит банка краски (это CSS-файл). Если краска не соединена со
статуей, она не поможет. Но если ты возьмёшь кисть и помажешь статую этой краской, она станет красивой. Тег `<link>` —
это кисть, которая соединяет HTML и CSS.

**Пример кода:**

Сначала создай файл `style.css`:

```css
body {
  background-color: lightblue;
}

h1 {
  color: darkblue;
}
```

Потом создай файл `index.html`:

```html
<html>
  <head>
    <title>Мой красивый сайт</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <h1>Привет!</h1>
    <p>Это красиво!</p>
  </body>
</html>
```

Открой `index.html` в браузере. Видишь, что фон стал голубым, а заголовок — тёмно-синим?

**Что попробовать:**

1. Измени `lightblue` на `green`, `red`, `yellow`. Что произойдёт?
2. Добавь новое правило в CSS: `p { color: red; }`. Видишь ли красный текст в абзацах?
3. Забудь подключить CSS (удали строку `<link>`). Вернётся ли сайт к серому виду?

---

### Задание 1.2: Селекторы и свойства

**Что мы делаем:**
CSS состоит из правил. Каждое правило говорит: "Все элементы вот-так должны выглядеть вот-так". Селектор — это "что",
свойства — это "как".

**Новый элемент / новое понятие:**

- **Селектор** — название тега, который ты хочешь оформить (например, `h1`, `p`, `body`).
- **Свойство** — то, что ты меняешь (например, `color`, `background-color`, `font-size`).
- **Значение** — новое значение свойства (например, `red`, `20px`, `Arial`).
- **Синтаксис:** `селектор { свойство: значение; }`

**Простое объяснение:**
Селектор — это "кого красим" (все заголовки h1), свойство — "что красим" (цвет), значение — "на что красим" (синий).

**Пример кода:**

```css
h1 {
  color: blue;
  font-size: 48px;
  text-align: center;
}

p {
  color: gray;
  font-size: 18px;
}

body {
  background-color: lightyellow;
}
```

А в HTML:

```html
<html>
  <head>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <h1>Заголовок будет синий, большой и в центре</h1>
    <p>Текст будет серый и среднего размера</p>
  </body>
</html>
```

**Что попробовать:**

1. Добавь ещё свойства к h1: `margin-top: 30px;`, `margin-bottom: 30px;` (это расстояния сверху и снизу).
2. Измени `text-align: center;` на `text-align: left;` или `text-align: right;`. Что изменится?
3. Добавь новый селектор для ссылок: `a { color: green; }`. Ссылки станут зелёными?

---

## Модуль 2: Цвета и текст

### Задание 2.1: Цвета текста и фона

**Что мы делаем:**
Самое простое — менять цвета. Есть `color` (цвет текста) и `background-color` (цвет фона).

**Новый элемент / новое понятие:**

- **`color`** — цвет текста.
- **`background-color`** — цвет фона (позади текста).
- **Названия цветов** — можно писать названия (red, blue, green) или коды (например, `#FF5733`).

**Простое объяснение:**
`color` — это как маркер, которым ты писал бы. `background-color` — это как фломастер, которым ты бы закрашивал фон
позади текста.

Браузер знает много названий цветов: red, blue, green, yellow, purple, orange, pink, black, white, gray и так далее.

**Пример кода:**

```css
h1 {
  color: white;
  background-color: darkblue;
  padding: 20px;
}

p {
  color: darkgreen;
  background-color: lightyellow;
}

.warning {
  color: white;
  background-color: red;
}
```

А в HTML:

```html
<h1>Белый текст на тёмно-синем фоне</h1>
<p>Тёмно-зелёный текст на светло-жёлтом фоне</p>
<p class="warning">Белый на красном (внимание!)</p>
```

Видишь `.warning`? Это называется "класс", мы про него скоро поговорим. А `padding: 20px;` — это расстояние внутри,
чтобы текст не прижимался к краям.

**Что попробовать:**

1. Найди в Интернете таблицу цветов HTML (например, "html color names"). Используй несколько интересных цветов.
2. Сделай фон белым, текст чёрным. Это скучно?
3. Добавь фон к абзацам (не только к h1).

---

### Задание 2.2: Размер и шрифт текста

**Что мы делаем:**
Текст можно делать больше или меньше, можно менять шрифт (например, Arial, Times New Roman).

**Новый элемент / новое понятие:**

- **`font-size`** — размер текста (например, `16px`, `20px`, `48px`).
- **`font-family`** — шрифт (например, `Arial`, `Georgia`, `Times New Roman`).
- **`px`** — пиксели, единица измерения размера на экране.

**Простое объяснение:**
`font-size: 48px;` означает "текст высотой 48 пикселей" (то есть очень крупный).

`font-family: Arial;` означает "используй шрифт Arial" (это стандартный, понятный шрифт).

**Пример кода:**

```css
h1 {
  font-size: 48px;
  font-family: Arial;
}

h2 {
  font-size: 32px;
  font-family: Georgia;
}

p {
  font-size: 16px;
  font-family: Arial;
}

.big-text {
  font-size: 24px;
}

.small-text {
  font-size: 12px;
}
```

А в HTML:

```html
<h1>Большой заголовок Arial</h1>
<h2>Заголовок поменьше Georgia</h2>
<p>Обычный текст Arial</p>
<p class="big-text">Большой текст</p>
<p class="small-text">Маленький текст</p>
```

**Что попробовать:**

1. Сделай h1 размером 60px, потом 30px. Какой нравится больше?
2. Измени все шрифты на `Georgia` и посмотри, как изменится вид сайта.
3. Сделай маленький текст в 10px. Видишь ли его?

---

### Задание 2.3: Жирный, курсив, подчёркивание

**Что мы делаем:**
Текст можно делать жирным, курсивом или подчёркнутым.

**Новый элемент / новое понятие:**

- **`font-weight: bold;`** — жирный текст.
- **`font-style: italic;`** — курсив.
- **`text-decoration: underline;`** — подчёркивание.

**Простое объяснение:**
Это как форматирование в Word: жирный, курсив, подчёркивание.

**Пример кода:**

```css
strong-text {
  font-weight: bold;
}

.italic-text {
  font-style: italic;
}

.underline-text {
  text-decoration: underline;
}

h1 {
  font-weight: bold;
}
```

А в HTML:

```html
<p class="strong-text">Это будет жирно</p>
<p class="italic-text">Это будет курсивом</p>
<p class="underline-text">Это будет подчёркнуто</p>
```

**Что попробовать:**

1. Сочетай жирный и курсив вместе (в одном селекторе два свойства).
2. Подчеркни ссылки (`a { text-decoration: underline; }`).
3. Сделай заголовки жирными.

---

## Модуль 3: Расстояния и размеры

### Задание 3.1: Margin и Padding — расстояния

**Что мы делаем:**
Элементы на странице редко прилипают друг к другу. Между ними бывают расстояния. Есть два способа создавать расстояния:

- `padding` — расстояние *внутри* элемента (между текстом и краями).
- `margin` — расстояние *снаружи* элемента (между элементом и его соседями).

**Новый элемент / новое понятие:**

- **`padding`** — пространство внутри элемента.
- **`margin`** — пространство вокруг элемента (снаружи).
- **`padding-top`, `padding-bottom`, `padding-left`, `padding-right`** — расстояния внутри с каждой стороны.
- **`margin-top`, `margin-bottom`, `margin-left`, `margin-right`** — расстояния вокруг с каждой стороны.

**Простое объяснение:**
Представь картину в раме. Рама — это padding (внутри рамы). Расстояние между картиной и стеной — это margin (вокруг
рамы).

**Пример кода:**

```css
h1 {
  padding: 20px;
  margin-bottom: 30px;
  background-color: lightblue;
}

p {
  margin-top: 10px;
  margin-bottom: 10px;
  padding-left: 15px;
}

.box {
  padding: 20px;
  margin: 30px;
  background-color: lightyellow;
}
```

А в HTML:

```html
<h1>Заголовок с внутренним расстоянием и фоном</h1>
<p>Текст с расстояниями</p>
<div class="box">Блок с padding и margin</div>
```

Видишь, как элементы не прижимаются друг к другу?

**Что попробовать:**

1. Измени `padding: 20px;` на `padding: 50px;`. Стало больше пустого места внутри?
2. Сделай `margin-bottom: 50px;` для h1. Увеличилось расстояние до следующего элемента?
3. Сделай `padding: 0;` и `margin: 0;`. Элемент прилипнет к соседям?

---

### Задание 3.2: Ширина и высота элементов

**Что мы делаем:**
Элементы имеют ширину и высоту. Обычно браузер сам определяет размеры, но ты можешь их менять.

**Новый элемент / новое понятие:**

- **`width`** — ширина элемента.
- **`height`** — высота элемента.
- **`max-width`** — максимальная ширина (не будет шире, чем это значение).

**Простое объяснение:**
`width: 200px;` означает "делай этот элемент шириной 200 пикселей".

`max-width: 600px;` означает "не делай шире 600 пикселей, даже если экран больше".

**Пример кода:**

```css
p {
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

img {
  width: 200px;
  height: 200px;
}

.container {
  width: 100%;
  background-color: lightyellow;
  padding: 20px;
}
```

А в HTML:

```html
<p>Текст будет не шире 600px и отцентрирован</p>
<img src="cat.jpg">
<div class="container">Контейнер во всю ширину</div>
```

Видишь `margin-left: auto; margin-right: auto;`? Это центрирует элемент горизонтально.

**Что попробовать:**

1. Сделай картинку шириной 100px. Станет ли она маленькой?
2. Сделай элемент `width: 50%;`. Займёт ли он половину экрана?
3. Сделай `height: 200px;` для абзаца. Что произойдёт?

---

## Модуль 4: Классы и группировка

### Задание 4.1: Классы — выделение специальных элементов

**Что мы делаем:**
Иногда нужно стилизовать не все элементы одного типа, а только некоторые. Для этого используют классы.

**Новый элемент / новое понятие:**

- **Класс** — "ярлычок", который ты добавляешь к элементу в HTML.
- **Селектор класса в CSS** — пишется через точку: `.название-класса`.
- **Атрибут `class` в HTML** — добавляешь класс к элементу так: `<p class="warning">`.

**Простое объяснение:**
Представь: у тебя есть 10 абзацев на странице. Но один из них — это предупреждение (warning). Ты не хочешь красить все
абзацы красным, только этот. Поэтому ты даёшь ему класс "warning" и в CSS пишешь стиль только для этого класса.

**Пример кода:**

HTML:

```html
<p>Обычный текст</p>
<p>Ещё обычный текст</p>
<p class="warning">Это предупреждение! Красный фон!</p>
<p>Опять обычный</p>
```

CSS:

```css
p {
  color: black;
  font-size: 16px;
}

.warning {
  background-color: red;
  color: white;
  padding: 10px;
}

.success {
  background-color: green;
  color: white;
  padding: 10px;
}
```

Видишь? Все `<p>` будут чёрные, но те, что с классом `.warning`, станут красными с белым текстом.

**Что попробовать:**

1. Добавь класс `.success` к одному из абзацев. Станет ли он зелёным?
2. Добавь класс к картинке: `<img src="cat.jpg" class="small-img">`. Потом в CSS: `.small-img { width: 100px; }`.
3. Добавь несколько абзацев с классом `.warning`. Все они будут красными?

---

### Задание 4.2: Несколько классов у одного элемента

**Что мы делаем:**
Элемент может иметь несколько классов. Это полезно, когда ты хочешь комбинировать стили.

**Новый элемент / новое понятие:**

- **Несколько классов** — пишутся через пробел в атрибуте `class`.

**Простое объяснение:**
Если я скажу "вот красная машина, большая машина", я описал машину двумя характеристиками. Так же и с классами: элемент
может быть и "warning" (красный), и "big-text" (большой).

**Пример кода:**

HTML:

```html
<p class="warning big-text">Это большое красное предупреждение</p>
<p class="success">Это зелёное успешное сообщение</p>
```

CSS:

```css
.warning {
  background-color: red;
  color: white;
}

.big-text {
  font-size: 24px;
  font-weight: bold;
}

.success {
  background-color: green;
  color: white;
}
```

Первый абзац будет красным И большим, потому что у него оба класса.

**Что попробовать:**

1. Создай класс `.italic-text { font-style: italic; }` и применил его вместе с `.warning`.
2. Создай три абзаца с разными комбинациями классов.

---

## Модуль 5: Расположение элементов

### Задание 5.1: Display — как элементы располагаются

**Что мы делаем:**
По умолчанию элементы располагаются один под другим (блоковые элементы) или рядом (встроенные элементы). Свойство
`display` меняет это поведение.

**Новый элемент / новое понятие:**

- **`display: block;`** — элемент занимает всю ширину строки и начинается с новой строки.
- **`display: inline;`** — элемент встроенный, занимает только столько места, сколько нужно его содержимому.
- **`display: inline-block;`** — гибрид: встроенный, но с возможностью задавать ширину и высоту.

**Простое объяснение:**
Block — это как кирпич (занимает всю ширину). Inline — это как текст в предложении (занимает столько, сколько нужно).
Inline-block — это как кирпич, который можно положить рядом с другими кирпичами.

**Пример кода:**

```css
a {
  display: inline-block;
  padding: 10px 20px;
  background-color: blue;
  color: white;
  margin: 5px;
}

span {
  display: block;
  background-color: lightyellow;
  padding: 10px;
  margin: 10px 0;
}
```

А в HTML:

```html
<a href="#">Кнопка 1</a>
<a href="#">Кнопка 2</a>
<a href="#">Кнопка 3</a>

<span>Я буду на отдельной строке</span>
<span>И я тоже</span>
```

Видишь ссылки рядом друг с другом (inline-block), а span'ы один под другим (block)?

**Что попробовать:**

1. Сделай `a { display: block; }`. Теперь ссылки будут под друг другом?
2. Сделай `a { display: inline; }`. Исчезнут ли padding и margin?
3. Сделай элемент `<strong>` с классом `button` и `display: inline-block;` для кнопочного эффекта.

---

### Задание 5.2: Text-align — выравнивание текста

**Что мы делаем:**
Текст можно выравнивать по левому краю, правому краю, по центру или по ширине.

**Новый элемент / новое понятие:**

- **`text-align: left;`** — выравнивание по левому краю (по умолчанию).
- **`text-align: center;`** — по центру.
- **`text-align: right;`** — по правому краю.
- **`text-align: justify;`** — по ширине.

**Простое объяснение:**
Как в Word: выбираешь выравнивание, и текст движется.

**Пример кода:**

```css
h1 {
  text-align: center;
}

.right-text {
  text-align: right;
}

.justify-text {
  text-align: justify;
}
```

А в HTML:

```html
<h1>Заголовок в центре</h1>
<p class="right-text">Текст справа</p>
<p class="justify-text">Длинный текст выравнен по ширине...</p>
```

**Что попробовать:**

1. Сделай все заголовки по центру.
2. Сделай все абзацы выравненными по ширине (`justify`).
3. Создай класс для правого выравнивания и применили его к ссылке.

---

## Модуль 6: Полная стилизация сайта

### Задание 6.1: Стилизуем сайт про место (из HTML уровня)

**Что мы делаем:**
Возьмём HTML-сайт, который ты создавал в первом уровне, и стилизуем его.

**Пример кода:**

HTML (`index.html`):

```html
<html>
  <head>
    <title>Москва — столица России</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <h1>Москва — столица России</h1>
    
    <h2>Общая информация</h2>
    <p>Москва — крупнейший город России с населением более 12 миллионов человек. Это центр политики, культуры и бизнеса.</p>
    
    <h2>Достопримечательности</h2>
    <ul>
      <li>Кремль</li>
      <li>Красная площадь</li>
      <li>Мавзолей Ленина</li>
    </ul>
    
    <h2>Что делать в Москве</h2>
    <ol>
      <li>Посетить музеи</li>
      <li>Погулять в парках</li>
      <li>Посмотреть цирк</li>
    </ol>
    
    <p><a href="https://en.wikipedia.org/wiki/Moscow" target="_blank">Подробнее на Википедии</a></p>
  </body>
</html>
```

CSS (`style.css`):

```css
body {
  background-color: #f5f5f5;
  font-family: Arial;
  margin: 0;
  padding: 20px;
  max-width: 800px;
  margin-left: auto;
  margin-right: auto;
}

h1 {
  color: #c60c30;
  background-color: white;
  text-align: center;
  padding: 20px;
  font-size: 48px;
}

h2 {
  color: #003366;
  border-bottom: 2px solid #003366;
  padding-bottom: 10px;
  font-size: 32px;
}

p {
  color: #333333;
  line-height: 1.6;
  font-size: 16px;
}

ul, ol {
  color: #333333;
  font-size: 16px;
}

a {
  color: #c60c30;
  text-decoration: none;
  font-weight: bold;
}

a:hover {
  text-decoration: underline;
}
```

Видишь, как сайт теперь выглядит красиво?

**Что попробовать:**

1. Измени цвета на свои любимые.
2. Добавь картинку Москвы.
3. Добавь класс `.highlight` для особо важного текста и красиво оформи его.
4. Сделай `line-height: 2;` для абзацев. Станет ли текст "воздушнее"?

---

## Проверка: полный стильный сайт

**Задание:** Возьми сайт про твоё любимое место (из HTML уровня) и стилизуй его красиво:

- [ ] Цвета: фон, текст, заголовки — все должны быть в хорошей цветовой схеме
- [ ] Размеры: заголовки h1, h2, h3, текст — разные размеры
- [ ] Расстояния: padding и margin для удобного чтения
- [ ] Классы: минимум 2-3 класса для разных типов текста или блоков
- [ ] Ширина: max-width для центрального контента
- [ ] Выравнивание: текст выравнен красиво
- [ ] Ссылки: цвет, наведение (`:hover`)

Когда закончишь, открой в браузере и убедись, что выглядит красиво!

---

Отлично! Ты научился делать сайты красивыми. На следующем уровне (CSS продвинутый) ты выучишь ещё более классные фишки!
