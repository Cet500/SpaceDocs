# 📄 Уровень 1: HTML — основы создания страницы

## Что такое HTML и почему мы начинаем с неё

HTML — это язык, на котором ты рассказываешь браузеру: "Вот здесь у нас заголовок, вот здесь текст, вот здесь картинка,
вот здесь кнопка". Браузер читает эти инструкции и показывает на экране красиво.

Это как меню ресторана: ты не просто пишешь "борщ, картошка, чай", а пишешь:

- Это блюдо (борщ)
- Это гарнир (картошка)
- Это напиток (чай)

Браузер видит эти ярлычки и знает, как каждое показывать.

HTML — это самая база. Если не будет HTML, нечего будет раскрашивать CSS-ом и оживлять JavaScript-ом. Поэтому начинаем
отсюда.

---

## Модуль 0: Подготовка — откуда вообще берётся сайт

### Задание 0.1: Создаём первый файл и открываем его в браузере

**Что мы делаем:**
Создаём простой текстовый файл, называем его `index.html` и открываем в браузере. Смотрим, что даже простой текст
браузер может показать.

**Новый элемент / новое понятие:**

- **HTML-файл** — это просто текстовый файл с расширением `.html`. Браузер его узнаёт и открывает как веб-страницу.
- **Браузер** — программа, которая читает HTML и показывает красиво (Chrome, Firefox, Safari, Edge).

**Простое объяснение:**
Когда ты пишешь текст в текстовом редакторе (например, Блокноте) и сохраняешь его как `.txt`, это просто текстовый файл.
Если ты сохраняешь его как `.html`, браузер говорит: "О, это веб-страница!" и открывает её не как текст, а как страницу.

**Пример кода:**

```html
Привет, мир!
```

Да, это всё. Сохрани это в файл `index.html`, открой его дважды кликом (или правой кнопкой → "Открыть с" → выбери
браузер), и увидишь "Привет, мир!" на экране.

**Что попробовать:**

1. Напиши несколько строк текста вместо одной. Сохрани. Обнови страницу в браузере (Ctrl+R или F5). Видишь новый текст?
2. Попробуй написать текст с буквами других языков (например, по-английски, по-китайски). Всё ли видно?
3. Сохрани файл с другим именем, например `hello.html`, и открой его. Работает?
4. Напиши текст на две строки. Видишь ли две строки в браузере или одну?

---

### Задание 0.2: Приносим в дом элементы HTML-структуры

**Что мы делаем:**
Один только текст — не совсем HTML. Надо обернуть текст в "теги" — специальные команды, которые рассказывают браузеру,
что здесь. Первый и главный тег — `<html>`.

**Новый элемент / новое понятие:**

- **Теги** — это команды HTML, которые пишутся вот так: `<имя>` и закрываются так: `</имя>`.
- **`<html>`** — главный тег, который говорит браузеру: "Всё что сейчас будет — это HTML".

**Простое объяснение:**
Теги — это как скобки в тексте. Когда ты пишешь *(текст в скобках)*, ты даёшь понять читателю: "Вот эта часть —
особенная, в скобках". Теги HTML делают то же самое, но для браузера.

`<html>` — это открывающий тег (открывающая скобка).
`</html>` — это закрывающий тег (закрывающая скобка).

Всё, что между ними, — это HTML-страница.

**Пример кода:**

```html
<html>
  Привет, мир!
</html>
```

Попробуй открыть это в браузере. Результат выглядит так же, как раньше, но теперь браузер знает, что это HTML. На самом
деле он рад.

**Что попробовать:**

1. Напиши несколько строк текста внутри `<html>` и `</html>`. Всё видно?
2. Напиши текст снаружи от `</html>` (внизу). Видишь ли его?
3. Забудь закрыть `</html>`. Что произойдёт? (Обычно браузер закроет его сам, но это не очень хорошо.)

---

## Модуль 1: Основные теги — голова и тело

### Задание 1.1: Добавляем структуру — head и body

**Что мы делаем:**
Внутри `<html>` есть две главные части: голова (head) и тело (body). Head — это невидимая часть (инструкции для
браузера), body — это видимая часть (то, что ты видишь на экране).

**Новый элемент / новое понятие:**

- **`<head>`** — голова страницы. Сюда пишут команды для браузера, название страницы и так далее. Это не видно на
  странице.
- **`<body>`** — тело страницы. Сюда пишут всё, что должно быть видно: текст, картинки, кнопки.

**Простое объяснение:**
Представь человека. Голова (head) — это то, что у него внутри: мозг, мысли, память. Тело (body) — это то, что видно
снаружи: руки, ноги, одежда, лицо.

Так и с сайтом. Head — невидимые инструкции. Body — всё видимое.

**Пример кода:**

```html
<html>
  <head>
    Здесь будут инструкции для браузера
  </head>
  <body>
    Привет, мир! Это видно.
  </body>
</html>
```

Открой это в браузере. На экране видишь только "Привет, мир! Это видно."? Текст из head не видно (он не предназначен для
глаз).

**Что попробовать:**

1. Напиши что-нибудь в head. Видишь ли это на странице?
2. Напиши что-нибудь в body. Видишь ли это?
3. Перепутай head и body местами. Браузер расстроится?

---

### Задание 1.2: Название страницы — тег title

**Что мы делаем:**
В браузере, в верхней части вкладки, написано название страницы. Мы добавим это название через тег `<title>` в head.

**Новый элемент / новое понятие:**

- **`<title>`** — тег в head, который задаёт название страницы. Его видно в вкладке браузера (той части, где написано,
  какая страница открыта).

**Простое объяснение:**
Когда ты открываешь вкладку в браузере, там где-то в верхней части написано "Гугл" или "Ютуб" или "Вконтакте". Это
title. Браузер берёт его из HTML и пишет его в вкладку. Это как надпись на обложке книги — ты видишь, какая это книга.

**Пример кода:**

```html
<html>
  <head>
    <title>Мой первый сайт</title>
  </head>
  <body>
    Привет, мир!
  </body>
</html>
```

Открой это в браузере. Видишь в вкладке "Мой первый сайт"?

**Что попробовать:**

1. Поменяй текст в title на что-нибудь другое. Обнови страницу (Ctrl+R). Изменилась вкладка?
2. Сделай title очень длинным (20 слов). Что произойдёт с вкладкой?
3. Поставь title в body вместо head. Что произойдёт?

---

## Модуль 2: Текст и заголовки

### Задание 2.1: Заголовки — теги h1, h2, h3

**Что мы делаем:**
Заголовки — это большой текст в начале страницы или секции. HTML имеет специальные теги для заголовков: h1, h2, h3 и так
далее (до h6). h1 — самый важный и большой, h6 — маленький.

**Новый элемент / новое понятие:**

- **`<h1>`, `<h2>`, `<h3>`, ..., `<h6>`** — теги для заголовков разного размера. h1 — главный, h2 — подзаголовок, и так
  далее.

**Простое объяснение:**
Когда ты читаешь книгу, ты видишь большое название в начале (это как h1), потом подзаголовки поменьше (это как h2, h3).
Браузер понимает, что это заголовки, и показывает их большим шрифтом, чтобы было ясно, что это важно.

**Пример кода:**

```html
<html>
  <head>
    <title>Футбол</title>
  </head>
  <body>
    <h1>Как играть в футбол</h1>
    <h2>Что нужно</h2>
    <h3>Мяч</h3>
    <h3>Поле</h3>
    <h2>Правила</h2>
  </body>
</html>
```

Открой это. Видишь, что h1 — самый большой, h2 поменьше, h3 ещё поменьше?

**Что попробовать:**

1. Создай h1, h2, h3, h4, h5, h6. Какой самый маленький?
2. Поменяй h1 на h6. Стал ли он маленьким?
3. Напиши несколько h2 подряд. Как они выглядят?

---

### Задание 2.2: Обычный текст — тег p

**Что мы делаем:**
`<p>` — это тег для абзаца (paragraph). Когда ты пишешь большой текст, ты не пишешь его просто так. Ты оборачиваешь его
в `<p>`, чтобы браузер понимал: "Это целый абзац, давай отделим его от других".

**Новый элемент / новое понятие:**

- **`<p>`** — тег для абзаца текста.

**Простое объяснение:**
Когда ты пишешь в Word, ты нажимаешь Enter, и новая строка отделяется от предыдущей. Это новый абзац. В HTML абзацы
обозначаются тегом `<p>`. Браузер автоматически добавляет пустое место до и после абзаца, чтобы было красиво.

**Пример кода:**

```html
<html>
  <head>
    <title>Про котиков</title>
  </head>
  <body>
    <h1>Котики — лучшие питомцы</h1>
    <p>Котики очень пушистые и мягкие. Они любят спать на диване и ловить мышей.</p>
    <p>Есть много пород котиков. Каждая порода уникальна и красива по-своему.</p>
    <p>Если у тебя есть котик, ты знаешь, как это весело!</p>
  </body>
</html>
```

Открой это. Видишь, что каждый абзац отделён пустой строкой?

**Что попробовать:**

1. Напиши пять абзацев подряд. Как они выглядят?
2. Напиши абзац без тега `<p>` (просто текст). Видишь ли разницу?
3. Сделай первый абзац очень длинным (20 строк текста). Браузер перенесёт текст автоматически?

---

### Задание 2.3: Разделители и новые строки

**Что мы делаем:**
Иногда хочется сделать разделительную линию между частями текста или перейти на новую строку. Для этого есть теги
`<hr>` (горизонтальная линия) и `<br>` (перевод строки).

**Новый элемент / новое понятие:**

- **`<hr>`** — горизонтальная линия. "Разделитель".
- **`<br>`** — перевод строки (break).
- **Теги без закрытия** — `<hr>` и `<br>` не закрываются. Это потому что они не содержат текста, они просто "делают
  что-то".

**Простое объяснение:**
`<hr>` — это как горизонтальная черта в тетради, которая отделяет разные темы.

`<br>` — это как нажать Enter в текстовом редакторе. Следующий текст перейдёт на новую строку, но не будет новым
абзацем (не будет пустого места до и после).

**Пример кода:**

```html
<html>
  <body>
    <h1>Рецепт печенья</h1>
    <p>Ингредиенты:</p>
    <p>Мука<br>Сахар<br>Яйцо<br>Масло</p>
    <hr>
    <p>Инструкция:</p>
    <p>Смешай все ингредиенты. Выпеки при 200 градусах.</p>
  </body>
</html>
```

Видишь, как ингредиенты на разных строках (но это один абзац), а между "Ингредиентами" и "Инструкцией" есть линия?

**Что попробовать:**

1. Напиши несколько строк без `<br>`. Все ли на одной строке?
2. Добавь `<br>` между ними. Что изменилось?
3. Добавь несколько `<hr>` подряд. Как это выглядит?
4. Сделай `<hr>` в середине слова. Работает ли?

---

## Модуль 3: Списки

### Задание 3.1: Неупорядоченный список (список с точками)

**Что мы делаем:**
Когда ты пишешь список, очень удобно показать каждый пункт на отдельной строке с точкой (или ещё чем-то). HTML имеет
теги для списков. Неупорядоченный список (`<ul>`) — это список с точками или другими символами.

**Новый элемент / новое понятие:**

- **`<ul>`** — неупорядоченный список (unordered list).
- **`<li>`** — элемент списка (list item). Каждый пункт списка — это отдельный `<li>`.

**Простое объяснение:**
Когда ты пишешь:

- Яблоки
- Груши
- Апельсины

Это неупорядоченный список (порядок не важен). В HTML это выглядит так:

```html
<ul>
  <li>Яблоки</li>
  <li>Груши</li>
  <li>Апельсины</li>
</ul>
```

Браузер автоматически добавит точки перед каждым пунктом.

**Пример кода:**

```html
<html>
  <body>
    <h1>Список покупок</h1>
    <ul>
      <li>Молоко</li>
      <li>Хлеб</li>
      <li>Масло</li>
      <li>Яйца</li>
    </ul>
  </body>
</html>
```

Открой это. Видишь точки перед каждым пунктом?

**Что попробовать:**

1. Добавь ещё пять пунктов в список.
2. Сделай первый пункт очень длинным (20 слов). Браузер перенесёт текст?
3. Напиши пункт списка без `<li>` (просто текст внутри `<ul>`). Видишь ли его?

---

### Задание 3.2: Упорядоченный список (с номерами)

**Что мы делаем:**
Иногда важен порядок. Например, рецепт: "1. Возьми ингредиенты, 2. Смешай, 3. Выпеки". Для этого есть упорядоченный
список (`<ol>`) — с номерами.

**Новый элемент / новое понятие:**

- **`<ol>`** — упорядоченный список (ordered list).
- **`<li>`** — также используется в упорядоченном списке.

**Простое объяснение:**
Упорядоченный список — это когда важен порядок. HTML автоматически нумерует пункты: 1, 2, 3, 4...

Ты просто пишешь пункты, браузер сам добавит номера.

**Пример кода:**

```html
<html>
  <body>
    <h1>Как сделать бутерброд</h1>
    <ol>
      <li>Взять два ломтика хлеба</li>
      <li>Намазать масло на хлеб</li>
      <li>Положить сыр и колбасу</li>
      <li>Закрыть вторым ломтиком хлеба</li>
    </ol>
  </body>
</html>
```

Открой это. Видишь номера 1, 2, 3, 4?

**Что попробовать:**

1. Поменяй `<ol>` и `<ul>` местами. Как изменилось?
2. Напиши два списка подряд: сначала упорядоченный, потом неупорядоченный.
3. Удали третий пункт. Перенумеруются ли остальные?

---

### Задание 3.3: Вложенные списки (список внутри списка)

**Что мы делаем:**
Иногда один пункт списка содержит под-пункты. Например: "Фрукты: 1. Яблоки 2. Груши". Мы можем вложить один список в
другой.

**Новый элемент / новое понятие:**

- **Вложенные списки** — список внутри `<li>` другого списка.

**Простое объяснение:**
Представь, что у тебя есть коробка. Внутри коробки — три вещи: мяч, машинка, коробка. А внутри этой третьей коробки —
ещё три вещи: конфета, леденец, шоколад. Это вложенность.

**Пример кода:**

```html
<html>
  <body>
    <h1>Животные</h1>
    <ul>
      <li>Млекопитающие
        <ul>
          <li>Кошка</li>
          <li>Собака</li>
          <li>Медведь</li>
        </ul>
      </li>
      <li>Птицы
        <ul>
          <li>Воробей</li>
          <li>Орёл</li>
        </ul>
      </li>
    </ul>
  </body>
</html>
```

Видишь, как внутри каждого большого пункта есть свой маленький список?

**Что попробовать:**

1. Добавь ещё одну категорию (например, "Рыбы") с под-пунктами.
2. Сделай три уровня вложенности (список в списке в списке). Как это выглядит?
3. Сделай первый уровень упорядоченным (`<ol>`), а второй неупорядоченным (`<ul>`). Работает?

---

## Модуль 4: Картинки и ссылки

### Задание 4.1: Вставляем картинку — тег img

**Что мы делаем:**
На сайте часто есть картинки. Мы вставляем их через тег `<img>`. Но нужно рассказать браузеру, где находится картинка.

**Новый элемент / новое понятие:**

- **`<img>`** — тег для картинки (image).
- **Атрибут** — дополнительная информация в теге. Например, `src` — это адрес файла картинки.
- **Атрибут `src`** — путь к файлу картинки (например, `my-cat.jpg`).
- **Атрибут `alt`** — описание картинки (если картинка не загрузится, покажет текст).

**Простое объяснение:**
Теги могут иметь "свойства". Это как когда ты говоришь: "Открой дверь номер 3 в комнате 5". `src` — это "номер дверь",
`alt` — это запасной план, если дверь не откроется.

`<img>` — это тег без закрытия (как `<br>`), потому что он не содержит текста.

**Пример кода:**

```html
<html>
  <body>
    <h1>Мой котик</h1>
    <img src="cat.jpg" alt="Рыжий котик спит">
    <p>Это мой любимый котик. Его зовут Васька.</p>
  </body>
</html>
```

Но подожди! Эта картинка работает только если в одной папке с файлом `index.html` есть файл `cat.jpg`.

Если картинка в подпапке, пишешь:

```html
<img src="images/cat.jpg" alt="Рыжий котик спит">
```

Если картинка в Интернете, пишешь полный адрес:

```html
<img src="https://example.com/cat.jpg" alt="Рыжий котик спит">
```

**Что попробовать:**

1. Возьми любую картинку из Интернета (например, с Google Images). Скопируй адрес и вставь в `src`. Работает?
2. Поменяй текст в `alt`. Что изменится (если картинка не загрузится)?
3. Забудь написать `alt`. Видишь ли картинку?

---

### Задание 4.2: Ссылки — тег a

**Что мы делаем:**
Когда ты кликаешь на синее слово на сайте, ты переходишь на другую страницу. Это ссылка. В HTML ссылки делаются через
тег `<a>` и атрибут `href`.

**Новый элемент / новое понятие:**

- **`<a>`** — тег для ссылки (anchor).
- **Атрибут `href`** — адрес, на который ведёт ссылка (может быть другая страница, или другой сайт, или даже якорь на
  этой же странице).

**Простое объяснение:**
`<a>` — это как жест пальцем: "Нажми на меня, я тебя куда-то отведу". `href` — это адрес, куда ты пойдёшь.

**Пример кода:**

```html
<html>
  <body>
    <h1>Мои любимые сайты</h1>
    <p>
      <a href="https://www.google.com">Гугл</a>
    </p>
    <p>
      <a href="https://www.youtube.com">Ютуб</a>
    </p>
  </body>
</html>
```

Видишь синие ссылки? Нажми на них — перейдёшь на другой сайт.

Ты также можешь сделать ссылку на другую страницу того же сайта:

```html
<a href="page2.html">Перейти на вторую страницу</a>
```

**Что попробовать:**

1. Создай ссылку на свой любимый сайт.
2. Сделай ссылку на несуществующую страницу (например, `page999.html`). Нажми на неё. Что произойдёт?
3. Оберни картинку в ссылку:
   ```html
   <a href="https://google.com">
     <img src="cat.jpg" alt="котик">
   </a>
   ```
   Работает ли?

---

### Задание 4.3: Атрибут target — открыть ссылку в новой вкладке

**Что мы делаем:**
Когда ты кликаешь на ссылку, она обычно открывается в той же вкладке. Но можно сделать так, чтобы открылась новая
вкладка. Для этого нужен атрибут `target`.

**Новый элемент / новое понятие:**

- **Атрибут `target="_blank"`** — когда ты добавишь это, ссылка откроется в новой вкладке.

**Простое объяснение:**
`target="_blank"` означает: "Открой в новой пустой (`blank`) вкладке, не закрывая текущую".

**Пример кода:**

```html
<html>
  <body>
    <h1>Сайты</h1>
    <p>
      <a href="https://www.google.com" target="_blank">Гугл (в новой вкладке)</a>
    </p>
    <p>
      <a href="https://www.youtube.com">Ютуб (в этой вкладке)</a>
    </p>
  </body>
</html>
```

Нажми на первую ссылку — откроется новая вкладка. Нажми на вторую — останешься на текущей странице.

**Что попробовать:**

1. Сделай пять ссылок, четыре с `target="_blank"`, одну без. Какая разница?
2. Открой ссылку в новой вкладке, потом в новом окне. Какая разница?

---

## Модуль 5: Комбинируем всё вместе

### Задание 5.1: Полная страница с заголовком, текстом, списками и ссылками

**Что мы делаем:**
Соберём всё, что выучили, в одну настоящую страницу. Ничего нового, просто комбинация уже известного.

**Пример кода:**

```html
<html>
  <head>
    <title>Про динозавров</title>
  </head>
  <body>
    <h1>Динозавры — самые крутые животные в истории</h1>
    
    <h2>Что такое динозавры?</h2>
    <p>Динозавры — это огромные рептилии, которые жили на Земле миллионы лет назад. Они были величиной с автобус, а некоторые ещё больше!</p>
    
    <h2>Типы динозавров</h2>
    <ul>
      <li>Травоядные (ели растения)
        <ul>
          <li>Трицератопс</li>
          <li>Брахиозавр</li>
        </ul>
      </li>
      <li>Хищные (ели мясо)
        <ul>
          <li>Тираннозавр Рекс</li>
          <li>Велоцираптор</li>
        </ul>
      </li>
    </ul>
    
    <h2>Интересные факты</h2>
    <ol>
      <li>Динозавры жили 200 миллионов лет.</li>
      <li>Они вымерли 66 миллионов лет назад.</li>
      <li>Некоторые динозавры были размером с курицу.</li>
    </ol>
    
    <h2>Хочешь узнать больше?</h2>
    <p>
      <a href="https://en.wikipedia.org/wiki/Dinosaur" target="_blank">Прочитай на Википедии</a>
    </p>
  </body>
</html>
```

Открой эту страницу. Видишь, как всё работает вместе?

**Что попробовать:**

1. Добавь новый раздел с h2, несколько абзацев, список.
2. Добавь картинку динозавра (найди в Интернете).
3. Измени `<title>`.
4. Поменяй какие-нибудь заголовки с h1 на h2 или h3.

---

## Проверка: тест всех знаний

**Задание:** Создай страницу про свой любимый город или место. На странице должно быть:

- [ ] Правильная структура (`<html>`, `<head>`, `<body>`, `<title>`)
- [ ] Заголовок (h1)
- [ ] Несколько абзацев текста
- [ ] Неупорядоченный список (что там есть)
- [ ] Упорядоченный список (что там можно делать)
- [ ] Картинка или две
- [ ] Ссылка на Википедию про это место
- [ ] Красивый title

Когда закончишь, открой страницу в браузере и проверь, что всё работает и выглядит красиво!

---

Отлично! Ты разобрался с HTML. Это база. Теперь ты можешь создавать структуру сайта. На следующем уровне (CSS) ты
научишься делать это красиво.
