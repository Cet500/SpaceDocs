# 🎮 Уровень 5: JavaScript основы — типы данных и консоль

## Что нас ждёт на этом уровне

На предыдущем уровне мы использовали переменные почти вслепую. Теперь разберёмся подробнее: какие типы данных
существуют, как браузер их понимает, и как работает консоль. Это база, на которой строится всё остальное.

---

## Модуль 1: Типы данных

### Задание 1.1: Строки (текст)

**Что мы делаем:**
Строка — это текст. Всё, что заключено в кавычки (одинарные, двойные или обратные).

**Новый элемент / новое понятие:**

- **Строка (string)** — текст, заключённый в кавычки.
- **`typeof`** — оператор, который показывает тип данных.
- **Кавычки:** одинарные `'`, двойные `"`, обратные `` ` ``.

**Простое объяснение:**

```javascript
let name = "Иван";          // строка
let city = 'Москва';        // строка (одинарные кавычки)
let greeting = `Привет!`;   // строка (обратные кавычки)
```

Браузер понимает, что это текст, потому что текст в кавычках. Без кавычек браузер подумает, что это переменная или
число.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testStrings()">Попробуй строки</button>

    <script>
      function testStrings() {
        let text1 = "Привет";
        let text2 = 'Мир';
        let text3 = `Шаблонная строка`;

        console.log("Текст 1: " + text1);
        console.log("Текст 2: " + text2);
        console.log("Текст 3: " + text3);

        // Проверяем тип
        console.log(typeof text1);  // "string"
        console.log(typeof text2);  // "string"
        console.log(typeof text3);  // "string"
      }
    </script>
  </body>
</html>
```

Открой консоль (F12) и нажми на кнопку. Увидишь типы данных.

**Что попробовать:**

1. Создай строку с одинарными кавычками, с двойными, с обратными.
2. Объедини две строки: `let result = "Привет" + ", " + "мир";`.
3. Выведи длину строки: `console.log(text1.length);`.

---

### Задание 1.2: Числа (number)

**Что мы делаем:**
Число — это просто цифры без кавычек. 42, 3.14, -5 — всё это числа.

**Новый элемент / новое понятие:**

- **Число (number)** — цифра без кавычек.
- **Целое число** — 5, 10, 100.
- **Дробное число** — 3.14, 2.5.
- **Отрицательное число** — -10, -3.5.

**Простое объяснение:**

```javascript
let age = 25;          // число
let price = 99.99;     // число (дробное)
let temperature = -5;  // число (отрицательное)
```

С числами можно делать математические операции.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testNumbers()">Попробуй числа</button>

    <script>
      function testNumbers() {
        let num1 = 10;
        let num2 = 3.5;
        let num3 = -7;

        console.log("Число 1: " + num1);
        console.log("Число 2: " + num2);
        console.log("Число 3: " + num3);

        // Проверяем типы
        console.log(typeof num1);  // "number"
        console.log(typeof num2);  // "number"
        console.log(typeof num3);  // "number"

        // Операции
        let sum = num1 + num2;
        console.log("Сумма: " + sum);

        // Важно: "10" + 5 это не 15!
        let text = "10";
        let result = text + 5;
        console.log(result);  // "105" (строка + число = строка)
      }
    </script>
  </body>
</html>
```

Открой консоль и посмотри результаты.

**Что попробовать:**

1. Создай переменные с целыми и дробными числами.
2. Посчитай разные операции: +, -, *, /, %.
3. Посмотри, что произойдёт, если сложить число и строку: `5 + "5"`.

---

### Задание 1.3: Булевы значения (true / false)

**Что мы делаем:**
Булево значение — это ответ на вопрос "да или нет?". Только два варианта: `true` (правда) или `false` (ложь).

**Новый элемент / новое понятие:**

- **Булево значение (boolean)** — true или false.
- **`true`** — правда, да.
- **`false`** — ложь, нет.

**Простое объяснение:**

```javascript
let isRaining = true;      // идёт дождь? да
let isSunny = false;       // светит солнце? нет
let isAdult = true;        // ты взрослый? да
```

Булевы значения используются в условиях (если это правда, делай то, если ложь — делай это).

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testBoolean()">Булевы значения</button>

    <script>
      function testBoolean() {
        let isRaining = true;
        let isCold = false;
        let isHappy = true;

        console.log("Идёт дождь? " + isRaining);     // true
        console.log("Холодно? " + isCold);           // false
        console.log("Ты счастлив? " + isHappy);      // true

        // Проверяем типы
        console.log(typeof isRaining);   // "boolean"
        console.log(typeof isCold);      // "boolean"

        // Сравнение (тоже булево значение)
        let age = 20;
        console.log(age > 18);           // true (20 больше 18)
        console.log(age < 18);           // false (20 не меньше 18)
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай несколько булевых переменных.
2. Сравни числа и посмотри булевы результаты: `5 > 3`, `10 === 10`, `"hello" === "world"`.

---

### Задание 1.4: Null и undefined

**Что мы делаем:**
Есть два специальных значения, которые означают "ничего нет".

**Новый элемент / новое понятие:**

- **`null`** — "ничего нет" (ты специально написал).
- **`undefined`** — "значение не определено" (браузер не знает, что там).

**Простое объяснение:**

- `null` — ты пошел в магазин за молоком, но его нет. Ты специально понимаешь, что его нет.
- `undefined` — ты попросил молоко, но забыл его название. Ты не знаешь, что ищешь.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testNullUndefined()">Null и Undefined</button>

    <script>
      function testNullUndefined() {
        let nothing = null;
        let unknown;  // переменная создана, но значения нет

        console.log("nothing: " + nothing);       // null
        console.log("unknown: " + unknown);       // undefined

        console.log(typeof nothing);     // "object" (это ошибка в JS)
        console.log(typeof unknown);     // "undefined"
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай переменную, не давая ей значения.
2. Посмотри, чему она равна (undefined).
3. Потом присвой ей значение.

---

## Модуль 2: Преобразование типов

### Задание 2.1: Преобразование в строку

**Что мы делаем:**
Иногда нужно превратить число в строку, булево значение в строку и так далее.

**Новый элемент / новое понятие:**

- **`String()`** — преобразовать в строку.
- **`.toString()`** — метод, который преобразует в строку.

**Простое объяснение:**

```javascript
let num = 42;
let str = String(num);   // "42" (строка, а не число)
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testStringConversion()">Преобразование в строку</button>

    <script>
      function testStringConversion() {
        let num = 42;
        let bool = true;
        let nothing = null;

        // Способ 1: String()
        console.log(String(num));         // "42"
        console.log(String(bool));        // "true"
        console.log(String(nothing));     // "null"

        // Способ 2: toString()
        console.log((123).toString());    // "123"

        // Проверяем тип
        let converted = String(num);
        console.log(typeof converted);    // "string"
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Преобразуй разные типы в строку.
2. Объедини преобразованное число и обычную строку.

---

### Задание 2.2: Преобразование в число

**Что мы делаем:**
Превращаем строку в число.

**Новый элемент / новое понятие:**

- **`Number()`** — преобразовать в число.
- **`parseInt()`** — преобразовать в целое число.
- **`parseFloat()`** — преобразовать в дробное число.

**Простое объяснение:**

```javascript
let str = "42";
let num = Number(str);   // 42 (число, а не строка)
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testNumberConversion()">Преобразование в число</button>

    <script>
      function testNumberConversion() {
        let str1 = "42";
        let str2 = "3.14";
        let str3 = "100 яблок";

        // Способ 1: Number()
        console.log(Number(str1));        // 42
        console.log(Number(str2));        // 3.14
        console.log(Number(str3));        // NaN (не число)

        // Способ 2: parseInt()
        console.log(parseInt(str1));      // 42
        console.log(parseInt(str2));      // 3 (целое число)
        console.log(parseInt(str3));      // 100 (берёт первое число)

        // Способ 3: parseFloat()
        console.log(parseFloat(str2));    // 3.14

        // Проверяем тип
        let converted = Number(str1);
        console.log(typeof converted);    // "number"
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Преобразуй строки с числами в настоящие числа.
2. Посчитай: `Number("10") + Number("5")`.
3. Посмотри, что будет с `Number("hello")`.

---

### Задание 2.3: Преобразование в булево значение

**Что мы делаем:**
Превращаем любое значение в true или false.

**Новый элемент / новое понятие:**

- **`Boolean()`** — преобразовать в булево значение.
- **Falsy значения** — значения, которые становятся false: 0, "", null, undefined, NaN.
- **Truthy значения** — остальные значения становятся true.

**Простое объяснение:**
Только несколько значений считаются "ложью": пустая строка, ноль, null, undefined, NaN. Всё остальное — "правда".

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testBooleanConversion()">Преобразование в boolean</button>

    <script>
      function testBooleanConversion() {
        // Truthy (становятся true)
        console.log(Boolean(1));           // true
        console.log(Boolean("hello"));     // true
        console.log(Boolean([1, 2, 3]));   // true

        // Falsy (становятся false)
        console.log(Boolean(0));           // false
        console.log(Boolean(""));          // false
        console.log(Boolean(null));        // false
        console.log(Boolean(undefined));   // false
        console.log(Boolean(NaN));         // false
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Проверь разные значения через Boolean().
2. Запомни, какие значения falsy, какие truthy.

---

## Модуль 3: Работа с консолью

### Задание 3.1: Различные способы логирования

**Что мы делаем:**
Консоль — это не только `console.log()`. Есть ещё `console.warn()`, `console.error()` и другие.

**Новый элемент / новое понятие:**

- **`console.log()`** — обычное сообщение.
- **`console.warn()`** — предупреждение (жёлтое).
- **`console.error()`** — ошибка (красное).
- **`console.info()`** — информация.

**Простое объяснение:**
Разные цвета в консоли помогают быстро найти нужную информацию.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testConsole()">Посмотри консоль</button>

    <script>
      function testConsole() {
        console.log("Это обычное сообщение");
        console.warn("Это предупреждение (жёлтое)");
        console.error("Это ошибка (красное)");
        console.info("Это информация");

        // Можно вывести несколько значений
        let name = "Иван";
        let age = 25;
        console.log("Имя:", name, "Возраст:", age);
      }
    </script>
  </body>
</html>
```

Открой консоль (F12) и смотри разные цвета.

**Что попробовать:**

1. Используй `console.warn()` и `console.error()`.
2. Выведи несколько значений в одной строке.

---

### Задание 3.2: Дебаг с помощью консоли

**Что мы делаем:**
Когда в коде ошибка или непонятное поведение, консоль помогает найти проблему. Выведи значения переменных в разных
местах кода.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="debugCode()">Дебаг кода</button>

    <script>
      function debugCode() {
        let a = 10;
        console.log("a после создания:", a);  // 10

        a = a + 5;
        console.log("a после добавления 5:", a);  // 15

        a = a * 2;
        console.log("a после умножения на 2:", a);  // 30

        let result = a / 2;
        console.log("result:", result);  // 15

        console.log("Финальное значение a:", a);  // 30
      }
    </script>
  </body>
</html>
```

Посмотри в консоли, как значение `a` меняется на каждом шаге.

**Что попробовать:**

1. Добавь console.log() в разные места своего кода.
2. Отследи, как меняются переменные.
3. Найди ошибку, выводя значения в консоль.

---

## Модуль 4: Практика

### Задание 4.1: Преобразование данных

**Что мы делаем:**
Создаём программу, которая берёт строки, преобразует их в числа и считает результат.

**Пример кода:**

HTML:

```html
<html>
  <head>
    <title>Преобразование данных</title>
  </head>
  <body>
    <h1>Калькулятор со строками</h1>
    <button onclick="calculate()">Посчитай</button>

    <script>
      function calculate() {
        // Строки (как если бы пришли с формы)
        let input1 = "25";
        let input2 = "7";

        console.log("Входные данные (строки):");
        console.log("input1:", input1, "тип:", typeof input1);
        console.log("input2:", input2, "тип:", typeof input2);

        // Преобразуем в числа
        let num1 = Number(input1);
        let num2 = Number(input2);

        console.log("\nПосле преобразования:");
        console.log("num1:", num1, "тип:", typeof num1);
        console.log("num2:", num2, "тип:", typeof num2);

        // Считаем
        let sum = num1 + num2;
        let product = num1 * num2;
        let division = num1 / num2;

        console.log("\nРезультаты:");
        console.log("Сумма:", sum);
        console.log("Произведение:", product);
        console.log("Деление:", division);

        // Преобразуем обратно в строку для вывода
        alert(`${num1} + ${num2} = ${sum}`);
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Измени значения input1 и input2.
2. Добавь новые операции.
3. Выведи всё в консоль подробно.

---

### Задание 4.2: Проверка типов

**Что мы делаем:**
Создаём программу, которая проверяет типы переменных и выводит информацию.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="checkTypes()">Проверить типы</button>

    <script>
      function checkTypes() {
        let data = [
          42,
          "hello",
          true,
          3.14,
          null,
          undefined
        ];

        console.log("Проверка типов:");
        for (let i = 0; i < data.length; i++) {
          let value = data[i];
          let type = typeof value;
          console.log(`Значение: ${value}, Тип: ${type}`);
        }
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Добавь в массив новые значения.
2. Выведи результаты в консоль.

---

## Итоговая проверка

**Задание:** Создай программу, которая демонстрирует все типы данных:

- [ ] Строка (string)
- [ ] Число (number)
- [ ] Булево значение (boolean)
- [ ] Null и undefined
- [ ] Преобразование между типами (минимум 3 примера)
- [ ] Вывод в консоль с разными методами (log, warn, error)
- [ ] Использование typeof

Когда закончишь, открой консоль (F12) и посмотри все сообщения!

---

Отлично! Ты разобрался с типами данных. Это база всех баз. Теперь переходи к логике и условиям!
