# 🎮 Уровень 6: JavaScript базовый — логика, условия и циклы

## Что нас ждёт на этом уровне

До сих пор наш код просто выполнял команды по порядку: сделай то, потом то, потом то. Но настоящие программы должны
принимать решения: "Если температура ниже нуля, включи отопление. Если ты выбрал пиццу, вычисли стоимость". Это
называется логика.

На этом уровне ты выучишь:

- Сравнение значений (больше, меньше, равно)
- Условия (if, else, else if)
- Циклы (for, while)

Это мощный инструмент. С этим ты сможешь делать такие вещи, которые кажутся магией.

---

## Модуль 1: Операторы сравнения

### Задание 1.1: Сравнение чисел

**Что мы делаем:**
Сравниваем числа и получаем ответ: правда это или ложь.

**Новый элемент / новое понятие:**

- **`>`** — больше.
- **`<`** — меньше.
- **`>=`** — больше или равно.
- **`<=`** — меньше или равно.
- **`===`** — равно (строгое сравнение).
- **`!==`** — не равно.
- **Результат** — всегда true или false.

**Простое объяснение:**

```javascript
5 > 3        // true (5 больше 3)
5 < 3        // false (5 не меньше 3)
5 === 5      // true (5 равно 5)
5 !== 3      // true (5 не равно 3)
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testComparison()">Сравни числа</button>

    <script>
      function testComparison() {
        let a = 10;
        let b = 5;

        console.log(a + " > " + b + ": " + (a > b));      // true
        console.log(a + " < " + b + ": " + (a < b));      // false
        console.log(a + " >= " + b + ": " + (a >= b));    // true
        console.log(a + " <= " + b + ": " + (a <= b));    // false
        console.log(a + " === " + b + ": " + (a === b));  // false
        console.log(a + " !== " + b + ": " + (a !== b));  // true

        // Сравнение со строками (осторожно!)
        console.log("'5' === 5: " + ('5' === 5));         // false (разные типы)
        console.log("'5' == 5: " + ('5' == 5));           // true (игнорирует тип)
      }
    </script>
  </body>
</html>
```

Открой консоль и посмотри результаты.

**Что попробовать:**

1. Сравни разные числа.
2. Посмотри разницу между `==` и `===`.
3. Сравни число и строку.

---

### Задание 1.2: Логические операторы (и, или, не)

**Что мы делаем:**
Иногда одного сравнения недостаточно. Нужны логические операторы: И (оба должны быть правда), ИЛИ (хотя бы один правда),
НЕ (обратное).

**Новый элемент / новое понятие:**

- **`&&`** (И) — оба условия должны быть true.
- **`||`** (ИЛИ) — хотя бы одно условие должно быть true.
- **`!`** (НЕ) — противоположное значение.

**Простое объяснение:**

```javascript
let age = 20;
let hasLicense = true;

age >= 18 && hasLicense   // true (оба условия верны)
age < 18 || hasLicense    // true (хотя бы одно верно)
!hasLicense               // false (противоположное)
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="testLogic()">Логические операторы</button>

    <script>
      function testLogic() {
        let age = 25;
        let hasJob = true;
        let hasMoney = true;

        // Оператор И (&&)
        console.log("Есть работа И деньги: " + (hasJob && hasMoney));        // true
        console.log("Молод И есть работа: " + (age < 30 && hasJob));          // true
        console.log("Молод И есть деньги: " + (age < 20 && hasMoney));        // false

        // Оператор ИЛИ (||)
        console.log("Есть работа ИЛИ деньги: " + (hasJob || hasMoney));       // true
        let hasHouse = false;
        console.log("Есть дом ИЛИ деньги: " + (hasHouse || hasMoney));        // true

        // Оператор НЕ (!)
        console.log("Нет дома: " + (!hasHouse));                              // true
        console.log("Нет денег: " + (!hasMoney));                             // false
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Создай несколько логических выражений.
2. Комбинируй &&, ||, !.
3. Посмотри, как меняется результат при разных значениях.

---

## Модуль 2: Условия (if, else, else if)

### Задание 2.1: Простой if

**Что мы делаем:**
Если условие правда, выполнить блок кода. Если ложь — пропустить.

**Новый элемент / новое понятие:**

- **`if`** — если условие правда, выполни код.
- **Блок кода** — код в фигурных скобках `{ }`.

**Простое объяснение:**

```javascript
if (age >= 18) {
  alert("Ты совершеннолетний!");
}
```

"Если возраст больше или равен 18, покажи сообщение."

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="checkAge()">Проверь возраст</button>

    <script>
      function checkAge() {
        let age = 20;

        if (age >= 18) {
          console.log("Ты взрослый!");
        }

        if (age < 13) {
          console.log("Ты ребёнок!");
        }

        // С числом
        if (age === 20) {
          console.log("Тебе ровно 20 лет!");
        }

        // С логикой
        if (age >= 18 && age < 65) {
          console.log("Ты в рабочем возрасте");
        }
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Измени значение age.
2. Создай несколько if'ов с разными условиями.
3. Используй логические операторы в условиях.

---

### Задание 2.2: If-else

**Что мы делаем:**
Если условие правда — делай то. Если ложь — делай это.

**Новый элемент / новое понятие:**

- **`if`** — если условие правда.
- **`else`** — иначе (если условие ложь).

**Простое объяснение:**

```javascript
if (temperature > 20) {
  alert("Тепло!");
} else {
  alert("Холодно!");
}
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="checkWeather()">Какая погода?</button>

    <script>
      function checkWeather() {
        let temperature = 15;

        if (temperature > 20) {
          console.log("Тепло, одень лёгкую одежду");
        } else {
          console.log("Холодно, одень куртку");
        }

        // Другой пример
        let hasUmbrella = false;

        if (hasUmbrella) {
          console.log("Хорошо, ты готов к дождю");
        } else {
          console.log("Возьми зонт!");
        }
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Измени переменные и смотри разные результаты.
2. Создай свою программу с if-else.

---

### Задание 2.3: If-else if-else

**Что мы делаем:**
Несколько условий. Первое неправда? Проверим второе. Второе неправда? Проверим третье. И так далее.

**Новый элемент / новое понятие:**

- **`else if`** — иначе если (проверь ещё одно условие).
- Можно иметь много `else if`.

**Простое объяснение:**

```javascript
if (age < 13) {
  alert("Ты ребёнок");
} else if (age < 18) {
  alert("Ты подросток");
} else if (age < 65) {
  alert("Ты взрослый");
} else {
  alert("Ты пенсионер");
}
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="rateScore()">Оценка</button>

    <script>
      function rateScore() {
        let score = 75;

        if (score >= 90) {
          console.log("Отлично! (A)");
        } else if (score >= 80) {
          console.log("Хорошо! (B)");
        } else if (score >= 70) {
          console.log("Нормально (C)");
        } else if (score >= 60) {
          console.log("Удовлетворительно (D)");
        } else {
          console.log("Неудовлетворительно (F)");
        }

        // Категория продукта
        let price = 150;

        if (price < 50) {
          console.log("Дешёвый товар");
        } else if (price < 200) {
          console.log("Средний товар");
        } else if (price < 500) {
          console.log("Дорогой товар");
        } else {
          console.log("Очень дорогой товар");
        }
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Измени значение score, посмотри разные оценки.
2. Создай свою систему if-else if-else.
3. Используй логические операторы в условиях.

---

## Модуль 3: Циклы

### Задание 3.1: Цикл for

**Что мы делаем:**
Когда нужно сделать одно и то же много раз, используют цикл. For-цикл исполняет блок кода определённое количество раз.

**Новый элемент / новое понятие:**

- **Цикл** — повторение кода несколько раз.
- **`for`** — цикл с известным количеством повторений.
- **Переменная-счётчик** (обычно `i`) — отслеживает, сколько раз повторили.

**Простое объяснение:**

```javascript
for (let i = 0; i < 5; i++) {
  console.log("Привет " + i);
}
```

"Создай переменную i с нулём. Пока i меньше 5, выполни код. После каждого повтора увеличь i на 1."

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="loopFor()">Цикл for</button>

    <script>
      function loopFor() {
        // Простой цикл
        console.log("Считаем от 0 до 4:");
        for (let i = 0; i < 5; i++) {
          console.log("i = " + i);
        }

        // Цикл с числами от 1 до 10
        console.log("\nСчитаем от 1 до 10:");
        for (let i = 1; i <= 10; i++) {
          console.log(i);
        }

        // Цикл в обратном направлении
        console.log("\nСчитаем от 10 вниз:");
        for (let i = 10; i > 0; i--) {
          console.log(i);
        }

        // Цикл с шагом 2
        console.log("\nЧётные числа от 0 до 10:");
        for (let i = 0; i <= 10; i += 2) {
          console.log(i);
        }
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Сделай цикл от 1 до 20.
2. Сделай цикл, который выводит только нечётные числа.
3. Посчитай сумму чисел от 1 до 100.

---

### Задание 3.2: Цикл while

**Что мы делаем:**
While-цикл исполняет блок кода, пока условие правда.

**Новый элемент / новое понятие:**

- **`while`** — цикл с условием.
- **Условие** — проверяется перед каждым повтором.

**Простое объяснение:**

```javascript
let i = 0;
while (i < 5) {
  console.log(i);
  i++;
}
```

"Пока i меньше 5, выполняй код. И не забудь увеличить i, иначе цикл будет вечным!"

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="loopWhile()">Цикл while</button>

    <script>
      function loopWhile() {
        // Простой while
        console.log("While от 0 до 4:");
        let i = 0;
        while (i < 5) {
          console.log(i);
          i++;  // ВАЖНО! Увеличиваем i, иначе бесконечный цикл
        }

        // Другой пример: угадай число
        console.log("\nУгадай число (1-10):");
        let tries = 0;
        let secretNumber = 7;
        let guess = 7;  // (в реальности это будет от пользователя)

        while (guess !== secretNumber) {
          console.log("Неправильно, попробуй снова");
          tries++;
          guess = secretNumber;  // Для примера
        }

        console.log("Правильно! Попыток: " + tries);
      }
    </script>
  </body>
</html>
```

**ВНИМАНИЕ:** Если забудешь увеличивать `i` (или менять условие), цикл будет повторяться вечно и браузер зависнет!

**Что попробовать:**

1. Напиши while, который считает от 10 до 1.
2. Напиши while, который считает сумму.
3. БУДЬ ОСТОРОЖНА с бесконечными циклами!

---

### Задание 3.3: Цикл do-while

**Что мы делаем:**
Do-while исполняет код хотя бы один раз, потом проверяет условие.

**Новый элемент / новое понятие:**

- **`do-while`** — цикл, который выполняется минимум один раз.

**Простое объяснение:**

```javascript
let i = 10;
do {
  console.log(i);  // Выполнится хотя бы один раз, даже если i не < 5
  i++;
} while (i < 5);
```

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="loopDoWhile()">Цикл do-while</button>

    <script>
      function loopDoWhile() {
        // Do-while выполняется минимум один раз
        console.log("Do-while:");
        let i = 0;
        do {
          console.log(i);
          i++;
        } while (i < 5);

        // Даже если условие ложь с самого начала
        console.log("\nDo-while с ложным условием:");
        let x = 10;
        do {
          console.log("Это выполнится, даже если x не < 5");
        } while (x < 5);
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Используй do-while для пользовательского ввода (симуляция).
2. Сравни for, while и do-while.

---

## Модуль 4: Применение в реальности

### Задание 4.1: Таблица умножения

**Что мы делаем:**
Создаём таблицу умножения, используя вложенные циклы.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="multiplicationTable()">Таблица умножения</button>
    <pre id="output"></pre>

    <script>
      function multiplicationTable() {
        let table = "";

        for (let i = 1; i <= 9; i++) {
          for (let j = 1; j <= 9; j++) {
            table += i + " × " + j + " = " + (i * j) + "  ";
          }
          table += "\n";
        }

        document.getElementById("output").innerHTML = table;
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Сделай таблицу от 1 до 12.
2. Выведи только чётные произведения.

---

### Задание 4.2: Проверка пароля

**Что мы делаем:**
Программа просит пароль, пока не введёшь правильный.

**Пример кода:**

HTML:

```html
<html>
  <body>
    <button onclick="passwordCheck()">Проверить пароль</button>

    <script>
      function passwordCheck() {
        let correctPassword = "12345";
        let attempts = 0;
        let maxAttempts = 3;

        // Симуляция (в реальности это формы)
        let enteredPassword = "12345";

        if (enteredPassword === correctPassword) {
          alert("Пароль правильный!");
        } else {
          alert("Пароль неправильный. Попытки закончились.");
        }

        // Другой пример: проверка на крепость пароля
        let password = "MyP@ssw0rd";
        let strength = 0;

        if (password.length >= 8) strength++;
        if (/[A-Z]/.test(password)) strength++;  // Заглавные буквы
        if (/[a-z]/.test(password)) strength++;  // Строчные буквы
        if (/[0-9]/.test(password)) strength++;  // Цифры
        if (/[^a-zA-Z0-9]/.test(password)) strength++;  // Специальные символы

        if (strength <= 2) {
          console.log("Пароль слабый");
        } else if (strength <= 4) {
          console.log("Пароль нормальный");
        } else {
          console.log("Пароль крепкий");
        }
      }
    </script>
  </body>
</html>
```

**Что попробовать:**

1. Измени пароль.
2. Добавь больше проверок.

---

## Итоговая проверка

**Задание:** Создай программу, которая демонстрирует всё, чему ты выучился:

- [ ] Операторы сравнения (>, <, ===, !==)
- [ ] Логические операторы (&&, ||, !)
- [ ] Условия (if, else, else if)
- [ ] Минимум один тип цикла (for или while)
- [ ] Вложенные циклы или циклы с условиями внутри
- [ ] Консоль для отладки

Примеры: угадай число, проверка возраста, система оценок, таблица, фильтр элементов.

---

Отлично! Ты овладел основной логикой программирования. Теперь переходи к функциям — это делает код красивым и мощным!
