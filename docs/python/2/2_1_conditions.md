# 2.1 Условия: Принимаем решения

## Введение

До сих пор наши программы выполняли **одинаковый код** каждый раз. Но в реальной жизни нам нужно **принимать решения**:

- Если пользователь ввёл пароль правильно → пусти его
- Если возраст больше 18 → покажи контент для взрослых
- Если баланс достаточный → позволи снять деньги
- Иначе → покажи ошибку

Для этого нужны **условия** (if/else/elif) — они позволяют выполнять разный код в зависимости от ситуации.

В этой главе ты научишься:

1. Писать условия (if/else/elif)
2. Комбинировать условия (and, or, not)
3. Вложенные условия
4. Применять условия в реальном коде

**Результат:** Ты сможешь писать программы, которые **принимают решения**.

---

## Живой пример: Условия в действии

Запусти этот код:

```python
# Проверяем возраст
age = 18

if age >= 18:
    print("✓ Добро пожаловать на сайт для взрослых!")
else:
    print("✗ Извини, ты слишком молод.")

# Проверяем оценку
score = 75

if score >= 90:
    print("Оценка: A (отлично)")
elif score >= 80:
    print("Оценка: B (хорошо)")
elif score >= 70:
    print("Оценка: C (удовлетворительно)")
else:
    print("Оценка: F (неудовлетворительно)")
```

**Результат:**

```
✓ Добро пожаловать на сайт для взрослых!
Оценка: C (удовлетворительно)
```

**Видишь?** Программа **выбирает разный код** в зависимости от значений. Это условия! 🎯

---

## Теория: Три типа условий

### 1️⃣ `if` — если условие верно

**Синтаксис:**

```python
if условие:
    код, если условие True
```

**Как это работает:**

```python
age = 25

if age >= 18:
    print("Взрослый")
    # Этот код выполнится, потому что age >= 18 это True
```

**Важно:** Сравни с 1.5 (операторы сравнения):

- `==` — равно
- `!=` — не равно
- `>` — больше
- `<` — меньше
- `>=` — больше или равно
- `<=` — меньше или равно

---

### 2️⃣ `else` — иначе (альтернатива)

Выполняется, если условие `if` было **False**:

```python
age = 15

if age >= 18:
    print("Взрослый")
else:
    print("Несовершеннолетний")
    # Этот код выполнится, потому что age < 18
```

---

### 3️⃣ `elif` — иначе если (ещё одно условие)

Проверяет **дополнительное условие**, если первое было False:

```python
score = 75

if score >= 90:
    print("A (отлично)")
elif score >= 80:
    print("B (хорошо)")
elif score >= 70:
    print("C (удовлетворительно)")
else:
    print("F (неудовлетворительно)")
```

**Как это работает:**

1. Проверяет: 75 >= 90? **Нет** → идёт к elif
2. Проверяет: 75 >= 80? **Нет** → идёт к elif
3. Проверяет: 75 >= 70? **Да!** → выводит "C (удовлетворительно)" и **выходит**

**Важно:** Код проходит условия **сверху вниз**, останавливается на первом True!

---

## Логические операторы (and, or, not)

Часто нужно проверить **несколько условий одновременно**:

### `and` — ВСЕ условия должны быть верны

```python
age = 25
has_license = True

if age >= 18 and has_license:
    print("✓ Можешь вести машину")
else:
    print("✗ Не можешь вести машину")
```

**Таблица истинности:**

```
True  and True   → True
True  and False  → False
False and True   → False
False and False  → False
```

---

### `or` — ХОТЯ БЫ ОДНО условие должно быть верно

```python
day = "суббота"
is_holiday = False

if day in ["суббота", "воскресенье"] or is_holiday:
    print("✓ Выходной!")
else:
    print("✗ Рабочий день")
```

**Таблица истинности:**

```
True  or True   → True
True  or False  → True
False or True   → True
False or False  → False
```

---

### `not` — инверсия (противоположное)

```python
is_blocked = False

if not is_blocked:
    print("✓ Аккаунт активен")
else:
    print("✗ Аккаунт заблокирован")

# Эквивалентно:
if is_blocked == False:
    print("✓ Аккаунт активен")
```

---

## Практика: Пишем условия

### Практика: Проверка четности

```python
number = 7

if number % 2 == 0:
    print(f"{number} — чётное число")
else:
    print(f"{number} — нечётное число")
```

**Результат:**

```
7 — нечётное число
```

---

### Практика: Система оценок

```python
# Система оценок
score = 85

if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
elif score >= 70:
    grade = "C"
elif score >= 60:
    grade = "D"
else:
    grade = "F"

print(f"Оценка: {grade}")
```

**Результат:**

```
Оценка: B
```

---

### Практика: Проверка логина

```python
correct_password = "secret123"
user_input = "secret123"

if user_input == correct_password:
    print("✓ Пароль верный!")
else:
    print("✗ Пароль неверный!")
```

---

## Вложенные условия

Условия можно вкладывать друг в друга:

```python
age = 25
has_license = True

if age >= 18:
    if has_license:
        print("✓ Можешь вести машину")
    else:
        print("✗ Нужны права")
else:
    print("✗ Слишком молод")
```

**Но это неудобно!** Лучше использовать `and`:

```python
if age >= 18 and has_license:
    print("✓ Можешь вести машину")
elif age >= 18:
    print("✗ Нужны права")
else:
    print("✗ Слишком молод")
```

---

## Вопросы для самопроверки

1. **Что выведет этот код?**
   ```python
   x = 10
   if x > 5:
       print("Больше")
   else:
       print("Меньше")
   ```

2. **Какой результат даст условие: `5 > 3 and 2 > 4`?**

3. **Что выведет этот код?**
   ```python
   score = 75
   if score >= 90:
       print("A")
   elif score >= 80:
       print("B")
   elif score >= 70:
       print("C")
   ```

4. **Какая разница между `and` и `or`?**

5. **Как перевести `not is_active` на логику?**

---

## Практическое задание

### Задание: Диагностика здоровья

**Что нужно:**

Создай программу для диагностики здоровья на основе ИМТ (индекс массы тела):

```python
# Формула ИМТ: вес (кг) / (рост (м) ^ 2)

height = 1.75      # Рост в метрах
weight = 70        # Вес в килограммах

# Вычисляем ИМТ
bmi = weight / (height ** 2)

# Диагностируем
print("=== ДИАГНОСТИКА ЗДОРОВЬЯ ===")
print(f"Рост: {height} м")
print(f"Вес: {weight} кг")
print(f"ИМТ: {bmi:.1f}")
print()

# Определяем категорию
if bmi < 18.5:
    category = "Недостаточный вес"
    status = "⚠️ Нужна консультация врача"
elif bmi < 25:
    category = "Нормальный вес"
    status = "✓ Здоровое состояние"
elif bmi < 30:
    category = "Избыточный вес"
    status = "⚠️ Рекомендуется физическая активность"
else:
    category = "Ожирение"
    status = "⚠️ Необходима консультация врача"

print(f"Категория: {category}")
print(f"Статус: {status}")

# Рекомендации
print("\n=== РЕКОМЕНДАЦИИ ===")
if bmi < 25:
    print("✓ Продолжай здоровый образ жизни")
elif bmi < 30:
    print("• Увеличь физическую активность")
    print("• Сбалансируй питание")
else:
    print("• Срочно обратись к врачу")
    print("• Начни программу похудения")
```

**Результат должен быть примерно:**

```
=== ДИАГНОСТИКА ЗДОРОВЬЯ ===
Рост: 1.75 м
Вес: 70 кг
ИМТ: 22.9

Категория: Нормальный вес
Статус: ✓ Здоровое состояние

=== РЕКОМЕНДАЦИИ ===
✓ Продолжай здоровый образ жизни
```

**Требования:**

- Используй формулу ИМТ
- Используй if/elif/else для категорий
- Используй условия для рекомендаций
- Выведи понятный результат

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач проверяет условия!**

```python
# Данные задачи
task = {
    "title": "Купить продукты",
    "completed": False,
    "priority": 1
}

# Проверяем условия
if task["completed"]:
    print(f"✓ {task['title']}")
else:
    print(f"✗ {task['title']}")

# Приоритет
if task["priority"] == 1:
    print("⚠️ ВЫСОКИЙ ПРИОРИТЕТ")
elif task["priority"] == 2:
    print("! Средний приоритет")
else:
    print("○ Низкий приоритет")

# Фильтрация
completed_count = 0
for task in tasks:
    if task["completed"] and task["priority"] >= 1:
        completed_count += 1
```

На этом этапе:

- 📝 Ты учишься принимать решения
- 🔜 Потом научишься повторять код (циклы)
- 🔜 Потом организовывать код (функции)

---

## Резюме: Три главных вывода

✅ **Условия позволяют выполнять разный код.** Используй if/else/elif для выбора.

✅ **Сравнивай правильно.** Используй ==, !=, >, <, >=, <= из главы 1.5.

✅ **Комбинируй условия логикой.** Используй and/or/not для сложных проверок.

---

## Что дальше?

→ **Глава 2.2** (Циклы): Как повторять код (for/while)

→ **Глава 2.3** (Функции): Как организовывать код

→ **Глава 2.4** (Работа с файлами): Как сохранять данные

---

## 💡 Важный совет: Порядок elif

**Правильный порядок:**

```python
score = 85

if score >= 90:
    print("A")
elif score >= 80:      # Проверяем от большего к меньшему
    print("B")
elif score >= 70:
    print("C")
else:
    print("F")
```

**Неправильный порядок:**

```python
score = 85

if score >= 70:
    print("C")         # Выведет C, хотя должно быть B!
elif score >= 80:
    print("B")
elif score >= 90:
    print("A")
```

**Правило:** Проверяй **от более специфичных к более общим условиям**.

---

**Поздравляем!** Ты освоил условия — фундамент всех интерактивных программ! 🎉

**Модуль 2 продолжается →** Следующий — циклы (повтор кода). 🚀✨