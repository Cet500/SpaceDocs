# 1.4 Словари: Данные с названиями

## Введение

В списках мы обращаемся к элементам по индексу:

```python
fruits = ["яблоко", "банан", "апельсин"]
print(fruits[0])  # яблоко
```

Но что если нам нужны **более понятные названия** для данных?

Например, информация о человеке:

```python
# Со списком непонятно
person = [25, "Миша", "Москва"]
age = person[0]         # Какой индекс был возраста? 0?
name = person[1]        # А имя на каком индексе?
```

Для этого существуют **словари** — контейнеры с **именованными ключами** вместо индексов.

В этой главе ты научишься:

1. Создавать словари
2. Получать значения по ключу (названию)
3. Добавлять и изменять пары ключ-значение
4. Проходить по словарю

**Результат:** Ты сможешь хранить структурированные данные (как в базе данных).

---

## Живой пример: Словарь в действии

Запусти этот код:

```python
# Создаём словарь о человеке
person = {
    "name": "Миша",
    "age": 25,
    "city": "Москва"
}

# Выводим информацию
print("Имя:", person["name"])
print("Возраст:", person["age"])
print("Город:", person["city"])

# Меняем значение
person["age"] = 26
print("Новый возраст:", person["age"])

# Добавляем новую информацию
person["email"] = "misha@example.com"
print("Email:", person["email"])
```

**Результат:**

```
Имя: Миша
Возраст: 25
Город: Москва
Новый возраст: 26
Email: misha@example.com
```

**Видишь?** Вместо индексов 0, 1, 2 мы используем понятные названия: "name", "age", "city". 🎯

---

## Теория: Что такое словарь?

### Аналогия: Телефонная книга

Словарь — это как **телефонная книга**:

- Ключ (key) = Имя человека ("Миша")
- Значение (value) = Номер телефона ("+7-900-123-45-67")

```
┌─────────────────────────────────┐
│ Словарь (Телефонная книга)      │
├─────────────┬──────────────────┤
│ Ключ        │ Значение         │
├─────────────┼──────────────────┤
│ "Миша"      │ "+7-900-123-45"  │
│ "Маша"      │ "+7-900-234-56"  │
│ "Иван"      │ "+7-900-345-67"  │
└─────────────┴──────────────────┘
```

### Синтаксис создания словаря

```python
имя_словаря = {
    "ключ1": значение1,
    "ключ2": значение2,
    "ключ3": значение3
}
```

**Примеры:**

```python
# Информация о человеке
person = {
    "name": "Миша",
    "age": 25,
    "city": "Москва"
}

# Конфигурация приложения
config = {
    "theme": "dark",
    "language": "ru",
    "font_size": 14
}

# Пустой словарь (добавим элементы потом)
empty_dict = {}
```

---

## Работа со словарями

### 1. Получение значения

Используй **ключ в квадратных скобках**:

```python
person = {
    "name": "Миша",
    "age": 25,
    "city": "Москва"
}

# Получаем значения
print(person["name"])   # Миша
print(person["age"])    # 25
print(person["city"])   # Москва
```

**Что происходит:**

```
person["name"]  →  Python ищет ключ "name"  →  Возвращает "Миша"
```

---

### 2. Изменение значения

```python
person = {
    "name": "Миша",
    "age": 25
}

# Меняем значение
person["age"] = 26
print(person)   # {'name': 'Миша', 'age': 26}

# Меняем ещё одно
person["name"] = "Иван"
print(person)   # {'name': 'Иван', 'age': 26}
```

---

### 3. Добавление новой пары ключ-значение

```python
person = {
    "name": "Миша",
    "age": 25
}

# Добавляем новые ключи
person["email"] = "misha@example.com"
person["city"] = "Москва"

print(person)
# {'name': 'Миша', 'age': 25, 'email': 'misha@example.com', 'city': 'Москва'}
```

---

### 4. Проверка наличия ключа

```python
person = {
    "name": "Миша",
    "age": 25
}

# Проверяем, есть ли ключ
if "name" in person:
    print("Ключ 'name' есть!")        # Выведется

if "email" in person:
    print("Ключ 'email' есть!")       # НЕ выведется
```

---

### 5. Удаление пары ключ-значение

**Метод `.pop()` — удаляет и возвращает значение:**

```python
person = {
    "name": "Миша",
    "age": 25,
    "city": "Москва"
}

# Удаляем и сохраняем значение
removed_city = person.pop("city")
print(removed_city)   # Москва
print(person)         # {'name': 'Миша', 'age': 25}
```

---

### 6. Получение всех ключей

```python
person = {
    "name": "Миша",
    "age": 25,
    "city": "Москва"
}

# Список всех ключей
keys = person.keys()
print(keys)          # dict_keys(['name', 'age', 'city'])

# Или просто в цикле
for key in person.keys():
    print(key)
```

**Результат:**

```
name
age
city
```

---

### 7. Получение всех значений

```python
person = {
    "name": "Миша",
    "age": 25,
    "city": "Москва"
}

# Список всех значений
values = person.values()
print(values)        # dict_values(['Миша', 25, 'Москва'])

# Или просто в цикле
for value in person.values():
    print(value)
```

**Результат:**

```
Миша
25
Москва
```

---

### 8. Получение пар ключ-значение

**Самый красивый способ — используй `.items()`:**

```python
person = {
    "name": "Миша",
    "age": 25,
    "city": "Москва"
}

# Проходим по всем парам
for key, value in person.items():
    print(f"{key}: {value}")
```

**Результат:**

```
name: Миша
age: 25
city: Москва
```

---

### 9. Метод `.get()` — безопасное получение

Если ключа нет, `.get()` вернёт `None` вместо ошибки:

```python
person = {
    "name": "Миша",
    "age": 25
}

# Способ 1: С индексом (опасно)
# print(person["email"])    # ❌ KeyError: 'email'

# Способ 2: С .get() (безопасно)
email = person.get("email")          # None
print(email)                         # None

# Способ 3: С значением по умолчанию
email = person.get("email", "unknown@example.com")
print(email)                         # unknown@example.com
```

---

## Практика: Работай со словарями

### Практика: Книга контактов

**Код:**

```python
# Создаём словарь контактов
contacts = {
    "Миша": "+7-900-111-11-11",
    "Маша": "+7-900-222-22-22",
    "Иван": "+7-900-333-33-33"
}

# Выводим контакты
print("=== МОИ КОНТАКТЫ ===")
for name, phone in contacts.items():
    print(f"{name}: {phone}")

# Добавляем новый контакт
contacts["Глаша"] = "+7-900-444-44-44"
print("\nПосле добавления Глаши:")
print(contacts)

# Ищем контакт
search_name = "Миша"
if search_name in contacts:
    print(f"\nТелефон {search_name}: {contacts[search_name]}")

# Удаляем контакт
removed_phone = contacts.pop("Иван")
print(f"\nУдален контакт: Иван ({removed_phone})")
print(contacts)
```

**Результат:**

```
=== МОИ КОНТАКТЫ ===
Миша: +7-900-111-11-11
Маша: +7-900-222-22-22
Иван: +7-900-333-33-33

После добавления Глаши:
{'Миша': '+7-900-111-11-11', 'Маша': '+7-900-222-22-22', 'Иван': '+7-900-333-33-33', 'Глаша': '+7-900-444-44-44'}

Телефон Миша: +7-900-111-11-11

Удален контакт: Иван (+7-900-333-33-33)
{'Миша': '+7-900-111-11-11', 'Маша': '+7-900-222-22-22', 'Глаша': '+7-900-444-44-44'}
```

---

## Вложенные словари (Nested Dictionaries)

Словари могут содержать другие словари:

```python
# Информация о нескольких людях
people = {
    "person1": {
        "name": "Миша",
        "age": 25,
        "city": "Москва"
    },
    "person2": {
        "name": "Маша",
        "age": 23,
        "city": "СПб"
    }
}

# Получаем информацию
print(people["person1"]["name"])   # Миша
print(people["person2"]["age"])    # 23

# Проходим по всем людям
for person_id, person_data in people.items():
    print(f"{person_id}: {person_data['name']}, {person_data['age']} лет")
```

**Результат:**

```
person1: Миша, 25 лет
person2: Маша, 23 лет
```

---

## Вопросы для самопроверки

1. **Что выведет этот код?**
   ```python
   person = {"name": "Alex", "age": 30}
   print(person["name"])
   ```

2. **Как добавить новый ключ в словарь?**

3. **Какая разница между `person["age"]` и `person.get("age")`?**

4. **Как проверить, есть ли ключ "email" в словаре?**

5. **Как получить все ключи словаря?**

---

## Практическое задание

### Задание: Каталог книг

**Что нужно:**

Создай программу для управления каталогом книг:

```python
# Создай словарь книг (каждая книга - это словарь)
books = {
    "book1": {
        "title": "1984",
        "author": "Джордж Оруэлл",
        "year": 1949,
        "pages": 328
    },
    "book2": {
        "title": "О дивный новый мир",
        "author": "Олдос Хаксли",
        "year": 1932,
        "pages": 311
    },
    "book3": {
        "title": "451° по Фаренгейту",
        "author": "Рэй Брэдбери",
        "year": 1953,
        "pages": 227
    }
}

# Выведи все книги
print("=== КАТАЛОГ КНИГ ===")
for book_id, book_info in books.items():
    print(f"\nКнига #{book_id}")
    print(f"  Название: {book_info['title']}")
    print(f"  Автор: {book_info['author']}")
    print(f"  Год: {book_info['year']}")
    print(f"  Страниц: {book_info['pages']}")

# Добавь новую книгу
books["book4"] = {
    "title": "Мастер и Маргарита",
    "author": "Михаил Булгаков",
    "year": 1967,
    "pages": 573
}

# Обнови информацию о книге
books["book1"]["year"] = 1948  # Исправляем год

# Выведи обновленный каталог
print("\n\n=== ОБНОВЛЕННЫЙ КАТАЛОГ ===")
for book_id, book_info in books.items():
    print(f"{book_info['title']} ({book_info['year']}) - {book_info['author']}")
```

**Результат должен быть примерно:**

```
=== КАТАЛОГ КНИГ ===

Книга #book1
  Название: 1984
  Автор: Джордж Оруэлл
  Год: 1949
  Страниц: 328

... (остальные книги)

=== ОБНОВЛЕННЫЙ КАТАЛОГ ===
1984 (1948) - Джордж Оруэлл
О дивный новый мир (1932) - Олдос Хаксли
451° по Фаренгейту (1953) - Рэй Брэдбери
Мастер и Маргарита (1967) - Михаил Булгаков
```

**Требования:**

- Используй вложенные словари
- Используй `.items()` для цикла
- Добавь новую книгу
- Обнови информацию о книге
- Используй понятные имена ключей

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач использует словари!**

```python
# Каждая задача - это словарь
task = {
    "id": 1,
    "title": "Купить продукты",
    "completed": False,
    "priority": 1
}

# Список всех задач
tasks = {
    "task1": {"title": "Задача 1", "completed": False},
    "task2": {"title": "Задача 2", "completed": True},
}

# Выводим задачи
for task_id, task_info in tasks.items():
    status = "✓" if task_info["completed"] else "✗"
    print(f"{status} {task_info['title']}")
```

На этом этапе:

- 📝 Ты учишься хранить структурированные данные
- 🔜 Потом научишься сохранять их в файл (JSON, CSV)
- 🔜 Потом работать с базами данных
- 🔜 Потом создавать веб-API

---

## Резюме: Три главных вывода

✅ **Словарь использует ключи вместо индексов.** Создай с `{"ключ": значение}`.

✅ **Ключи должны быть уникальными и понятными.** Используй "name", а не "n".

✅ **Используй методы для работы.**

- `dict["key"]` — получить значение
- `dict.keys()` — все ключи
- `dict.values()` — все значения
- `dict.items()` — все пары
- `dict.get("key")` — безопасное получение

---

## Что дальше?

→ **Глава 1.5** (Условия): Как принимать решения (if/else/elif)

→ **Глава 1.6** (Циклы): Как повторять код (for/while)

→ **Глава 2.1** (Функции): Как организовывать код

---

## 💡 Важный совет: Ключи в словарях

**Хорошие ключи:**

```python
person = {
    "name": "Миша",
    "age": 25,
    "email": "misha@example.com",
    "is_student": True
}
```

**Плохие ключи:**

```python
person = {
    "n": "Миша",
    "a": 25,
    "e": "misha@example.com",
    "s": True
}
```

**Правила:**

- Используй понятные названия
- Используй snake_case (name_first, not nameFirst)
- Избегай сокращений (name, не n)
- Выбирай между единственным и множественным (phone, не phones, если один номер)

**Почему?** Код, который легко читать — это хороший код. 📖

---

**Готов к следующему уровню?** Словари — это основа работы с данными в Python и веб-разработке. Практикуйся! 🚀✨