# 1.5 Операторы: Вычисления и действия

## Введение

Hasta сих пор мы делали очень простые вещи: сохраняли данные и выводили их. Но что если нам нужно **считать**, *
*сравнивать** или **решать**?

Например:

- Посчитать сумму двух чисел: 5 + 3
- Проверить, больше ли число A, чем число B
- Повторить действие 10 раз

Для этого нужны **операторы** — это "команды" для выполнения действий.

В этой главе ты научишься:

1. Считать с помощью математических операторов
2. Правильно использовать скобки
3. Сокращённо записывать операции (+=, -=)
4. Применять операторы в реальном коде

**Результат:** Ты сможешь писать программы, которые **считают и вычисляют**.

---

## Живой пример: Операторы в действии

Запусти этот код:

```python
# Базовая математика
a = 10
b = 3

print(f"Сумма: {a + b}")           # 13
print(f"Разница: {a - b}")         # 7
print(f"Произведение: {a * b}")    # 30
print(f"Деление: {a / b}")         # 3.333...
print(f"Целое деление: {a // b}")  # 3
print(f"Остаток: {a % b}")         # 1

# Сокращённая запись
count = 0
count += 5   # count = count + 5
count += 3   # count = count + 3
print(f"Счётчик: {count}")         # 8
```

**Результат:**

```
Сумма: 13
Разница: 7
Произведение: 30
Деление: 3.3333333333333335
Целое деление: 3
Остаток: 1
Счётчик: 8
```

**Видишь?** Мы считаем, делим, находим остаток. Это операторы! 🎯

---

## Теория: Что такое оператор?

### Определение

**Оператор** — это символ или слово, которое выполняет **действие** над одним или несколькими значениями.

**Примеры:**

- `+` — оператор сложения
- `-` — оператор вычитания
- `*` — оператор умножения
- `=` — оператор присваивания

---

## Математические операторы

### 1️⃣ Сложение (`+`)

```python
# Числа
result = 5 + 3          # 8
result = 10 + 20 + 30   # 60

# Строки (конкатенация)
text = "Hello" + " " + "World"  # "Hello World"

# Списки
list1 = [1, 2] + [3, 4]  # [1, 2, 3, 4]
```

**Важно:** Нельзя складывать разные типы:

```python
# ❌ Ошибка!
result = "5" + 5      # TypeError: can only concatenate str (not "int") to str
```

---

### 2️⃣ Вычитание (`-`)

```python
# Числа
result = 10 - 3       # 7
result = 100 - 25 - 10  # 65

# Отрицательные числа
result = 5 - 10       # -5
```

---

### 3️⃣ Умножение (`*`)

```python
# Числа
result = 5 * 3        # 15
result = 2 * 3 * 4    # 24

# Строка × число (повтор)
text = "Ha" * 3       # "HaHaHa"

# Список × число (повтор)
list1 = [1, 2] * 3    # [1, 2, 1, 2, 1, 2]
```

---

### 4️⃣ Деление (`/`)

```python
# Обычное деление (всегда возвращает float)
result = 10 / 2       # 5.0
result = 10 / 3       # 3.333...
result = 7 / 2        # 3.5

# ❌ Деление на ноль — ошибка!
# result = 10 / 0     # ZeroDivisionError
```

**Важно:** Деление всегда возвращает `float`, даже если результат целый:

```python
result = 10 / 2       # 5.0, не 5
```

---

### 5️⃣ Целочисленное деление (`//`)

Возвращает только целую часть (без остатка):

```python
result = 10 // 3      # 3 (не 3.333...)
result = 10 // 2      # 5
result = 7 // 2       # 3
result = -7 // 2      # -4 (округляет в сторону минус бесконечности)
```

---

### 6️⃣ Остаток от деления (`%`)

Возвращает остаток после деления:

```python
result = 10 % 3       # 1 (10 = 3*3 + 1)
result = 10 % 2       # 0 (делится нацело)
result = 7 % 3        # 1
result = 25 % 5       # 0

# Полезное применение: проверить чётность
number = 10
if number % 2 == 0:
    print("Число чётное")
else:
    print("Число нечётное")
```

---

### 7️⃣ Возведение в степень (`**`)

```python
result = 2 ** 3       # 8 (2 в степени 3)
result = 5 ** 2       # 25
result = 10 ** 0      # 1 (любое число в степени 0 = 1)
result = 2 ** -1      # 0.5 (отрицательная степень)
```

---

## Порядок операций (BODMAS/PEMDAS)

Python следует стандартному математическому порядку:

1. **Скобки** `()`
2. **Возведение в степень** `**`
3. **Умножение, деление, целое деление, остаток** `*`, `/`, `//`, `%`
4. **Сложение, вычитание** `+`, `-`

**Примеры:**

```python
# Без скобок: сначала * и /, потом + и -
result = 2 + 3 * 4    # 2 + 12 = 14, не 20

# Со скобками: сначала в скобках
result = (2 + 3) * 4  # 5 * 4 = 20

# Смешанные операции
result = 10 + 5 * 2 - 3 / 3 ** 2
#      = 10 + 10 - 3 / 9
#      = 10 + 10 - 0.333...
#      = 19.666...
```

---

## Сокращённые операторы

Вместо `x = x + 5` можно писать `x += 5`:

```python
x = 10

# Полная запись
x = x + 5       # 15

# Сокращённая запись (то же самое)
x += 5          # 20
```

**Все сокращённые операторы:**

```python
x = 10

x += 5    # x = x + 5    → 15
x -= 3    # x = x - 3    → 12
x *= 2    # x = x * 2    → 24
x /= 4    # x = x / 4    → 6.0
x //= 2   # x = x // 2   → 3.0
x %= 2    # x = x % 2    → 1.0
x **= 2   # x = x ** 2   → 1.0
```

---

## Практика: Вычисляй с Python

### Практика: Калькулятор площади

**Код:**

```python
# Вычисляем площадь прямоугольника
width = 5
height = 3
area = width * height

print(f"Ширина: {width}")
print(f"Высота: {height}")
print(f"Площадь: {area}")

# Вычисляем площадь круга
import math
radius = 4
circle_area = math.pi * radius ** 2

print(f"\nРадиус: {radius}")
print(f"Площадь круга: {circle_area:.2f}")  # .2f - две цифры после точки
```

**Результат:**

```
Ширина: 5
Высота: 3
Площадь: 15

Радиус: 4
Площадь круга: 50.27
```

---

### Практика: Сок из очков

```python
# Программа для подсчёта всех очков
game1_score = 45
game2_score = 38
game3_score = 52

total_score = game1_score + game2_score + game3_score
average_score = total_score / 3

print("=== РЕЗУЛЬТАТЫ ===")
print(f"Игра 1: {game1_score}")
print(f"Игра 2: {game2_score}")
print(f"Игра 3: {game3_score}")
print(f"Всего: {total_score}")
print(f"Средняя оценка: {average_score:.1f}")
```

**Результат:**

```
=== РЕЗУЛЬТАТЫ ===
Игра 1: 45
Игра 2: 38
Игра 3: 52
Всего: 135
Средняя оценка: 45.0
```

---

## Вопросы для самопроверки

1. **Что выведет этот код?**
   ```python
   x = 10
   x += 5
   x *= 2
   print(x)
   ```

2. **Какой результат у выражения: `2 + 3 * 4`?**
   (14 или 20?)

3. **Что вернёт `10 % 3`?**

4. **Какая разница между `/` и `//`?**

5. **Напиши сокращённую форму: `x = x - 5`**

---

## Практическое задание

### Задание: Калькулятор баланса

**Что нужно:**

Создай программу для управления балансом счёта:

```python
# Начальный баланс
balance = 1000

print("=== БАНКОВСКИЙ СЧЁТ ===")
print(f"Начальный баланс: {balance} руб.")

# Операция 1: пополнение
balance += 500
print(f"После пополнения на 500: {balance} руб.")

# Операция 2: снятие
balance -= 200
print(f"После снятия 200: {balance} руб.")

# Операция 3: начисление процентов (5% годовых)
percent_rate = 0.05
interest = balance * percent_rate
balance += interest
print(f"После начисления {percent_rate * 100}% процентов: {balance:.2f} руб.")

# Операция 4: комиссия
commission = 10
balance -= commission
print(f"После комиссии {commission} руб.: {balance:.2f} руб.")

# Итоговый отчёт
print(f"\n=== ИТОГОВЫЙ БАЛАНС ===")
print(f"Ваш баланс: {balance:.2f} руб.")
```

**Результат должен быть:**

```
=== БАНКОВСКИЙ СЧЁТ ===
Начальный баланс: 1000 руб.
После пополнения на 500: 1500 руб.
После снятия 200: 1300 руб.
После начисления 5.0% процентов: 1365.0 руб.
После комиссии 10 руб.: 1355.0 руб.

=== ИТОГОВЫЙ БАЛАНС ===
Ваш баланс: 1355.00 руб.
```

**Требования:**

- Используй математические операторы
- Используй сокращённую запись (+=, -=, *=)
- Вычисли проценты
- Выведи результат с двумя цифрами после точки (используй `.2f`)

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач может подсчитывать статистику!**

```python
tasks = [
    {"title": "Задача 1", "completed": True},
    {"title": "Задача 2", "completed": False},
    {"title": "Задача 3", "completed": True},
]

# Подсчитываем выполненные задачи
completed_count = 0
for task in tasks:
    if task["completed"]:
        completed_count += 1

total_count = len(tasks)
percent_done = (completed_count / total_count) * 100

print(f"Выполнено: {completed_count}/{total_count} ({percent_done:.1f}%)")
```

На этом этапе:

- 📝 Ты учишься считать и вычислять
- 🔜 Потом научишься проверять условия (if/else)
- 🔜 Потом повторять действия (циклы)
- 🔜 Потом организовывать код (функции)

---

## Резюме: Три главных вывода

✅ **Операторы — это инструменты для действий.** Используй +, -, *, / для вычислений.

✅ **Порядок операций важен.** Python следует математическому порядку: сначала `**`, потом `*` и `/`, потом `+` и `-`.
Используй скобки, если не уверен.

✅ **Сокращённые операторы экономят код.** Используй `x += 5` вместо `x = x + 5`.

---

## Что дальше?

→ **Глава 1.6** (Логические операторы): Как сравнивать значения (==, >, <) и проверять условия

→ **Глава 1.7** (Условия): Как принимать решения (if/else/elif)

→ **Глава 1.8** (Циклы): Как повторять код (for/while)

---

## 💡 Важный совет: Типизация при вычислениях

**Помни об этой таблице:**

| Операция      | Типы      | Результат   |
|---------------|-----------|-------------|
| int + int     | 5 + 3     | 8 (int)     |
| float + float | 5.0 + 3.0 | 8.0 (float) |
| int + float   | 5 + 3.0   | 8.0 (float) |
| str + str     | "5" + "3" | "53" (str)  |
| int + str     | 5 + "3"   | ❌ Ошибка    |
| int * int     | 5 * 3     | 15 (int)    |
| str * int     | "x" * 3   | "xxx" (str) |

**Правило:** Если смешиваешь int и float → результат float. Если смешиваешь str и число → ошибка!

---

**Готов к условиям и циклам?** Операторы — основа всех вычислений. Практикуйся! 🚀✨