# 1.2 Переменные: Как сохранять информацию

## Введение

Во все предыдущие примеры мы писали код, который сразу выполнялся и забывался. Но что если нам нужно **сохранить
информацию** и использовать её позже?

Например:

- Запросить у пользователя его имя
- Сохранить это имя
- Потом вывести: "Привет, [его имя]!"

Для этого нужны **переменные** — "коробки", в которых мы можем хранить информацию.

В этой главе ты научишься:

1. Создавать переменные
2. Сохранять разные типы данных (числа, текст)
3. Использовать переменные в программе

**Результат:** К концу главы ты напишешь программу, которая запросит у пользователя информацию и сохранит её.

---

## Живой пример: Переменная в действии

Запусти этот код:

```python
# Сохраняем информацию в переменные
name = "Миша"
age = 25

# Используем переменные
print(f"Меня зовут {name}")
print(f"Мне {age} лет")
```

**Результат:**

```
Меня зовут Миша
Мне 25 лет
```

**Видишь?** Мы сохранили информацию и потом её использовали. Это переменные! 🎯

---

## Теория: Что такое переменная?

### Аналогия: Коробка на полке

Переменная — это **коробка**, в которой ты можешь что-то хранить.

```
┌─────────────────────┐
│   name = "Миша"     │
│  [Коробка "name"]   │  ← Это переменная
│  Содержит: "Миша"   │  ← Это значение
└─────────────────────┘
```

**Что можно делать с коробкой:**

- 📦 **Создать коробку** (объявить переменную)
- 📝 **Положить туда что-то** (присвоить значение)
- 🔍 **Посмотреть, что внутри** (использовать переменную)
- ✏️ **Заменить содержимое** (изменить значение)

### Синтаксис создания переменной

```python
имя_переменной = значение
```

**Разбор:**

- `имя_переменной` — как ты назовёшь коробку
- `=` — "положи туда"
- `значение` — что положишь

**Примеры:**

```python
x = 5           # Создаём переменную x, кладём туда число 5
name = "Alex"   # Создаём переменную name, кладём туда текст
is_student = True  # Создаём переменную is_student, кладём True
```

---

## Основные типы данных

Python понимает 4 основных типа данных для новичков:

### 1️⃣ Целые числа (int)

**Что это?**
Целые числа без дробной части: 5, -10, 0, 1000.

**Когда использовать?**

- Возраст человека
- Количество предметов
- Баланс денег (в рублях)

**Примеры:**

```python
age = 25           # Возраст
count = 10         # Количество
balance = 1000     # Баланс
negative = -5      # Отрицательное число
zero = 0           # Ноль
```

**Узнать тип:**

```python
x = 42
print(type(x))     # Выведет: <class 'int'>
```

---

### 2️⃣ Дробные числа (float)

**Что это?**
Числа с точкой (дробная часть): 3.14, 2.5, -0.5.

**Когда использовать?**

- Рост человека (в метрах)
- Средняя оценка
- Цена товара
- Математические вычисления

**Примеры:**

```python
height = 1.75      # Рост в метрах
price = 99.99      # Цена в рублях
average_grade = 4.5  # Средняя оценка
pi = 3.14159       # Число Пи
```

**Узнать тип:**

```python
x = 3.14
print(type(x))     # Выведет: <class 'float'>
```

---

### 3️⃣ Текст (str — строка)

**Что это?**
Текст, заключённый в кавычки. Может быть одна буква или целый роман.

**Когда использовать?**

- Имя человека
- Название книги
- Адрес
- Описание чего-то

**Примеры:**

```python
name = "Миша"
city = "Москва"
message = "Привет, мир!"
# Одна буква тоже строка
letter = "A"
```

**Важно:** Кавычки могут быть одинарные или двойные:

```python
str1 = "Hello"     # Двойные кавычки
str2 = 'Hello'     # Одинарные кавычки
# Оба варианта одинаковые
```

**Узнать тип:**

```python
x = "Hello"
print(type(x))     # Выведет: <class 'str'>
```

**Внимание:** Цифры в кавычках — это текст, не число!

```python
x = "42"     # Это строка, не число
y = 42       # Это число
print(type(x))   # <class 'str'>
print(type(y))   # <class 'int'>
```

---

### 4️⃣ Истина / Ложь (bool)

**Что это?**
Переменная, которая может быть либо `True` (правда), либо `False` (ложь).

**Когда использовать?**

- "Ученик ли пользователь?" — `True` или `False`
- "Задача выполнена?" — `True` или `False`
- "Пользователь зарегистрирован?" — `True` или `False`

**Примеры:**

```python
is_student = True           # Это студент? Да
is_completed = False        # Задача выполнена? Нет
has_license = True          # Есть права? Да
```

**Узнать тип:**

```python
x = True
print(type(x))     # Выведет: <class 'bool'>
```

---

## Практика: Создай свои переменные

### Практика: Анкета

**Задача:** Создай переменные с информацией о себе и выведи их.

**Код:**

```python
# Информация о себе
name = "Твоё имя"
age = 25
height = 1.75
is_programmer = True

# Выводим информацию
print("Имя:", name)
print("Возраст:", age)
print("Рост:", height)
print("Программист?", is_programmer)
```

**Что попробовать:**

1. Скопируй этот код
2. Поменяй значения на свои данные
3. Запусти и посмотри результат
4. Добавь ещё одну переменную (например, `city = "Москва"`)

**Результат должен быть:**

```
Имя: Твоё имя
Возраст: 25
Рост: 1.75
Программист? True
```

---

## Расширение: Type Hints (для чистоты кода)

**Что это?**
Type hints — это способ сказать Python, какой тип должна иметь переменная. Это необязательно, но полезно.

**Зачем?**

- Другие люди (и ты через неделю) поймут, какой тип данных ожидается
- IDE подсказывает ошибки в типах

**Синтаксис:**

```python
имя: тип = значение
```

**Примеры:**

```python
name: str = "Миша"
age: int = 25
height: float = 1.75
is_student: bool = True
```

**IDE будет помогать:**

- Если ты напишешь `name = 25`, IDE подсвечивает как ошибку (ты же сказал, что это должна быть строка!)

**Но помни:** Type hints — это просто подсказка для тебя и IDE. Python не будет ругаться, если ты присвоишь переменной
другой тип.

---

## Изменение переменной

Переменная — это не "коробка на вечность". Ты можешь менять её содержимое:

```python
x = 5
print(x)     # Выведет: 5

x = 10       # Меняем значение
print(x)     # Выведет: 10

x = 20       # Меняем ещё раз
print(x)     # Выведет: 20
```

**Переменная обновляется, предыдущее значение забывается.**

---

## Вопросы для самопроверки

1. **Что выведет этот код?**
   ```python
   x = 10
   name = "Bob"
   print(x)
   print(name)
   ```

2. **Какой тип данных будет у переменной?**
   ```python
   x = "42"
   ```
   (Строка или число?)

3. **Правда или ложь: Ты можешь изменить значение переменной после создания?**

4. **Какой из этих вариантов неправильный?**
   ```python
   a = 5           # OK
   b = "5"         # OK
   c = 5.0         # OK
   d = "5.0"       # OK?
   ```
   (Все они правильные! 😉)

---

## Практическое задание

### Задание: Информация о товаре

**Что нужно:**

Напиши программу, которая хранит информацию о товаре и выводит её:

```python
# Создай переменные о товаре
product_name = "Ноутбук"
product_price = 50000.0
quantity = 5
is_available = True

# Выводи информацию красиво
print("=== ТОВАР ===")
print("Название:", product_name)
print("Цена:", product_price, "рублей")
print("Количество на складе:", quantity)
print("В наличии?", is_available)
print("================")
```

**Результат должен быть:**

```
=== ТОВАР ===
Название: Ноутбук
Цена: 50000.0 рублей
Количество на складе: 5
В наличии? True
================
```

**Требования:**

- Как минимум 4 переменные разных типов
- Красивый вывод (с дополнительными символами, как в примере)
- Используй `print()` для вывода

**Бонус:** Добавь операцию с переменными:

```python
total_cost = product_price * quantity
print("Сумма всех товаров:", total_cost)
```

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач** часто работает с переменными!

```python
# В менеджере задач будет так:
task_title = "Купить продукты"
is_completed = False
priority = 1

print(f"Задача: {task_title}")
print(f"Выполнено: {is_completed}")
print(f"Приоритет: {priority}")
```

На сегодня:

- 📝 Ты учишься сохранять информацию в переменные
- 🔜 Потом научишься брать информацию от пользователя
- 🔜 Потом будешь хранить список задач
- 🔜 Потом будешь сохранять их в файл

---

## Резюме: Три главных вывода

✅ **Переменная — это коробка для информации.** Ты кладёшь туда значение, потом используешь его сколько угодно раз.

✅ **Основные типы:**

- `int` — целые числа (5, 100, -10)
- `float` — дробные числа (3.14, 2.5)
- `str` — текст ("Hello", "42")
- `bool` — правда/ложь (True, False)

✅ **Переменные можно менять.** Переменная не "раз и на вечность", она может обновляться.

---

## Что дальше?

→ **Глава 1.3** (Операторы): Как считать, сравнивать и комбинировать переменные

→ **Глава 1.4** (Ввод данных): Как брать информацию от пользователя

→ **Глава 1.5** (Строки): Как работать с текстом посложнее

---

## 💡 Важный совет: Именование переменных

**Хорошие названия:**

```python
age = 25
user_name = "Миша"
is_student = True
total_cost = 1000.50
```

**Плохие названия:**

```python
a = 25
x = "Миша"
is_s = True
tc = 1000.50
```

**Правила:**

- Используй понятные названия (не `a`, а `age`)
- Используй английский язык (не `возраст`, а `age`)
- Используй snake_case (слова через подчёркивание: `user_name`, не `userName`)
- Избегай зарезервированных слов (не `int`, `str`, `print` как имена переменных)

**Почему?** Потому что через неделю ты не помнишь, что такое `a`, но помнишь, что такое `age`.

---

**Готов к следующей главе?** Дальше узнаешь, как СЧИТАТЬ с переменными! 🚀✨