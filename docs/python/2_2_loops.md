# 2.2 Циклы: Повторяем код

## Введение

До сих пор наши программы выполняли код **один раз сверху вниз**. Но часто нам нужно **повторить** одинаковый код много раз:
- Вывести числа от 1 до 100
- Обработать каждый элемент списка
- Спросить пользователя 5 раз
- Ждать команды, пока пользователь не введёт "выход"

Для этого нужны **циклы** (for и while) — они выполняют код **несколько раз**.

В этой главе ты научишься:
1. Писать циклы (for и while)
2. Использовать range() для счёта
3. Проходить по спискам (foreach)
4. Вложенные циклы
5. Прерывать и пропускать итерации (break, continue)

**Результат:** Ты сможешь **повторять код** столько раз, сколько нужно.

---

## Живой пример: Циклы в действии

Запусти этот код:

```python
# Простой цикл for
print("=== ЦИКЛ FOR ===")
for i in range(5):
    print(f"Итерация {i}")

# Цикл while
print("\n=== ЦИКЛ WHILE ===")
count = 0
while count < 3:
    print(f"Счётчик: {count}")
    count += 1

# Проход по списку
print("\n=== ЦИКЛ ПО СПИСКУ ===")
fruits = ["яблоко", "банан", "апельсин"]
for fruit in fruits:
    print(f"Я люблю {fruit}")
```

**Результат:**
```
=== ЦИКЛ FOR ===
Итерация 0
Итерация 1
Итерация 2
Итерация 3
Итерация 4

=== ЦИКЛ WHILE ===
Счётчик: 0
Счётчик: 1
Счётчик: 2

=== ЦИКЛ ПО СПИСКУ ===
Я люблю яблоко
Я люблю банан
Я люблю апельсин
```

**Видишь?** Код повторяется много раз. Это циклы! 🎯

---

## Теория: Что такое цикл?

### Определение

**Цикл** — это конструкция, которая **повторяет код несколько раз** (или даже бесконечно).

**Две части цикла:**
1. **Условие цикла** — как долго повторять?
2. **Тело цикла** — какой код повторять?

---

## Цикл `for` — повторяй для каждого значения

### Синтаксис

```python
for переменная in последовательность:
    код, который повторяется
```

### Пример 1: Счёт от 0 до 4 с `range()`

```python
for i in range(5):
    print(i)
```

**Результат:**
```
0
1
2
3
4
```

### Что такое `range()`?

`range()` — это **генератор чисел**:
- `range(5)` → 0, 1, 2, 3, 4 (от 0 до 4)
- `range(1, 6)` → 1, 2, 3, 4, 5 (от 1 до 5)
- `range(0, 10, 2)` → 0, 2, 4, 6, 8 (от 0 до 9, шаг 2)

```python
# Счёт от 1 до 5
for i in range(1, 6):
    print(i)

# Чётные числа до 10
for i in range(0, 11, 2):
    print(i)  # 0, 2, 4, 6, 8, 10
```

---

### Пример 2: Цикл по списку (foreach)

Часто нужно **обработать каждый элемент списка**:

```python
fruits = ["яблоко", "банан", "апельсин"]

for fruit in fruits:
    print(f"Фрукт: {fruit}")
```

**Результат:**
```
Фрукт: яблоко
Фрукт: банан
Фрукт: апельсин
```

---

### Пример 3: Если нужен индекс и значение

Используй `enumerate()`:

```python
fruits = ["яблоко", "банан", "апельсин"]

for index, fruit in enumerate(fruits):
    print(f"{index}: {fruit}")
```

**Результат:**
```
0: яблоко
1: банан
2: апельсин
```

---

## Цикл `while` — повторяй, пока условие верно

### Синтаксис

```python
while условие:
    код, который повторяется
```

**Важно:** Сам обновляй переменную в условии!

### Пример 1: Простой счётчик

```python
count = 0

while count < 5:
    print(f"Счёт: {count}")
    count += 1  # Не забудь обновить!
```

**Результат:**
```
Счёт: 0
Счёт: 1
Счёт: 2
Счёт: 3
Счёт: 4
```

---

### Пример 2: Бесконечный цикл (осторожно!)

```python
while True:
    name = input("Введи своё имя (или 'выход' для выхода): ")
    if name == "выход":
        print("До встречи!")
        break  # Выход из цикла
    print(f"Привет, {name}!")
```

---

## Управление циклом: `break` и `continue`

### `break` — прервать цикл

```python
for i in range(10):
    if i == 5:
        print("Стоп! Выхожу из цикла")
        break
    print(i)
```

**Результат:**
```
0
1
2
3
4
Стоп! Выхожу из цикла
```

---

### `continue` — пропустить текущую итерацию

```python
for i in range(5):
    if i == 2:
        print("Пропускаю 2")
        continue
    print(i)
```

**Результат:**
```
0
1
Пропускаю 2
3
4
```

---

## Вложенные циклы

Цикл в цикле:

```python
# Таблица умножения
for i in range(1, 4):
    for j in range(1, 4):
        print(f"{i} × {j} = {i * j}")
```

**Результат:**
```
1 × 1 = 1
1 × 2 = 2
1 × 3 = 3
2 × 1 = 2
2 × 2 = 4
2 × 3 = 6
3 × 1 = 3
3 × 2 = 6
3 × 3 = 9
```

---

## Практика: Пишем циклы

### Практика: Сумма чисел

```python
# Сумма чисел от 1 до 10
total = 0

for i in range(1, 11):
    total += i
    print(f"Итерация {i}: сумма = {total}")

print(f"Итоговая сумма: {total}")
```

**Результат:**
```
Итерация 1: сумма = 1
Итерация 2: сумма = 3
Итерация 3: сумма = 6
...
Итоговая сумма: 55
```

---

### Практика: Поиск в списке

```python
# Найти элемент в списке
numbers = [10, 25, 30, 45, 50]
search = 30

for index, num in enumerate(numbers):
    if num == search:
        print(f"Найдено! {search} на индексе {index}")
        break
else:
    print(f"{search} не найдено")
```

**Результат:**
```
Найдено! 30 на индексе 2
```

---

### Практика: Проверка таблицы умножения

```python
# Проверка, чётная или нечётная каждая итерация
for i in range(1, 6):
    if i % 2 == 0:
        print(f"{i} — чётное")
    else:
        print(f"{i} — нечётное")
```

**Результат:**
```
1 — нечётное
2 — чётное
3 — нечётное
4 — чётное
5 — нечётное
```

---

## Вопросы для самопроверки

1. **Что выведет этот код?**
   ```python
   for i in range(3):
       print(i)
   ```

2. **Какая разница между `for` и `while`?**

3. **Что делает `range(1, 6)`?**

4. **Как обойти список с индексами и значениями?**

5. **Что делает `break` и `continue`?**

---

## Практическое задание

### Задание: Калькулятор средней оценки

**Что нужно:**

Создай программу, которая:
1. Спрашивает у пользователя, сколько оценок он хочет ввести
2. Принимает оценки по одной
3. Вычисляет среднюю оценку
4. Выводит результат

```python
# Получаем количество оценок
count = int(input("Сколько оценок? "))

# Проверяем корректность
if count <= 0:
    print("Количество должно быть больше нуля!")
else:
    grades = []
    
    # Цикл для ввода оценок
    for i in range(count):
        grade = int(input(f"Оценка {i + 1}: "))
        
        # Проверяем, что оценка от 1 до 5
        if grade < 1 or grade > 5:
            print(f"⚠️ Оценка должна быть от 1 до 5, пропускаю")
            continue
        
        grades.append(grade)
    
    # Вычисляем среднюю оценку
    if len(grades) > 0:
        average = sum(grades) / len(grades)
        print(f"\n=== РЕЗУЛЬТАТ ===")
        print(f"Ввод оценок: {grades}")
        print(f"Средняя оценка: {average:.1f}")
        
        # Выводим соответствующее сообщение
        if average >= 4.5:
            print("🌟 Отличный результат!")
        elif average >= 3.5:
            print("👍 Хороший результат")
        elif average >= 2.5:
            print("📚 Нужно подтянуть")
        else:
            print("⚠️ Нужна помощь")
    else:
        print("Корректных оценок не введено")
```

**Пример работы:**
```
Сколько оценок? 4
Оценка 1: 5
Оценка 2: 4
Оценка 3: 6
⚠️ Оценка должна быть от 1 до 5, пропускаю
Оценка 4: 5

=== РЕЗУЛЬТАТ ===
Ввод оценок: [5, 4, 5]
Средняя оценка: 4.7
🌟 Отличный результат!
```

**Требования:**
- Используй `for` или `while` для цикла
- Используй `continue` для пропуска неверных значений
- Проверяй корректность данных
- Выведи понятный результат

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач использует циклы!**

```python
tasks = [
    {"title": "Купить продукты", "completed": False},
    {"title": "Написать отчёт", "completed": True},
    {"title": "Позвонить другу", "completed": False}
]

# Вывести все задачи
print("=== МОИ ЗАДАЧИ ===")
for i, task in enumerate(tasks, 1):
    status = "✓" if task["completed"] else "✗"
    print(f"{i}. {status} {task['title']}")

# Обработать завершённые задачи
completed = 0
for task in tasks:
    if task["completed"]:
        completed += 1

print(f"\nВыполнено: {completed} из {len(tasks)}")
```

На этом этапе:
- 📝 Ты учишься повторять код
- 🔜 Потом научишься организовывать код (функции)
- 🔜 Потом сохранять данные (файлы)

---

## Резюме: Три главных вывода

✅ **`for` нужен, когда знаешь сколько повторений.** Используй `for` с `range()` или со списком.

✅ **`while` нужен, когда условие динамичное.** Используй `while`, когда остановка зависит от пользователя или переменной.

✅ **`break` прерывает, `continue` пропускает.** Используй их для управления циклом.

---

## Что дальше?

→ **Глава 2.3** (Функции): Как организовывать код

→ **Глава 2.4** (Работа с файлами): Как сохранять данные

---

## 💡 Важный совет: Бесконечный цикл

**Осторожно с `while True`!**

Если забудешь обновить переменную или `break`, цикл будет работать **вечно**:

```python
# ❌ ПЛОХО! Бесконечный цикл
count = 0
while True:
    print(count)
    # Забыли count += 1!

# ✅ ХОРОШО! Контролируемый цикл
count = 0
while count < 10:
    print(count)
    count += 1
```

**Правило:** Всегда думай о том, когда цикл **закончится**.

---

**Поздравляем!** Ты освоил циклы — самый мощный инструмент для повтора кода! 🎉

**Модуль 2 продолжается →** Следующий — функции (организация кода). 🚀✨