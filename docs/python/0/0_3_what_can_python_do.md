# 0.3 Что можно делать с Python? Практические примеры

## Введение

Python — это универсальный инструмент, как швейцарский нож. Он может резать, открывать, измерять — почти всё. Но
новичков часто пугают вопросом: "Окей, а конкретно что я буду писать?"

В этой главе покажу тебе **реальные проекты**, которые ты сможешь создать с Python, от простых до сложных. Не "может в
теории", а "вот код, который это делает".

---

## Живой пример: Один инструмент, много применений

Посмотри, как **один код** решает три совсем разных задачи:

### Задача 1: Получить информацию с сайта 🌐

```python
# Скачиваем список цен на товары с сайта
import requests
from bs4 import BeautifulSoup

response = requests.get("https://example.com/prices")
soup = BeautifulSoup(response.text, "html.parser")
prices = soup.find_all("span", class_="price")

for price in prices:
    print(price.text)
```

### Задача 2: Обработать данные 📊

```python
# Загружаем таблицу с продажами и вычисляем среднюю
import pandas as pd

data = pd.read_csv("sales.csv")
average = data["amount"].mean()
print(f"Средняя сумма продажи: {average} рублей")
```

### Задача 3: Автоматизировать файлы 🔄

```python
# Переименовываем все фотографии в папке по дате
import os
from datetime import datetime

for filename in os.listdir("photos/"):
    old_path = f"photos/{filename}"
    timestamp = os.path.getmtime(old_path)
    date = datetime.fromtimestamp(timestamp).strftime("%Y-%m-%d")
    new_name = f"photos/{date}_{filename}"
    os.rename(old_path, new_name)
```

**Видишь?** Три совершенно разных задачи — парсинг веб-сайтов, анализ данных, работа с файлами. Python справляется со
всеми. 🐍

---

## 1️⃣ Веб-скрейпинг: Сбор данных с интернета

### Что это?

**Web scraping** (скрейпинг) — это автоматическое получение информации с веб-сайтов. Вместо того, чтобы вручную
копировать данные, ты пишешь скрипт, который это делает за тебя.

### Реальный пример: Собрать все книги с сайта

```python
import requests
from bs4 import BeautifulSoup

# Открываем веб-сайт с книгами
url = "https://books.example.com"
response = requests.get(url)

# Парсим HTML (превращаем в структурированные данные)
soup = BeautifulSoup(response.text, "html.parser")

# Находим все названия и цены
books = soup.find_all("div", class_="book")
for book in books:
    title = book.find("h2").text
    price = book.find("span", class_="price").text
    print(f"{title}: {price}")
```

### Когда это полезно?

- 📊 Собрать цены конкурентов для анализа
- 🏠 Получить данные о недвижимости со всех сайтов объявлений
- 📰 Автоматически собирать новости из интернета
- 📈 Мониторить изменения на сайте (когда появилась нужная вещь в наличии)

### На практике

**Библиотеки для веб-скрейпинга:**

- `requests` — загружает веб-сайты
- `BeautifulSoup` — парсит HTML
- `Selenium` — для сайтов с JavaScript (более сложные)

---

## 2️⃣ Обработка данных: Работа с таблицами и статистикой

### Что это?

**Data processing** — это работа с большими объёмами информации: сортировка, фильтрация, анализ, создание отчётов.

### Реальный пример: Анализ продаж

```python
import pandas as pd

# Загружаем таблицу с продажами
sales = pd.read_csv("sales.csv")

# Выводим информацию о продажах по месяцам
sales_by_month = sales.groupby("month")["amount"].sum()
print(sales_by_month)

# Находим лучшего продавца
best_seller = sales.groupby("seller")["amount"].sum().idxmax()
print(f"Лучший продавец: {best_seller}")

# Сохраняем отчёт в новый файл
sales_by_month.to_csv("report.csv")
```

### Когда это полезно?

- 📊 Анализировать продажи компании
- 🏥 Обрабатывать медицинские данные пациентов
- 🎓 Анализировать результаты опросов
- 💼 Создавать автоматические отчёты для босса
- 📈 Находить тренды в данных

### На практике

**Библиотека номер 1 для работы с данными:**

- `pandas` — работа с таблицами (самая популярная)
- `numpy` — работа с числовыми массивами
- `matplotlib` — создание графиков

---

## 3️⃣ Автоматизация: Делаем скучную работу за нас

### Что это?

**Автоматизация** — это когда ты пишешь скрипт, который делает рутинную работу вместо тебя. Например, переименовывает
файлы, отправляет письма, обновляет базу данных.

### Реальный пример 1: Переименовать фотографии по дате

```python
import os
from datetime import datetime

# В папке 1000 фотографий с названиями вроде "IMG_0001.jpg"
# Хотим переименовать в "2025-01-15_photo_1.jpg"

for filename in os.listdir("photos/"):
    if filename.endswith(".jpg"):
        old_path = f"photos/{filename}"
        # Берём дату создания файла
        timestamp = os.path.getmtime(old_path)
        date = datetime.fromtimestamp(timestamp).strftime("%Y-%m-%d")
        # Новое имя
        new_name = f"photos/{date}_{filename}"
        os.rename(old_path, new_name)
        print(f"Переименован: {filename} → {new_name}")

print("✅ Готово! Все фотографии переименованы")
```

**Результат:** Вместо того, чтобы вручную переименовывать 1000 файлов (5+ часов), скрипт сделает это за 10 секунд.

### Реальный пример 2: Отправить письма 100 людям

```python
import smtplib
from email.mime.text import MIMEText

# Список людей и их результатов
results = [
    ("ivan@example.com", "Иван", 95),
    ("maria@example.com", "Мария", 87),
    ("petr@example.com", "Пётр", 91),
    # ... ещё 97 человек
]

# Отправляем письмо каждому
for email, name, score in results:
    message = f"Привет, {name}! Твой результат тестирования: {score} из 100"
    
    msg = MIMEText(message)
    msg["Subject"] = "Результаты теста"
    msg["From"] = "admin@example.com"
    msg["To"] = email
    
    # Отправляем через SMTP
    server = smtplib.SMTP("smtp.gmail.com", 587)
    server.sendmail("admin@example.com", email, msg.as_string())
    server.quit()
    
    print(f"✅ Письмо отправлено {name}")
```

**Результат:** Вместо того, чтобы писать письма вручную (10 часов), скрипт отправит их за 2 минуты.

### Когда это полезно?

- 📁 Организовать файлы на компьютере (переименовать, переместить, сжать)
- 📧 Отправить 1000 писем автоматически
- 🗄️ Обновить данные в базе данных (найти ошибки, добавить новые записи)
- 🔄 Синхронизировать папки между компьютерами
- ⏰ Запустить процесс по расписанию (каждый день в 9 утра)

### На практике

**Стандартные модули Python:**

- `os`, `shutil` — работа с файлами
- `smtplib` — отправка писем
- `sqlite3` — работа с базами данных
- `schedule` — запуск по расписанию

---

## 4️⃣ Веб-приложения: Создание сайтов и API

### Что это?

**Web application** — это сайт или сервис, который живёт на интернете. Например, Instagram, Gmail, Spotify. Python
позволяет создать сервернуючасть (бэкенд).

### Реальный пример: Создать простой API

```python
# Файл: app.py
from flask import Flask, jsonify

app = Flask(__name__)

# Наша база данных (списком)
tasks = [
    {"id": 1, "title": "Купить молоко", "done": False},
    {"id": 2, "title": "Написать отчёт", "done": False},
]

# Эндпоинт для получения всех задач
@app.route("/tasks", methods=["GET"])
def get_tasks():
    return jsonify(tasks)

# Эндпоинт для добавления новой задачи
@app.route("/tasks", methods=["POST"])
def add_task():
    new_task = {"id": len(tasks) + 1, "title": "Новая задача", "done": False}
    tasks.append(new_task)
    return jsonify(new_task), 201

if __name__ == "__main__":
    app.run(debug=True)
```

**Результат:**

- Запустил скрипт → `http://localhost:5000/tasks` работает
- Любой фронтенд (web, мобильное приложение) может работать с твоим API

### Когда это полезно?

- 💬 Создать свой мессенджер
- 🛒 Сделать интернет-магазин
- 📱 Сделать бэкенд для мобильного приложения
- 📊 Создать панель для управления данными
- 🎮 Создать сервер для многопользовательской игры

### На практике

**Фреймворки для веб-приложений:**

- `Flask` — лёгкий фреймворк (идеален для начинающих)
- `Django` — мощный фреймворк (сложнее, но больше возможностей)
- `FastAPI` — современный фреймворк (высокая производительность)

---

## 5️⃣ Машинное обучение: Обучить компьютер думать

### Что это?

**Machine Learning** — это когда компьютер сам учится находить закономерности в данных. Например, угадать спам,
распознать лицо, предсказать погоду.

### Реальный пример: Предсказать цену дома

```python
# Примерный алгоритм (реальный код сложнее)
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
import pandas as pd

# Загружаем данные о домах
data = pd.read_csv("houses.csv")  # площадь, год постройки, цена

# Разделяем на тренировочную и тестовую выборку
X_train, X_test, y_train, y_test = train_test_split(
    data[["square_feet", "year"]], 
    data["price"], 
    test_size=0.2
)

# Обучаем модель
model = LinearRegression()
model.fit(X_train, y_train)

# Делаем предсказание
new_house = [[2000, 2010]]  # 2000 кв.м., построен в 2010
predicted_price = model.predict(new_house)
print(f"Предсказанная цена: {predicted_price[0]} рублей")
```

### Когда это полезно?

- 📧 Фильтр спама (определить, это спам или нет)
- 📸 Распознавание лиц (как в Instagram)
- 🎬 Рекомендации (что показать пользователю)
- 🚗 Беспилотные машины
- 💊 Медицинская диагностика

### На практике

**Библиотеки для машинного обучения:**

- `scikit-learn` — простой ML (классификация, регрессия)
- `TensorFlow` / `PyTorch` — глубокое обучение (нейросети)
- `numpy` / `pandas` — обработка данных

---

## 6️⃣ Научные вычисления: Математика и физика

### Что это?

Python используют учёные и инженеры для вычислений, моделирования и визуализации.

### Реальный пример: Симуляция падения предмета

```python
import numpy as np
import matplotlib.pyplot as plt

# Параметры
g = 9.8  # ускорение свободного падения
t = np.linspace(0, 2, 100)  # время от 0 до 2 секунд

# Высота в момент времени t (для объекта, брошенного вверх)
h0 = 50  # начальная высота в метрах
v0 = 20  # начальная скорость в м/с
height = h0 + v0 * t - 0.5 * g * t**2

# Визуализируем
plt.plot(t, height)
plt.xlabel("Время (секунды)")
plt.ylabel("Высота (метры)")
plt.title("Падение предмета")
plt.show()
```

### Когда это полезно?

- 🔬 Математические исследования
- 🏗️ Инженерные расчёты (прочность материалов, аэродинамика)
- 📡 Обработка сигналов
- 🌌 Астрономические вычисления

---

## 7️⃣ Прототипирование игр: Простые игры и демки

### Что это?

Python позволяет быстро создать прототип игры. Когда идея работает, её переписывают на C++ для высокой
производительности.

### Реальный пример: Простая игра "Угадай число"

```python
import random

# Компьютер загадывает число
number = random.randint(1, 100)
attempts = 0

print("Я загадал число от 1 до 100")
print("Попробуй его угадать!")

while True:
    guess = int(input("Твой ответ: "))
    attempts += 1
    
    if guess == number:
        print(f"🎉 Угадал! Число было {number}")
        print(f"Ты потратил {attempts} попыток")
        break
    elif guess < number:
        print("Это число меньше. Попробуй больше.")
    else:
        print("Это число больше. Попробуй меньше.")
```

### На практике

**Библиотека для 2D-игр:**

- `pygame` — полнофункциональная библиотека для 2D-игр

---

## Практика: Выбери свой проект

### Практика: Куда ты пойдёшь дальше?

**Задача:** Подумай, какой проект тебя интересует больше всего. Прочитай описание, посмотри на код (даже если не всё
понимаешь).

**Варианты:**

1. **Веб-скрейпинг** — ты интересуешься данными с интернета?
2. **Обработка данных** — ты любишь работать с таблицами и цифрами?
3. **Автоматизация** — ты хочешь избавиться от скучной работы?
4. **Веб-приложение** — ты мечтаешь создать свой сервис?
5. **Машинное обучение** — тебя привлекает AI?
6. **Научные вычисления** — ты увлекаешься наукой?
7. **Игры** — ты хочешь создать игру?

**Что попробовать:**

1. Выбери направление, которое звучит интересно
2. Запусти пример кода (если можешь)
3. Поменяй значения в коде и посмотри, что изменится

**Совет:** Не зубри все примеры. Просто посмотри, что возможно. Когда будешь готов, вернёшься к нужному направлению.

---

## Вопросы для самопроверки

1. **Какие три области применения Python ты запомнил?**
   (Напиши в одной строке)

2. **Почему Python популярен для веб-скрейпинга?**
   (Подсказка: простота + библиотеки)

3. **Какую библиотеку используют для работы с таблицами?**

4. **Может ли Python автоматизировать работу с файлами?**
   (Да, но покажи пример)

---

## Практическое задание

### Задание: Определи свой путь

**Что нужно сделать:**

Напиши в текстовом файле (или просто в консоли) ответы на вопросы:

1. **Какая область Python тебя интересует больше всего?**
   (Выбери одну из семи)

2. **Почему она тебе интересна?**
   (1-2 предложения)

3. **Какую задачу ты хотел бы решить в этой области?**
   (Конкретный пример, например: "Собрать цены на смартфоны с сайта")

4. **Какую библиотеку нужно будет выучить?**
   (Посмотри в тексте главы)

**Пример:**

```
1. Автоматизация
2. Мне надоело переименовывать 1000 файлов вручную
3. Написать скрипт, который переименует все фотографии по дате
4. os, shutil, datetime
```

---

## Резюме: Три главных вывода

✅ **Python используется везде.** От веб-сайтов до научных исследований. Это не просто "язык для начинающих" — это язык,
который используют профессионалы.

✅ **У Python есть библиотека для любой задачи.** Парсинг, данные, автоматизация, веб, ML — всё уже написано опытными
разработчиками.

✅ **Ты можешь начать с любого направления.** Не нужно изучать ВСЁ. Выбери одно, посмотри примеры, заинтересовался —
углубляйся дальше.

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач** — это автоматизация!

Ты создаёшь инструмент, который:

- Модуль 2: **Хранит задачи** в памяти (как база данных)
- Модуль 3: **Фильтрует задачи** по критериям (поиск, сортировка)
- Модуль 4: **Выделяет логику** в функции (чистая архитектура)
- Модуль 5: **Сохраняет в файл** (постоянное хранилище)
- Модуль 5: **Обрабатывает ошибки** (надёжность)

Это **настоящий проект**, который используют люди, чтобы организовать свою жизнь.

---

## Что дальше?

→ **Модуль 1** (Основы Python): Теперь ты готов учиться писать код!

→ **Глава 1.2** (следующая): Переменные — как хранить данные

→ **Глава 4** (Функции): Как организовать код, как в настоящих проектах

---

## 💡 Финальная мысль

Каждый сложный проект начинается с простого скрипта на 10 строк кода. Ты сейчас тоже начинаешь с нуля, но через месяц
сможешь написать:

- Скрипт, который скачивает данные
- Программу, которая анализирует их
- Сервис, который люди будут использовать

Начни с первого шага. Учись переменным. Потом циклам. Потом функциям. И вот уже ты создаёшь реальный менеджер задач. 🐍✨