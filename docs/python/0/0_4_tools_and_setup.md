# 0.4 Инструменты для Python: Выбираем рабочее место

## Введение

Ты вот-вот начнёшь писать код. Но сначала нужно подготовить "рабочее место". Это как выбирать инструмент для работы:
нельзя пилить дерево щёткой и нельзя красить забор пилой.

Есть три основных типа инструментов для написания Python-кода:

1. **Текстовые редакторы** — простые и лёгкие
2. **IDE (интегрированные среды разработки)** — мощные и полнофункциональные
3. **Консольные редакторы** — для опытных (не рекомендуем новичкам)

В этой главе я помогу тебе выбрать подходящий инструмент и установить его. **Рекомендация для новичков: VS Code** — это
идеальный баланс между простотой и мощностью.

---

## Сравнение инструментов: Какой выбрать?

| Характеристика            | Текстовый редактор (VS Code) | IDE (PyCharm)            | Консольный редактор (Vim) |
|---------------------------|------------------------------|--------------------------|---------------------------|
| **Сложность установки**   | ⭐ Очень простая              | ⭐⭐ Средняя               | ⭐⭐⭐ Сложная               |
| **Скорость работы**       | ⭐⭐⭐ Быстрая                  | ⭐⭐ Медленнее             | ⭐⭐⭐ Очень быстрая         |
| **Функции из коробки**    | ⭐⭐ Нужно настраивать         | ⭐⭐⭐ Всё готово           | ⭐ Минимум                 |
| **Для новичков?**         | ✅ Идеально                   | ✅ Хорошо                 | ❌ Сложно                  |
| **Для больших проектов?** | ⭐⭐ Нужны расширения          | ⭐⭐⭐ Отлично              | ⭐⭐ Для опытных            |
| **Стоимость**             | 💰 Бесплатно                 | 💰 Бесплатно (Community) | 💰 Бесплатно              |
| **Занимаемое место**      | 📦 ~300 МБ                   | 📦 ~2 ГБ                 | 📦 ~10 МБ                 |

---

## 1️⃣ Текстовый редактор: VS Code (рекомендуем)

### Что это?

**Visual Studio Code** (VS Code) — это лёгкий, но мощный текстовый редактор от Microsoft. Он изначально прост, но через
расширения можно добавить функции IDE.

### Преимущества для новичка

✅ **Просто устанавливается** — 5 минут и всё готово  
✅ **Быстро работает** — не тормозит на старых компьютерах  
✅ **Много расширений** — легко добавить нужную функцию  
✅ **Большое сообщество** — много туториалов и помощи  
✅ **Бесплатно** — не нужно платить за лицензию  
✅ **Работает везде** — Windows, Mac, Linux

### Недостатки

❌ Нужно самому настроить расширения (но это просто)

### Пошаговая установка VS Code

#### Шаг 1: Скачать VS Code

1. Открой официальный сайт: https://code.visualstudio.com/
2. Нажми на большую синюю кнопку "Download for [твоя ОС]"
3. Скачается файл `.exe` (Windows) или `.dmg` (Mac)

#### Шаг 2: Установить VS Code

**На Windows:**

1. Двойной клик по скачанному файлу
2. Нажимай "Next" → "Next" → "Install"
3. После установки VS Code откроется автоматически

**На Mac/Linux:**

1. Следуй инструкциям установщика
2. Скопируй VS Code в папку Applications

#### Шаг 3: Установить расширение Python

1. Открой VS Code
2. Слева внизу найди иконку с четырьмя квадратами (Extensions / Расширения)
3. В поле поиска напиши: `Python`
4. Найди расширение **Python** от Microsoft (с синей иконкой)
5. Нажми "Install"

**Готово!** Теперь VS Code полностью настроен для Python.

### Как запустить первую программу в VS Code

1. **Создай файл:**
	- Файл → Новый файл (или Ctrl+N)
	- Сохрани как `hello.py` (обязательно расширение `.py`)

2. **Напиши код:**
   ```python
   print("Привет из VS Code!")
   ```

3. **Запусти код:**
	- Нажми кнопку ▶️ (Play) вверху справа
	- Или нажми Ctrl+F5

4. **Посмотри результат:**
	- Снизу откроется "Terminal" с выводом программы

---

## 2️⃣ IDE: PyCharm (для более опытных)

### Что это?

**PyCharm** — это мощная интегрированная среда разработки, специально созданная для Python. Она делает огромную часть
работы за тебя.

### Преимущества

✅ **Всё готово из коробки** — не нужно ничего настраивать  
✅ **Отличный отладчик** — видишь, что происходит в коде строка за строкой  
✅ **Подсказки** — IDE говорит, что ты пишешь неправильно, ЕЩЁ ДО того, как запустишь  
✅ **Встроенный тестировщик** — проверка кода включена по умолчанию  
✅ **Управление проектом** — удобно работать со сложными проектами

### Недостатки

❌ **Требовательна к системе** — медленнее, чем VS Code  
❌ **Больше функций** — может быть сложновата для новичка (много кнопок и меню)  
❌ **Дольше устанавливается** — ~2 ГБ на диск

### Когда использовать PyCharm?

**Используй PyCharm, когда:**

- Ты уже освоился с основами Python
- Работаешь над большим проектом (100+ строк кода)
- Хочешь, чтобы IDE помогала с отладкой

**Не используй PyCharm, если:**

- Ты первый день пишешь код (слишком много функций)
- У тебя старый компьютер (слишком тяжело)

### Краткая установка

1. Скачай отсюда: https://www.jetbrains.com/pycharm/
2. Выбери "Community Edition" (бесплатная)
3. Установи как обычную программу
4. При первом запуске выбери тему (Dark или Light)

---

## 3️⃣ Консольные редакторы (не рекомендуем новичкам)

### Что это?

**Vim**, **Nano**, **Emacs** — это редакторы, которые работают в консоли (чёрное окно с текстом). Они очень древние (30+
лет), но до сих пор используются опытными программистами.

### Преимущества

✅ Очень быстрые  
✅ Занимают мало места  
✅ Работают на любом компьютере (даже на серверах)

### Недостатки

❌ **Очень сложно учиться** — управление мышкой не работает, только клавиатура  
❌ **Много горячих клавиш** — нужно запомнить 50+ команд  
❌ **Нет визуальной помощи** — IDE показывает ошибки, Vim — нет

### Наш совет для тебя

**Не используй Vim/Nano в начале!** Научись основам на VS Code, а потом (через полгода) попробуешь консольный редактор,
если захочешь.

---

## ⭐ Наша рекомендация для тебя

### Если ты новичок: **VS Code**

✅ **Почему?**

- Устанавливается за 5 минут
- Хватает функций, но не перегружает
- Можно писать код в тот же день
- Большое сообщество и помощь в интернете

**Установка:**

1. Скачай: https://code.visualstudio.com/
2. Установи расширение Python
3. Создай файл `hello.py`
4. Напиши: `print("Я готов к Python!")`
5. Запусти код (кнопка ▶️)

### Если ты уже программировал: **PyCharm Community**

✅ **Почему?**

- Специально для Python
- Много помощи встроено
- Удобно работать над сложными проектами

### Если ты в сервере или любишь вызовы: **Vim** (позже)

⚠️ **Не сейчас!** Вернись к этому через несколько месяцев.

---

## 🔧 Важная настройка: Виртуальное окружение (venv)

### Что это и зачем?

**Виртуальное окружение (venv)** — это изолированная копия Python специально для твоего проекта. Это как отдельная
комната с полочками для твоих инструментов (библиотек).

**Зачем нужно?**

Представь:

- **Без venv:** Все библиотеки Python уходят в одну папку. Если одна библиотека конфликтует с другой — весь Python
  сломан.
- **С venv:** Каждый проект — своя изолированная копия Python. Ломаешь — только один проект, остальные в порядке.

### Как создать venv?

#### Шаг 1: Открой консоль в папке твоего проекта

**На Windows:**

1. В проводнике открой папку, где будешь писать код
2. Щелчок правой кнопкой в пустом месте
3. Выбери "Open in Terminal" (или "Open Command Prompt here")

**На Mac/Linux:**

1. Открой Terminal
2. Напиши: `cd` (пробел) и перетащи папку проекта в Terminal
3. Нажми Enter

#### Шаг 2: Создай виртуальное окружение

Напиши в консоли:

```bash
python -m venv venv
```

**Что произойдёт?**

- Создастся папка `venv` с копией Python
- Это займёт 30-50 секунд

#### Шаг 3: Активируй виртуальное окружение

**На Windows:**

```bash
venv\Scripts\activate
```

**На Mac/Linux:**

```bash
source venv/bin/activate
```

**Как проверить, что активировано?**
В консоли появится `(venv)` в начале строки:

```
(venv) C:\Users\Your Name\projects\my_project>
```

✅ **Готово! Теперь твой Python изолирован.**

### Как использовать venv дальше?

1. **Если закрыл консоль:** Нужно снова её открыть и активировать venv
2. **Установка библиотек:**
   ```bash
   pip install requests
   ```
3. **Запуск программ:** Всё работает как обычно, но в изолированной среде

---

## Практика: Выбери инструмент и установи

### Практика: Подготовка рабочего места

**Задача:** Выбери один инструмент (рекомендуем VS Code), установи его и напиши первую программу.

**Пошагово:**

1. **Выбрать:** VS Code (новичок) или PyCharm (если уже программировал)
2. **Установить:** Следуй инструкциям выше
3. **Создать папку проекта:**
	- Создай папку: `python_course` (или любое имя без пробелов)
4. **Создать виртуальное окружение:**
   ```bash
   python -m venv venv
   ```
5. **Активировать venv:**
	- Windows: `venv\Scripts\activate`
	- Mac/Linux: `source venv/bin/activate`
6. **Создать файл `hello.py`:**
   ```python
   # Моя первая программа!
   name = input("Как тебя зовут? ")
   print(f"Привет, {name}!")
   print("Я готов к Python! 🐍")
   ```
7. **Запустить программу:**
	- В VS Code: кнопка ▶️
	- В консоли: `python hello.py`

**Результат:**

```
Как тебя зовут? Миша
Привет, Миша!
Я готов к Python! 🐍
```

**Если что-то не работает:** Проверь:

- Python установлен? (напиши в консоли: `python --version`)
- venv активирован? (в консоли должно быть `(venv)` в начале)
- Файл сохранён? (Ctrl+S)

---

## Вопросы для самопроверки

1. **Какой инструмент рекомендуется новичкам?**
   (VS Code, PyCharm или Vim?)

2. **Зачем нужно виртуальное окружение (venv)?**
   (Для чего оно изолирует код?)

3. **Как активировать venv на Windows?**
   (Напиши команду)

4. **В какой IDE работает Microsoft?**
   (VS Code, PyCharm, Vim?)

---

## Практическое задание

### Задание: Установи инструмент и напиши "Hello, Python!"

**Требования:**

1. Установи VS Code или PyCharm (рекомендуем VS Code)
2. Создай папку для проекта: `my_first_python`
3. Создай виртуальное окружение: `python -m venv venv`
4. Активируй его
5. Создай файл `main.py` с таким кодом:
   ```python
   # Это моя первая программа на Python!
   message = "Я официально начал изучать Python!"
   print(message)
   print("=" * len(message))
   ```
6. Запусти программу и скопируй результат в текстовый файл

**Результат должен быть похож на:**

```
Я официально начал изучать Python!
=====================================
```

**Отправь скриншот консоли со своим результатом (если есть кому).**

---

## Резюме: Три главных вывода

✅ **Выбирай VS Code, если ты новичок.** Это идеальный баланс: просто устанавливается, быстро работает, но имеет все
нужные функции.

✅ **Создавай виртуальное окружение для каждого проекта.** Это защищает твой Python от конфликтов библиотек и делает код
чистым.

✅ **IDE (PyCharm) пригодится позже, когда проекты станут больше.** Сейчас VS Code будет оптимально.

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач** будет твоим первым проектом.

- **Модуль 1:** Напишешь первый `main.py` в VS Code
- **Модуль 2:** Добавишь функции для работы со списком задач
- **Модуль 3:** Вынесешь логику в отдельные функции
- **Модуль 4:** Добавишь сохранение в файл
- **Финал:** Проект с 200+ строк кода, готовый к использованию

Всё это будет в VS Code с venv. Просто, удобно, профессионально. 🐍

---

## Что дальше?

→ **Модуль 1.1** (Переменные): Первые строки кода на Python!

→ **Консоль и горячие клавиши:** После нескольких недель можешь посмотреть советы по ускорению работы.

---

## 💡 Финальный совет

**Не зацикливайся на выборе инструмента.** VS Code — хороший выбор, потом всегда сможешь перейти на PyCharm. Главное —
начать писать код, а не разбираться с настройками.

Потрать 30 минут на установку → начни писать код → учишься на практике.

Вперёд! 🚀✨