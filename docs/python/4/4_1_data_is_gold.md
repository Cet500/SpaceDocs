# 4.1 Данные: Цифровое золото

## Введение: Программирование = преобразование данных

До сих пор ты писал код, который:

```python
# Что-то вычисляет
result = 2 + 2  # = 4

# Что-то выводит
print("Привет, мир!")

# Что-то запоминает
name = "Иван"
```

**Но код нужен для чего?**

Для **преобразования одних данных в другие!**

```
Входные данные → Код (преобразование) → Выходные данные

Примеры:
Email + пароль → Код (проверка) → True/False (можно войти?)
CSV файл → Код (анализ) → Статистика (средний возраст)
Звук → Код (обработка) → Текст (распознавание речи)
Картинка → Код (нейросеть) → "это кот" (классификация)
```

**Вот это и есть программирование!** 🔄

**В этой главе ты узнаешь про "сырьё" для твоего кода — ДАННЫЕ.**

**План модуля 4:**

1. **4.1** (эта глава): Что такое данные, форматы, отличие от информации
2. **4.2**: JSON и XML (как хранятся структурированные данные)
3. **4.3**: Работа с файлами (читать, писать, обрабатывать)
4. **4.4**: CSV и таблицы (работать с табличными данными)
5. **4.5**: Введение в БД (базы данных, SQL, SQLite)
6. **4.6**: Откуда берутся данные (API, веб-скрепинг, датасеты)

---

## Живой пример: Данные повсюду

### Приложение "Менеджер задач" (из модуля 3)

```python
# Входные данные:
"Купить продукты"        # название
"2025-01-05"            # дата
1                       # приоритет

# Код (преобразование):
task = Task(
    title="Купить продукты",
    deadline="2025-01-05",
    priority=1
)
task.save()

# Выходные данные:
# В памяти: объект Task
# На диске: JSON файл
# В БД: запись в таблице
# В веб-приложении: HTML страница
```

**Видишь?** Одни и те же данные представлены по-разному!

### Веб-приложение (Instagram-подобное)

```
Пользователь заполнил форму:
    Фото JPG → Код (обработка) → Картинка 500x500 + 200x200
    Текст "Привет!" → Код (парсинг) → Ссылки, хэштеги
    Локация → Код (валидация) → Координаты (lat, lng)

Всё это сохраняется в БД → Код (запрос) → Выводится другим пользователям на странице
```

**Программирование = машина преобразования данных!** 🔄

---

## Данные vs Информация: В чём разница?

### Определения:

**Данные** — это **сырые факты без контекста**

```
"25"           ← просто число
"Москва"       ← просто слово
"2025-01-05"   ← просто строка
```

**Информация** — это **данные с контекстом и смыслом**

```
"25 лет"           ← я теперь знаю возраст человека!
"Москва, Россия"   ← я знаю город и страну
"2025-01-05"       ← это дата... но дата чего?
"Дедлайн: 2025-01-05"  ← АХА! Это дедлайн задачи!
```

### Примеры:

| Данные | + Контекст | = Информация |
|--------|-----------|--------------|
| "25" | возраст | "Мне 25 лет" |
| "100" | зарплата (руб) | "Зарплата 100,000 руб" |
| "5.4" | оценка | "Фильм получил 5.4/10 на IMDb" |
| "москва, питер, казань" | города | "Я жил в 3 городах" |

### Почему это важно?

```python
# Данные (код без смысла):
data = "25, 100000, москва, 5.4"

# Информация (код с контекстом):
user = {
    "age": 25,
    "salary": 100000,
    "city": "москва",
    "movie_rating": 5.4
}
```

**Программист должен превращать данные в информацию!** 📊

---

## Какие данные обрабатывает компьютер?

### Всё можно представить в виде чисел!

**Текст:**
```python
"A" = 65 (ASCII код)
"B" = 66
"Hello" = [72, 101, 108, 108, 111]
```

**Звук:**
```
Амплитуда волны в каждый момент времени
44,100 раз в секунду (44.1 кГц)
[0.2, 0.3, 0.5, -0.1, ...]
```

**Картинка:**
```
Каждый пиксель = RGB значения (0-255)
1920x1080 пиксели = 1920 * 1080 * 3 = 6,220,800 чисел!
```

**Видео:**
```
30 кадров в секунду
1 минута видео = 30 * 60 = 1,800 картинок
Каждая картинка = 6 млн чисел
= 10.8 млрд чисел за минуту!
```

**Координаты (GPS):**
```
Широта: 55.7558 (Москва)
Долгота: 37.6173
Всё = число!
```

### Основной закон:

**ВСЁ В КОМПЬЮТЕРЕ = ЧИСЛА!** 🔢

```
Текст → ASCII коды → числа
Звук → амплитуда волны → числа
Видео → RGB значения → числа
Фото → пиксели → числа
Геолокация → координаты → числа
Даты → Unix timestamp → числа

Компьютер может работать только с числами!
```

---

## Как хранятся данные?

### На уровне "железа":

Компьютер использует **биты** (0 и 1):

```
Число 5 = 00000101 (8 бит = 1 байт)
Число 255 = 11111111 (8 бит)
Число 256 = 100000000 (9 бит)
```

### Иерархия:

```
Бит          (0 или 1)          ← минимум
Байт         (8 бит)            ← 1 символ примерно
Килобайт     (1,000 байт)       ← короткий текст
Мегабайт     (1,000,000 байт)   ← музыка или фото
Гигабайт     (1 млрд байт)      ← фильм в высоком качестве
Терабайт     (1 трлн байт)      ← большая БД или архив
```

### На диске (физически):

Данные хранятся на **магнитных дисках** (HDD) или **электронных чипах** (SSD):

```
HDD (старое):          SSD (новое):
↻ вращается            ← нет движения
медленнее               быстрее
дешевле                дороже
громче                 тише
но: ёмкость больше     ёмкость меньше
```

### В памяти (во время работы):

Когда программа работает, данные находятся в **RAM** (оперативная память):

```
Диск (медленно, постоянно)
↓ загрузить в RAM
Память (быстро, пока работает)
↓ обработать
Результат
↓ сохранить на диск
```

---

## Что такое формат данных?

**Формат** — это **способ организации данных**.

### Примеры:

**Текстовый формат (легко читать людям):**
```
name: Иван
age: 25
city: Москва
```

**CSV (табличный):**
```
name,age,city
Иван,25,Москва
Мария,23,Казань
```

**JSON (структурированный):**
```json
{
  "name": "Иван",
  "age": 25,
  "city": "Москва"
}
```

**XML (много скобок):**
```xml
<user>
  <name>Иван</name>
  <age>25</age>
  <city>Москва</city>
</user>
```

**Бинарный (для компьютера, не читается):**
```
110110110110110110110110
(непонятно для человека, но компьютер понимает)
```

### Почему разные форматы?

```
Если нужно:
- люди читали      → текст, CSV
- структура важна  → JSON, XML
- компактно        → бинарный
- быстро обработать → бинарный
- простота        → JSON
```

---

## JSON: Король форматов

### Что такое JSON?

**JSON** (JavaScript Object Notation) — это способ представления данных в виде текста.

```json
{
  "name": "Иван",
  "age": 25,
  "email": "ivan@example.com",
  "is_active": true,
  "skills": ["Python", "JavaScript", "SQL"],
  "address": {
    "city": "Москва",
    "country": "Россия"
  }
}
```

### Почему он везде?

✅ **Простой** — легко читать и писать
✅ **Компактный** — не много лишнего
✅ **Универсальный** — все языки его поддерживают
✅ **Структурированный** — видна иерархия

```
JSON используется в:
- Веб API (99% API используют JSON)
- Конфигурационные файлы
- Мобильные приложения
- Микросервисы
- Облако (AWS, Google Cloud)
```

### Основные типы в JSON:

```json
{
  "string": "текст",
  "number": 42,
  "float": 3.14,
  "boolean": true,
  "null": null,
  "array": [1, 2, 3],
  "object": {"nested": "value"}
}
```

### Пример: API ответ

```json
{
  "status": "success",
  "data": {
    "users": [
      {"id": 1, "name": "Иван", "age": 25},
      {"id": 2, "name": "Мария", "age": 23}
    ],
    "total": 2
  }
}
```

### Почему "перекладывают с места на место"?

Потому что JSON — универсальный формат преобразования между системами:

```
Веб-сервер
    ↓ (JSON)
Мобильное приложение
    ↓ (JSON)
Облако (AWS)
    ↓ (JSON)
Аналитика
    ↓ (JSON)
Другой сервис
```

Все общаются через JSON! 🔄

---

## XML: Старший брат JSON

### Что такое XML?

**XML** (Extensible Markup Language) — старый формат, похож на HTML.

```xml
<?xml version="1.0"?>
<users>
  <user>
    <id>1</id>
    <name>Иван</name>
    <age>25</age>
  </user>
  <user>
    <id>2</id>
    <name>Мария</name>
    <age>23</age>
  </user>
</users>
```

### XML vs JSON:

| XML | JSON |
|-----|------|
| Много текста | Компактнее |
| Теги везде | Минимум символов |
| Старый (2000) | Новый (2006) |
| Отчёты, SOAP | API, конфиги |
| Сложнее парсить | Проще парсить |

**Вердикт:** JSON выиграл! JSON везде, XML только в наследии. 🏆

---

## Бинарные данные: Суть и ненависть

### Что такое бинарные данные?

Это данные в виде **байтов (0 и 1)**, которые **компьютер понимает**, но **человек не читает**.

### Примеры бинарных форматов:

```
.jpg   ← фотография (бинарный)
.mp3   ← музыка (бинарный)
.exe   ← программа (бинарный)
.pdf   ← документ (бинарный)
.db    ← база данных (бинарный)
```

### Как это выглядит в редакторе:

```
Текстовый файл:
"Hello, World!"

Бинарный файл:
╫╧║ ║®╔ ╧║╕╫ ║╔┴ ║╧ ╔║╒
(абракадабра для человека)
```

### Почему программисты их любят?

✅ **Компактные** — занимают мало места
✅ **Быстрые** — легко обработать
✅ **Безопасные** — нельзя случайно испортить
✅ **Эффективные** — можно сохранить всё

### Почему люди их ненавидят?

❌ **Невидимые** — не можешь открыть блокнотом
❌ **Неразборчивые** — не понять что внутри
❌ **Нечитаемые** — нужен специальный софт
❌ **Непрямые** — нельзя отредактировать вручную

```
Спас: используй текстовые форматы (JSON, CSV, XML)
когда можешь, а бинарные (JPG, MP3, PDF)
когда нужна скорость и размер.
```

---

## CRUD: Основные операции над данными

### Что такое CRUD?

**CRUD** — это аббревиатура четырёх основных операций:

**C**reate (Создать)
```python
# Создать новую запись
user = User(name="Иван", age=25)
user.save()  # сохранить в БД
```

**R**ead (Прочитать)
```python
# Прочитать запись из БД
user = User.get(id=1)
print(user.name)  # Иван
```

**U**pdate (Обновить)
```python
# Обновить существующую запись
user = User.get(id=1)
user.age = 26
user.save()
```

**D**elete (Удалить)
```python
# Удалить запись
user = User.get(id=1)
user.delete()
```

### Почему CRUD повсюду?

Потому что **это основа всех операций!**

```
Facebook: Create пост → Read в ленте → Update лайки → Delete пост
Gmail: Create письмо → Read входящие → Update статус → Delete письмо
Spotify: Create плейлист → Read песни → Update позицию → Delete трек
Instagram: Create фото → Read ленту → Update лайки → Delete фото

ВСЮДУ CRUD!!! 🔄
```

### CRUD в SQL (базы данных):

```sql
-- Create (INSERT)
INSERT INTO users (name, age) VALUES ('Иван', 25);

-- Read (SELECT)
SELECT * FROM users WHERE id = 1;

-- Update (UPDATE)
UPDATE users SET age = 26 WHERE id = 1;

-- Delete (DELETE)
DELETE FROM users WHERE id = 1;
```

---

## Объёмы данных: Когда начинается Big Data?

### Шкала объёмов:

```
Маленькие данные       (КБ - МБ)      → 1 файл, локально
Средние данные        (ГБ)            → несколько файлов, на диске
Большие данные        (ТБ)            → БД, облако
Очень большие данные  (ПБ+)           → Big Data (Hadoop, Spark)
```

### Примеры:

```
Маленькие:
- Контакты в телефоне (КБ)
- Фотография (5 МБ)

Средние:
- Фильм в HD (2 ГБ)
- Голос на всей неделе (1 ГБ)

Большие:
- Данные одной компании (100 ГБ - 10 ТБ)
- Логи веб-сервера (10 ТБ)

Big Data:
- Вся история Google (в пета и экзабайтах!)
- Социальная сеть с млн пользователей
- Метеорологические спутники (ПБ в день!)
```

### Когда нужен Big Data?

Когда количество данных **не помещается в память одного компьютера**:

```
1 компьютер: обработать 10 ГБ = быстро
1 компьютер: обработать 10 ТБ = ОЧЕНЬ медленно (дни!)
10 компьютеров + Hadoop: обработать 10 ТБ = быстро (часы!)
```

**Big Data = разделённая обработка на множестве машин** 🔀

---

## Базы данных: На чём держится мир

### Что такое БД?

**База данных (БД)** — это **структурированное хранилище больших объёмов данных**.

```
Файл JSON (маленький):
{
  "users": [
    {"id": 1, "name": "Иван", "age": 25},
    {"id": 2, "name": "Мария", "age": 23}
  ]
}
← работает для 100 пользователей

База данных (большая):
Table: users (1,000,000 записей)
↓ индексирование, кэширование, оптимизация
Быстро найти нужного пользователя за миллисекунды
← работает для миллиардов!
```

### Зачем нужны БД?

✅ **Быстрый поиск** — индексы ускоряют в 1000х раз
✅ **Надёжность** — backup, восстановление
✅ **Масштабируемость** — миллиарды записей
✅ **Гибкость** — запросы (SQL)
✅ **Синхронизация** — несколько пользователей одновременно

### Примеры БД везде:

```
Facebook      → миллиард пользователей в одной БД
Google        → экзабайты данных в множестве БД
Банки         → каждый рубль = запись в БД
Яндекс.Карты  → каждая улица, магазин, точка = БД
Netflix       → каждый фильм, рейтинг = БД
```

### Без БД мир остановится!

```
Если упадёт БД:
- Люди не могут войти в соцсеть
- Банки не могут перевести деньги
- Интернет-магазины не могут принять заказ
- Всё падает! 💥
```

**Вот почему от БД зависит весь мир!** 🌍

---

## Data Science: Искусство извлекать золото

### Что такое Data Science?

**Data Science** — это процесс **превращения сырых данных в ценную информацию**.

```
Сырые данные
    ↓ (очистка, подготовка)
Чистые данные
    ↓ (анализ, статистика)
Инсайты (открытия)
    ↓ (машинное обучение)
Предсказания
    ↓
Деньги! 💰
```

### Примеры:

**Netflix (что смотрят люди?)**
```
Данные: что кто смотрит, когда пауза, когда выключает
↓ анализ
Инсайт: люди смотрят сериалы с 20 по 23 часа
↓ рекомендация
Действие: выпускать серии в 19:00 (готовы к просмотру!)
```

**Amazon (что покупать?)**
```
Данные: история покупок, время, категория, отзывы
↓ анализ
Инсайт: кто покупал X, тот обычно покупает Y
↓ рекомендация
Действие: показать "похожие товары"
```

**Яндекс (куда ехать?)**
```
Данные: GPS треки, текущее время, день недели
↓ анализ
Инсайт: в 9 утра понедельника на МКАД пробки!
↓ предсказание
Действие: предупредить водителей заранее
```

### Почему Python классный для Data Science?

✅ **Удобные библиотеки** — Pandas, NumPy, Scikit-learn
✅ **Быстро прототипировать** — не нужно код переписывать
✅ **Графики** — Matplotlib, Seaborn (красиво визуализировать)
✅ **ML фреймворки** — TensorFlow, PyTorch (нейросети)
✅ **Ecosystem** — всё для анализа в одном месте

```python
import pandas as pd
import matplotlib.pyplot as plt

# Загрузить данные
data = pd.read_csv("sales.csv")

# Анализ
print(data.describe())  # статистика

# Визуализация
plt.plot(data['date'], data['sales'])
plt.show()

# Машинное обучение
from sklearn.linear_model import LinearRegression
model = LinearRegression()
model.fit(X, y)  # предсказать продажи!
```

**В 10 строк код которого в других языках 100!** 📊

---

## Можно ли оцифровать весь мир?

### Краткий ответ: ДА!

Всё можно представить в виде данных:

```
Люди → ID, имя, возраст, координаты, социальный граф
Места → GPS, название, описание, фотографии
Вещи → ID, цена, описание, категория, изображения
События → дата, место, участники, медиа
Эмоции → рейтинги, комментарии, реакции
```

### Примеры оцифровки:

**Google Maps** — весь мир в виде координат и фотографий
```
Каждый магазин = место на карте с координатами, отзывами, часами работы
```

**Wikipedia** — всё знание в виде текста и ссылок
```
Каждая статья = структурированный текст с гиперссылками
```

**Spotify** — вся музыка в виде файлов и метаданных
```
Каждая песня = MP3 + имя, исполнитель, жанр, длительность
```

**медицина** — каждый организм в виде данных
```
Генетический код (ДНК) = последовательность букв (ATCG)
```

### Где граница?

```
Технически можно оцифровать:
✅ Информацию (текст, цифры)
✅ Образы (фото, видео)
✅ Звук (музыка, речь)
✅ Поведение (GPS, клики)
✅ Генетику (ДНК последовательность)

Сложно оцифровать:
❓ Эмоции (как измерить грусть?)
❓ Сознание (что это в цифрах?)
❓ Смысл (когда становится значимым?)

Невозможно оцифровать:
❌ Опыт (только впечатления)
❌ Вкусы (только описания)
```

### Больший вопрос:

**Даже если можно оцифровать, нужно ли?**

```
Плюсы:
✅ Сохранить информацию
✅ Поделиться с миром
✅ Проанализировать
✅ Улучшить

Минусы:
❌ Приватность (геолокация везде)
❌ Контроль (кто имеет доступ?)
❌ Манипуляция (фейк-новости)
❌ Отчуждение (люди забывают быть людьми)
```

**Вывод: Можем оцифровать 99% мира, но 100% — это вопрос этики.** 🤔

---

## Вопросы для самопроверки

1. **Чем данные отличаются от информации?**

2. **Всё в компьютере — числа или нет?**

3. **Почему JSON везде?**

4. **Что такое CRUD?**

5. **Где начинается Big Data?**

6. **Почему миру нужны базы данных?**

7. **Что делает Data Scientist?**

---

## Практическое задание

### Задание: Анализ своих данных

**Требования:**
1. Создай JSON файл с 10 записями (люди, товары, или события)
2. Загрузи в Python (json.load)
3. Проанализируй данные (среднее, сумма, поиск)
4. Сохрани результаты в новый JSON (json.dump)

**Пример структуры (люди):**

```json
{
  "people": [
    {"name": "Иван", "age": 25, "city": "Москва", "salary": 100000},
    {"name": "Мария", "age": 23, "city": "СПб", "salary": 90000},
    ...
  ]
}
```

**Код для анализа:**

```python
import json

# Загрузить
with open("people.json") as f:
    data = json.load(f)

# Анализировать
people = data["people"]
avg_age = sum(p["age"] for p in people) / len(people)
total_salary = sum(p["salary"] for p in people)
moscow_people = [p for p in people if p["city"] == "Москва"]

# Результаты
results = {
    "average_age": avg_age,
    "total_salary": total_salary,
    "moscow_count": len(moscow_people),
    "moscow_people": moscow_people
}

# Сохранить
with open("results.json", "w") as f:
    json.dump(results, f, ensure_ascii=False, indent=2)

print("✓ Анализ завершён, результаты в results.json")
```

---

## 🎯 Связь со сквозным проектом

**Менеджер задач работает с данными!**

```json
{
  "tasks": [
    {
      "id": 1,
      "title": "Купить продукты",
      "description": "Молоко, яйца, хлеб",
      "priority": 1,
      "deadline": "2025-01-05",
      "status": "todo",
      "created": "2025-01-03",
      "tags": ["покупки", "срочно"]
    }
  ]
}
```

```python
# CRUD операции:

# Create (создать)
new_task = Task(title="Купить продукты")
new_task.save()  # сохраняем в JSON

# Read (прочитать)
task = Task.get(id=1)
print(task.title)

# Update (обновить)
task.status = "done"
task.save()

# Delete (удалить)
task.delete()
```

**Вот почему нужно понимать данные!** 📊

---

## Резюме: Три главных вывода

✅ **Программирование = преобразование данных** (вход → процесс → выход)

✅ **Всё в мире можно оцифровать** (текст, звук, видео, координаты)

✅ **Данные — это золото 21 века** (BigData, AI, анализ)

---

## Что дальше?

→ **4.2**: JSON и как его парсить в Python

→ **4.3**: Работа с файлами (чтение, запись, обработка)

→ **4.4**: CSV (табличные данные) и Pandas

→ **4.5**: Базы данных и SQL

→ **Потом**: Реальные данные (API, веб-скрепинг)

---

## 🌟 ГЛАВНАЯ ИДЕЯ

**Данные — это цифровое золото 21 века.** 💰

Тот кто:
- Понимает данные
- Может их обработать
- Может их интерпретировать
- Может на них заработать

**Тот понял суть современного программирования!** 🚀

---

**Поздравляем!** Ты начал модуль 4 (Данные)! 🎉

**Дальше — реальная работа с файлами, JSON, CSV и БД!** 📊✨