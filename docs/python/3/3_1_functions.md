# 3.1 Функции: Организуй свой код

## Введение: Приближаемся к мастерству

До сих пор ты:

- ✅ Прошёл условия (if/else/elif)
- ✅ Освоил циклы (for/while)
- ✅ Выучил приёмы Python (распаковка, срезы, f-строки)

Но есть проблема — ты повторяешь одинаковый код много раз:

```python
# Вычисляем среднюю оценку (первый раз)
grades_1 = [5, 4, 3]
avg_1 = sum(grades_1) / len(grades_1)
print(f"Средняя: {avg_1}")

# Вычисляем среднюю оценку (второй раз) — копи-паста!
grades_2 = [4, 4, 5]
avg_2 = sum(grades_2) / len(grades_2)
print(f"Средняя: {avg_2}")

# Вычисляем среднюю оценку (третий раз) — копи-паста!
# ...
```

Это нарушает принцип **DRY** (Don't Repeat Yourself — не повторяйся).

**Функции решают эту проблему.** Они позволяют:

- 🎯 **Не повторять код** — написал один раз, используешь много раз
- 📖 **Сделать код понятнее** — функция с хорошим названием объясняет что она делает
- 🔧 **Поддерживать код** — если нужно изменить логику, меняешь в одном месте

**В этой главе ты научишься:**

1. Писать свои функции (def)
2. Передавать параметры
3. Возвращать значения (return)
4. Использовать функции как строительные блоки

**Результат:** Твой код станет **организованнее, понятнее и короче**. 🎉

---

## Живой пример: Функция в действии

Посмотри, как функция избавляет от копи-пасты:

```python
# РАНЬШЕ: копи-паста
avg_1 = (5 + 4 + 3) / 3
print(f"Класс 1: {avg_1}")

avg_2 = (4 + 4 + 5) / 3
print(f"Класс 2: {avg_2}")

avg_3 = (5 + 5 + 5) / 3
print(f"Класс 3: {avg_3}")

# ТЕПЕРЬ: одна функция
def calculate_average(grades):
    return sum(grades) / len(grades)

print(f"Класс 1: {calculate_average([5, 4, 3])}")
print(f"Класс 2: {calculate_average([4, 4, 5])}")
print(f"Класс 3: {calculate_average([5, 5, 5])}")
```

**Видишь разницу?** С функцией **короче, понятнее и легче менять**! 🎯

---

## Теория: Что такое функция?

### Определение

**Функция** — это **именованный кусок кода**, который:

1. **Принимает входные данные** (параметры)
2. **Выполняет какие-то действия** (тело функции)
3. **Возвращает результат** (return) или выполняет побочный эффект

### Синтаксис

```python
def название_функции(параметры):
    """Документация (что делает функция)"""
    # Тело функции
    return результат
```

### Части функции

```python
def add(a, b):        # ← Объявление функции
    """Складывает два числа"""  # ← Документация
    result = a + b    # ← Тело функции
    return result     # ← Возврат результата
```

- `def` — ключевое слово (объявление функции)
- `add` — имя функции (как её вызывать)
- `a, b` — параметры (входные данные)
- `return` — оператор возврата (что вернуть)

---

## Простые функции

### Пример 1: Функция без параметров

```python
def greet():
    print("Привет, мир!")

# Вызов функции
greet()  # Привет, мир!
greet()  # Привет, мир!
```

### Пример 2: Функция с параметрами

```python
def greet_person(name):
    print(f"Привет, {name}!")

greet_person("Миша")   # Привет, Миша!
greet_person("Анна")   # Привет, Анна!
```

### Пример 3: Функция с возвращаемым значением

```python
def add(a, b):
    return a + b

result = add(5, 3)
print(result)  # 8
print(f"5 + 3 = {add(5, 3)}")  # 5 + 3 = 8
```

---

## Параметры функции

### Обязательные параметры

Параметр нужно передать, иначе будет ошибка:

```python
def multiply(x, y):
    return x * y

print(multiply(4, 5))      # 20 ✓
# print(multiply(4))       # ✗ Ошибка! Не хватает y
```

### Параметры по умолчанию

Параметр можно не передавать, и будет использовано значение по умолчанию:

```python
def power(x, exp=2):  # exp по умолчанию = 2
    return x ** exp

print(power(3))      # 9 (3 в степени 2)
print(power(3, 3))   # 27 (3 в степени 3)
print(power(3, exp=4))  # 81 (с именованным аргументом)
```

### Типы параметров (подсказки)

Можешь указать тип параметра (IDE будет помогать):

```python
def calculate_age(birth_year: int) -> int:
    """Вычисляет возраст по году рождения"""
    current_year = 2024
    return current_year - birth_year

age = calculate_age(1999)
print(f"Тебе {age} лет")  # Тебе 25 лет
```

- `birth_year: int` — параметр должен быть целое число
- `-> int` — функция возвращает целое число

---

## Возвращаемые значения

### Один результат

```python
def square(x):
    return x * x

print(square(5))  # 25
```

### Множество результатов (кортеж)

```python
def get_user_info():
    name = "Миша"
    age = 25
    city = "Москва"
    return name, age, city  # Возвращаем несколько значений

# Распаковка (помнишь из главы 2.3?)
name, age, city = get_user_info()
print(f"{name}, {age} лет, город {city}")
```

### Нет возврата

Если нет `return`, функция возвращает `None`:

```python
def log_message(message):
    print(f"LOG: {message}")
    # Нет return

result = log_message("Тест")
print(result)  # None
```

---

## Документирование функций

**Docstring** — это описание функции в кавычках:

```python
def calculate_bmi(weight: float, height: float) -> float:
    """
    Вычисляет индекс массы тела (BMI).
    
    Args:
        weight: вес в килограммах
        height: рост в метрах
    
    Returns:
        float: индекс массы тела
    """
    return weight / (height ** 2)

print(calculate_bmi(70, 1.75))  # 22.86
```

**Зачем это нужно?**

- 📖 Понимать, что делает функция
- 💡 Помогает IDE выводить подсказки
- 📚 Облегчает работу в команде

---

## Практика: Пишем функции

### Практика 1: Функция для валидации

```python
def is_valid_email(email: str) -> bool:
    """Проверяет корректность email"""
    return "@" in email and "." in email

# Используем
emails = ["user@mail.com", "invalid.email", "test@domain.org"]
for email in emails:
    if is_valid_email(email):
        print(f"✓ {email}")
    else:
        print(f"✗ {email}")
```

**Результат:**

```
✓ user@mail.com
✗ invalid.email
✓ test@domain.org
```

---

### Практика 2: Функция для обработки текста

```python
def format_name(first_name: str, last_name: str) -> str:
    """Форматирует имя в заглавные буквы"""
    return f"{first_name.capitalize()} {last_name.capitalize()}"

# Используем
names = [
    ("иван", "петров"),
    ("мария", "иванова"),
    ("александр", "смирнов")
]

for first, last in names:
    print(format_name(first, last))
```

**Результат:**

```
Иван Петров
Мария Иванова
Александр Смирнов
```

---

### Практика 3: Функция с условиями

```python
def get_grade(score: int) -> str:
    """Возвращает оценку по количеству баллов"""
    if score >= 90:
        return "A (отлично)"
    elif score >= 80:
        return "B (хорошо)"
    elif score >= 70:
        return "C (удовлетворительно)"
    else:
        return "F (неудовлетворительно)"

# Используем со списком
scores = [95, 85, 75, 65, 88]
for score in scores:
    print(f"{score} баллов → {get_grade(score)}")
```

**Результат:**

```
95 баллов → A (отлично)
85 баллов → B (хорошо)
75 баллов → C (удовлетворительно)
65 баллов → F (неудовлетворительно)
88 баллов → B (хорошо)
```

---

## Вопросы для самопроверки

1. **Что такое функция и зачем она нужна?**

2. **Какая разница между параметром и аргументом?**

3. **Как написать функцию с параметром по умолчанию?**

4. **Что означает `return` и зачем он нужен?**

5. **Как вернуть несколько значений из функции?**

---

## Практическое задание

### Задание: Калькулятор с функциями

**Что нужно:**

Создай набор функций для калькулятора:

```python
def add(a: float, b: float) -> float:
    """Сложение"""
    return a + b

def subtract(a: float, b: float) -> float:
    """Вычитание"""
    return a - b

def multiply(a: float, b: float) -> float:
    """Умножение"""
    return a * b

def divide(a: float, b: float) -> float:
    """Деление с проверкой деления на ноль"""
    if b == 0:
        return None
    return a / b

def power(base: float, exp: float = 2) -> float:
    """Возведение в степень (по умолчанию в квадрат)"""
    return base ** exp

def calculate(a: float, operation: str, b: float) -> str:
    """
    Главный калькулятор
    
    Args:
        a: первое число
        operation: операция (+, -, *, /, **)
        b: второе число
    
    Returns:
        строка с результатом
    """
    if operation == "+":
        result = add(a, b)
    elif operation == "-":
        result = subtract(a, b)
    elif operation == "*":
        result = multiply(a, b)
    elif operation == "/":
        result = divide(a, b)
        if result is None:
            return "Ошибка: деление на ноль!"
    elif operation == "**":
        result = power(a, b)
    else:
        return f"Ошибка: операция '{operation}' не найдена"
    
    return f"{a} {operation} {b} = {result}"

# Тестирование
print(calculate(10, "+", 5))   # 10 + 5 = 15
print(calculate(10, "-", 3))   # 10 - 3 = 7
print(calculate(4, "*", 5))    # 4 * 5 = 20
print(calculate(20, "/", 4))   # 20 / 4 = 5.0
print(calculate(20, "/", 0))   # Ошибка: деление на ноль!
print(calculate(2, "**", 8))   # 2 ** 8 = 256
```

**Требования:**

- Создай функции для каждой операции
- Используй подсказки типов
- Добавь docstring для каждой функции
- Обработай деление на ноль
- Протестируй функции

**Усложнение:** Добавь функцию, которая принимает список чисел и операцию:

```python
def calculate_list(numbers: list, operation: str) -> float:
    """Применить операцию ко всем числам в списке"""
    if operation == "sum":
        return sum(numbers)
    elif operation == "avg":
        return sum(numbers) / len(numbers)
    elif operation == "max":
        return max(numbers)
    elif operation == "min":
        return min(numbers)
    else:
        return None

print(calculate_list([1, 2, 3, 4, 5], "sum"))  # 15
print(calculate_list([1, 2, 3, 4, 5], "avg"))  # 3.0
```

---

## 🎯 Связь со сквозным проектом

**Менеджер задач использует функции!**

```python
def add_task(tasks: list, title: str, priority: int = 1) -> None:
    """Добавляет новую задачу в список"""
    task = {
        "id": len(tasks) + 1,
        "title": title,
        "priority": priority,
        "completed": False
    }
    tasks.append(task)

def mark_completed(tasks: list, task_id: int) -> bool:
    """Отмечает задачу как выполненную"""
    for task in tasks:
        if task["id"] == task_id:
            task["completed"] = True
            return True
    return False

def display_tasks(tasks: list) -> None:
    """Выводит все задачи"""
    for task in tasks:
        status = "✓" if task["completed"] else "○"
        print(f"{task['id']}. {status} {task['title']} (приоритет: {task['priority']})")

# Использование
my_tasks = []
add_task(my_tasks, "Купить продукты", 1)
add_task(my_tasks, "Написать отчёт", 2)
add_task(my_tasks, "Позвонить другу", 1)

display_tasks(my_tasks)  # Выводит все задачи
mark_completed(my_tasks, 1)  # Отмечает первую как выполненную
display_tasks(my_tasks)  # Снова выводит
```

---

## 💡 Важный совет: Хорошая функция

**Хорошая функция:**

- ✅ Делает **одно** — print_user, а не print_user_and_save_and_send_email
- ✅ Имеет **понятное имя** — get_user_age, а не func1
- ✅ Имеет **документацию** — docstring объясняет, что она делает
- ✅ **Не слишком длинная** — если больше 20 строк, возможно нужно разделить

**Плохая функция:**

```python
# ✗ Непонятное имя
def x(a, b, c):
    return a + b * c

# ✗ Нет документации
def calculate(data):
    result = 0
    for item in data:
        result += item[0] * item[1]
    return result

# ✗ Делает слишком много
def process_and_save(user_data):
    # обработка данных
    # сохранение в БД
    # отправка письма
    # логирование
    pass
```

**Хорошая функция:**

```python
# ✓ Понятное имя
def multiply_sum(a: int, b: int, c: int) -> int:
    """Вычисляет a + b * c"""
    return a + b * c

# ✓ Документация и ясность
def calculate_total_cost(items: list) -> float:
    """
    Вычисляет общую стоимость товаров.
    
    Args:
        items: список кортежей (цена, количество)
    
    Returns:
        float: общая стоимость
    """
    total = 0
    for price, quantity in items:
        total += price * quantity
    return total

# ✓ Делает одно
def save_user(user_data: dict) -> bool:
    """Сохраняет данные пользователя в БД"""
    # только сохранение
    pass
```

---

## Резюме: Три главных вывода

✅ **Функция нужна, чтобы не повторять код** — DRY принцип.

✅ **Функция имеет параметры и возвращает значения** — это её интерфейс.

✅ **Хорошая функция делает одно и имеет понятное имя** — это залог читаемости.

---

## Что дальше?

→ **Глава 3.2** (Области видимости): Как переменные видят друг друга

→ **Глава 3.3** (Рекурсия): Как функция вызывает сама себя

→ **Глава 3.4** (Модули): Как делить код на файлы

---

## 🌟 ПОЗДРАВЛЯЕМ!

Ты начал **Модуль 3: Организация кода** и уже знаешь **функции**! 🎉

Функции — это:

- 🎯 Строительные блоки больших программ
- 📖 Ключ к читаемому коду
- 🔧 Инструмент переиспользования кода

**Следующая глава будет ещё интереснее!** 🚀✨