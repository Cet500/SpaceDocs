# 3.3 Организация кода в файлы: Разделяй и властвуй

## Введение: Когда код становится горой

Представь, ты пишешь менеджер задач. В начале это просто:

```python
# main.py — весь код в одном файле
class Task:
    def __init__(self, title):
        self.title = title
        self.completed = False

class User:
    def __init__(self, name):
        self.name = name
        self.tasks = []

# Функции
def add_task(user, title):
    task = Task(title)
    user.tasks.append(task)

def save_to_file(user):
    with open("data.txt", "w") as f:
        for task in user.tasks:
            f.write(task.title + "\n")

def load_from_file(user):
    with open("data.txt", "r") as f:
        for line in f:
            add_task(user, line.strip())

def validate_email(email):
    return "@" in email

# Главная программа
if __name__ == "__main__":
    user = User("Иван")
    add_task(user, "Купить продукты")
    save_to_file(user)
```

**Проблемы:**

- 📁 Всё в одном файле — 200+ строк в одном месте
- 🔀 Сложно найти нужный код
- 🐛 Если сломается что-то в классах, может повредиться и сохранение
- 😵 Новому разработчику нужно читать весь файл, чтобы понять что-то
- ♻️ Сложно переиспользовать код в других проектах

**Решение:** Раздели код на логические файлы!

```
project/
├── main.py              # Главная программа
├── models.py            # Классы Task и User
├── file_handler.py      # Работа с файлами
└── utils.py             # Вспомогательные функции
```

**В этой главе:**

1. **Зачем делить на файлы?**
2. **Как понять, когда делить?**
3. **Как организовать структуру проекта?**
4. **Стратегия "разделяй и властвуй"**
5. **Баланс между делением и избытком файлов**

**Результат:** Твой код станет **понятнее, проще тестировать и переиспользовать**. 🎯

---

## Живой пример: Одна гора vs правильная структура

### ❌ ВСЁ В ОДНОМ ФАЙЛЕ (250 строк в main.py)

```python
# main.py
class Task: ...          # 20 строк
class User: ...          # 15 строк
class Database: ...      # 50 строк
class EmailValidator: .. # 30 строк
def save_task(): ...     # 20 строк
def load_task(): ...     # 20 строк
def send_email(): ...    # 30 строк
def generate_report(): .. # 15 строк
if __name__ == "__main__":  # 50 строк
```

**Проблемы:**

- 😵 Невозможно найти что-либо быстро
- 🔗 Всё связано со всем
- 🧪 Нельзя тестировать отдельно
- 👥 Новичок не поймёт структуру

### ✅ РАЗДЕЛЕНО НА ФАЙЛЫ (по логике)

```
project/
├── main.py              # 50 строк (главная программа)
├── models.py            # 35 строк (Task, User)
├── database.py          # 50 строк (работа с БД)
├── validators.py        # 30 строк (валидация)
├── email_service.py     # 30 строк (отправка писем)
└── reports.py           # 15 строк (отчёты)
```

**Преимущества:**

- ✅ Легко найти нужный код
- ✅ Каждый файл отвечает за одно
- ✅ Можно тестировать отдельно
- ✅ Новичок сразу видит структуру

---

## Когда делить на файлы? Правила

### Правило 1: По смыслу (логике)

**Делить, если:**

- 📦 Код отвечает за разные функции (данные, сохранение, валидация)
- 👥 Разные разработчики могут работать на разными файлами
- 🔄 Код можно переиспользовать в других проектах

**Не делить, если:**

- 📄 Файл меньше 100 строк
- 🔗 Код очень сильно связан
- 🚀 Это простой скрипт на одноразовое использование

### Правило 2: Один файл = одна ответственность

```python
# ✅ ХОРОШО
# validators.py — только валидация
def validate_email(email): ...
def validate_password(password): ...
def validate_username(username): ...

# ❌ ПЛОХО
# validators.py — много всего
def validate_email(email): ...
def save_to_database(user): ...
def send_email(to, text): ...
def generate_report(): ...
```

### Правило 3: Логируй связи

```python
# ✅ ХОРОШО (видно что зависит от чего)
# main.py
from models import Task, User
from database import save_task
from validators import validate_email

# ❌ ПЛОХО (запутанные зависимости)
# main.py
from validators import validate_email
from validators import save_task  # Это не валидатор!
from validators import database   # Это не валидатор!
```

---

## Стратегия "Разделяй и властвуй"

### Шаг 1: Определи категории кода

Раздели код на категории:

1. **Данные** (классы, структуры) → `models.py`
2. **Логика работы** (функции) → `logic.py` или `handlers.py`
3. **Сохранение данных** (файлы, БД) → `database.py`
4. **Валидация** (проверки) → `validators.py`
5. **Главная программа** (старт) → `main.py`

### Шаг 2: Распредели код

```python
# models.py — структуры данных
class Task:
    def __init__(self, title, priority=1):
        self.title = title
        self.priority = priority
        self.completed = False

class User:
    def __init__(self, name):
        self.name = name
        self.tasks = []

# validators.py — проверки
def validate_task_title(title: str) -> bool:
    return len(title) > 0 and len(title) < 100

def validate_priority(priority: int) -> bool:
    return 1 <= priority <= 5

# database.py — работа с данными
def save_tasks(user, filename="tasks.json"):
    import json
    data = {
        "user": user.name,
        "tasks": [
            {"title": t.title, "completed": t.completed}
            for t in user.tasks
        ]
    }
    with open(filename, "w") as f:
        json.dump(data, f)

def load_tasks(user, filename="tasks.json"):
    import json
    with open(filename, "r") as f:
        data = json.load(f)
    for task_data in data["tasks"]:
        user.tasks.append(
            Task(task_data["title"])
        )

# main.py — главная программа
from models import Task, User
from validators import validate_task_title, validate_priority
from database import save_tasks, load_tasks

def main():
    user = User("Иван")
    
    # Добавляем задачу
    title = "Купить продукты"
    if validate_task_title(title):
        task = Task(title)
        user.tasks.append(task)
    
    # Сохраняем
    save_tasks(user)
    
    # Загружаем
    new_user = User("Анна")
    load_tasks(new_user)

if __name__ == "__main__":
    main()
```

### Шаг 3: Структура проекта

```
task_manager/
├── main.py              # Точка входа
├── models.py            # Task, User классы
├── validators.py        # Проверки
├── database.py          # Сохранение/загрузка
└── README.md            # Документация
```

---

## Баланс: Не переусложняй!

### ❌ СЛИШКОМ МНОГО ФАЙЛОВ (перебор)

```
project/
├── main.py
├── models/
│   ├── task.py
│   ├── user.py
│   ├── priority.py       # Слишком мелко!
│   └── date.py           # Слишком мелко!
├── validators/
│   ├── email.py
│   ├── password.py       # Отдельный файл на 10 строк?
│   └── username.py       # Отдельный файл на 5 строк?
├── database/
│   ├── connection.py
│   ├── queries.py
│   └── migrations.py     # Для маленького проекта нет смысла
```

**Проблемы:**

- 😵 Много файлов для маленького проекта
- 🔀 Сложно ориентироваться
- 📖 Много импортов во всё
- 🐢 Медленнее работает

### ✅ ПРАВИЛЬНЫЙ БАЛАНС

```
project/
├── main.py              # ~50 строк (главная программа)
├── models.py            # ~50 строк (Task, User)
├── validators.py        # ~40 строк (все проверки)
└── database.py          # ~60 строк (сохранение/загрузка)
```

**Правило:** 1 файл = 50-200 строк кода (в зависимости от сложности).

---

## Как понять, когда делить, а когда дописывать?

### Признак, что ПОРА ДЕЛИТЬ файл:

✅ **Файл больше 200 строк** → раздели на логические части
✅ **Несколько разработчиков работают на одном файле** → раздели ответственность
✅ **Код сложно находить** → раздели по категориям
✅ **Несвязанные функции в одном файле** → раздели по смыслу

```python
# ❌ Файл слишком большой (250 строк)
# all_code.py
class Task: ...      # 30 строк
class User: ...      # 40 строк
def validate(...): .. # 50 строк
def save_file(...): . # 50 строк
def load_file(...): . # 50 строк

# ✅ Разделилось на логику
# models.py (70 строк): Task, User
# validators.py (50 строк): валидация
# database.py (100 строк): сохранение
```

### Признак, что РАНО ДЕЛИТЬ файл:

❌ **Файл меньше 50 строк** → оставь в одном
❌ **Код очень взаимосвязан** → оставь рядом
❌ **Проект совсем маленький** → не спеши с архитектурой

```python
# ✅ Рано делить (всего 80 строк)
# game.py
class Player: ...      # 25 строк
class Enemy: ...       # 20 строк
def start_game(): ...  # 35 строк

# ❌ Не делили бы, если было всего 80 строк
```

---

## Практика: Реальный пример

### Начало: Один файл (100 строк)

```python
# calculator.py
class Calculator:
    def add(self, a, b):
        return a + b
    
    def subtract(self, a, b):
        return a - b
    
    def multiply(self, a, b):
        return a * b
    
    def divide(self, a, b):
        if b == 0:
            raise ValueError("Деление на ноль")
        return a / b

def validate_number(value):
    try:
        return float(value)
    except ValueError:
        return None

def log_operation(operation, a, b, result):
    with open("log.txt", "a") as f:
        f.write(f"{operation}: {a} {b} = {result}\n")

def main():
    calc = Calculator()
    
    a_str = input("Первое число: ")
    a = validate_number(a_str)
    if a is None:
        print("Неверный ввод")
        return
    
    op = input("Операция (+, -, *, /): ")
    
    b_str = input("Второе число: ")
    b = validate_number(b_str)
    if b is None:
        print("Неверный ввод")
        return
    
    if op == "+":
        result = calc.add(a, b)
    elif op == "-":
        result = calc.subtract(a, b)
    elif op == "*":
        result = calc.multiply(a, b)
    elif op == "/":
        result = calc.divide(a, b)
    else:
        print("Неверная операция")
        return
    
    print(f"Результат: {result}")
    log_operation(op, a, b, result)

if __name__ == "__main__":
    main()
```

### Растёт: Нужно делить (150+ строк)

Делим на:

- `models.py` — класс Calculator
- `validators.py` — валидация
- `logger.py` — логирование
- `main.py` — главная программа

```
# models.py
class Calculator:
    def add(self, a, b): return a + b
    def subtract(self, a, b): return a - b
    def multiply(self, a, b): return a * b
    def divide(self, a, b):
        if b == 0: raise ValueError("Деление на ноль")
        return a / b

# validators.py
def validate_number(value):
    try:
        return float(value)
    except ValueError:
        return None

# logger.py
def log_operation(operation, a, b, result):
    with open("log.txt", "a") as f:
        f.write(f"{operation}: {a} {b} = {result}\n")

# main.py
from models import Calculator
from validators import validate_number
from logger import log_operation

def main():
    calc = Calculator()
    # ... остальной код
```

---

## Чек-лист: Готов ли твой проект к делению?

**Делить, если:**

- [ ] Файл больше 150 строк
- [ ] Несколько разработчиков
- [ ] Разные функции в одном месте
- [ ] Есть логические группы кода

**Не делить, если:**

- [ ] Файл меньше 80 строк
- [ ] Один разработчик
- [ ] Весь код связан
- [ ] Это одноразовый скрипт

---

## Вопросы для самопроверки

1. **Почему плохо писать всё в одном файле?**

2. **Как понять, когда начинать делить файлы?**

3. **Один файл = одна... что?**

4. **Какой оптимальный размер файла?**

5. **Что хуже: 1 большой файл или 20 маленьких?**

---

## Практическое задание

### Задание: Реорганизуй проект

**У тебя есть один большой файл (200+ строк):**

```python
# school_system.py (200 строк — слишком много!)
class Student: ...
class Teacher: ...
class Grade: ...
class Course: ...
def validate_email(): ...
def validate_grade(): ...
def save_to_file(): ...
def load_from_file(): ...
def generate_report(): ...
def main(): ...
```

**Разделить на:**

```
school/
├── main.py              # главная программа
├── models.py            # Student, Teacher, Course классы
├── validators.py        # валидация email и оценок
├── database.py          # сохранение и загрузка
└── reports.py           # генерирование отчётов
```

**Требования:**

1. Определи категории кода
2. Раздели на файлы по смыслу
3. Напиши импорты в main.py
4. Убедись, что каждый файл отвечает за одно

```python
# models.py
class Student:
    def __init__(self, name):
        self.name = name

class Teacher:
    def __init__(self, name):
        self.name = name

# validators.py
def validate_email(email: str) -> bool:
    return "@" in email

# database.py
def save_students(students, filename="students.json"):
    pass

# reports.py
def generate_student_report(student):
    pass

# main.py
from models import Student, Teacher
from validators import validate_email
from database import save_students
from reports import generate_student_report

def main():
    student = Student("Иван")
    if validate_email("ivan@mail.com"):
        save_students([student])
        print(generate_student_report(student))

if __name__ == "__main__":
    main()
```

---

## 🎯 Связь со сквозным проектом

**Менеджер задач растёт и требует деления!**

```
task_manager/
├── main.py              # главная программа
├── models.py            # Task, User классы
├── validators.py        # проверки задач
├── database.py          # сохранение в JSON
└── ui.py                # интерфейс (будущее!)
```

**На следующем этапе (модуль 4) добавим:**

- Работа с файлами
- JSON/CSV формат
- Реальная база данных

---

## 💡 Важный совет: Не переусложняй архитектуру

**На начальном этапе:**

```
project/
├── main.py
└── utils.py (если нужно)
```

**Когда растёт:**

```
project/
├── main.py
├── models.py
└── database.py
```

**Когда становится большим:**

```
project/
├── main.py
├── models/
│   ├── __init__.py
│   └── task.py
├── services/
│   ├── __init__.py
│   └── database.py
└── utils/
    ├── __init__.py
    └── validators.py
```

**Но не сразу!** Сначала 3 файла, потом папки. 📈

---

## Резюме: Три главных вывода

✅ **Разделяй по смыслу** — один файл = одна ответственность

✅ **Не спеши с архитектурой** — сначала 3-5 файлов, потом папки

✅ **Баланс** — не 1 большой файл, но и не 20 маленьких

---

## Что дальше?

→ **Модуль 4** (Работа с данными): Файлы, JSON, CSV

→ **Модуль 5** (Продвинутое): Пакеты и __init__.py

---

## 🌟 ГЛАВНАЯ ИДЕЯ

**"Разделяй и властвуй" = порядок в коде** 📁

Небольшой проект в одном файле? OK.
Растёт? Раздели на 3-5 файлов.
Становится большим? Потом добавь папки.

Не усложняй сразу. Расти постепенно. 🌱

---

**Поздравляем!** Ты теперь знаешь, как организовывать код в файлы! 🎉

**Это важный навык профессионального разработчика.** 👨‍💻✨