# 3.5 Стандартная библиотека Python: Не изобретай велосипед

## Введение: Python поставляется с подарками

До сих пор ты писал код **с нуля**:

```python
# Ты писал сам:
def validate_email(email):
    return "@" in email

def log_message(msg):
    print(f"[LOG] {msg}")

def read_file(filename):
    with open(filename, "r") as f:
        return f.read()
```

**Но Python уже всё это имеет!** 🎁

```python
# Python уже есть:
import re              # регулярные выражения
import logging         # логирование
import json            # работа с JSON
import os              # работа с ОС
import random          # случайные числа
import time            # время
import turtle          # рисование! 🐢
```

**Почему же ты это не знал?**

Потому что сначала нужно было **научиться программировать**, а не просто копировать готовое!

Теперь ты готов. **Встречайте стандартную библиотеку Python!** 🎉

**В этой главе:**

1. Что такое пакеты и библиотеки
2. Почему "стандартная"
3. Что входит в поставку Python
4. Самые полезные модули
5. Turtle (забавное рисование!)
6. Когда изобретать велосипед, а когда нет

**Результат:** Ты будешь писать **в 10 раз меньше кода**, потому что будешь переиспользовать готовое! 🚀

---

## Живой пример: С библиотекой vs без

### ❌ БЕЗ библиотеки (200+ строк)

```python
# Сам пишешь всё:
def read_json_file(filename):
    with open(filename, "r") as f:
        content = f.read()
    # парсинг JSON вручную (!)
    # ...много кода...

def validate_email(email):
    # Регулярное выражение вручную
    # ...много кода...

def get_current_time():
    # Работа с временем вручную
    # ...много кода...

def get_random_number(a, b):
    # Генератор случайных чисел вручную
    # ...много кода...
```

### ✅ С библиотекой (5 строк!)

```python
import json
import re
import time
import random

# Всё готово!
data = json.load(open("data.json"))
is_valid = re.match(r"[\w\.-]+@[\w\.-]+\.\w+", email)
current_time = time.time()
random_number = random.randint(a, b)
```

**Видишь разницу?** От 200+ строк к 5 строкам! ✨

---

## Что такое пакеты и библиотеки?

### Определения (просто!)

**Модуль** — это один `.py` файл:

```python
# utils.py — модуль
def validate_email(email):
    return "@" in email
```

**Пакет** — это папка с модулями:

```
utils/              # Пакет
├── __init__.py
├── validators.py
└── logger.py
```

**Библиотека** — это набор пакетов для одной цели:

```
requests/           # Библиотека для HTTP
├── __init__.py
├── api.py
├── auth.py
└── utils.py
```

**Стандартная библиотека** — это встроенные пакеты Python:

```python
# Всё это приходит с Python "из коробки"
import os           # работа с ОС
import json         # JSON
import random       # случайность
import time         # время
```

### Почему "стандартная"?

Потому что она:

- ✅ **Встроена в Python** (не нужно устанавливать)
- ✅ **Стабильна** (не меняется между версиями)
- ✅ **Универсальна** (работает везде)
- ✅ **Быстра** (написана на C)

---

## Что входит в поставку Python?

Python приходит с **200+ встроенными модулями!**

### Основные категории:

**Работа с файлами и системой:**

- `os` — работа с операционной системой
- `sys` — параметры интерпретатора
- `pathlib` — работа с путями
- `shutil` — копирование, удаление файлов

**Работа с данными:**

- `json` — JSON парсинг
- `csv` — CSV таблицы
- `pickle` — сериализация Python объектов
- `sqlite3` — встроенная база данных

**Математика и числа:**

- `math` — математические функции
- `random` — случайные числа
- `statistics` — статистика
- `decimal` — точная арифметика

**Время и дата:**

- `time` — время
- `datetime` — даты и время
- `calendar` — календарь

**Текст и парсинг:**

- `re` — регулярные выражения
- `string` — константы строк
- `textwrap` — работа с текстом

**Интернет:**

- `urllib` — HTTP запросы
- `email` — парсинг email
- `http` — HTTP протокол

**Отладка:**

- `logging` — логирование
- `pdb` — отладчик
- `traceback` — трассировка ошибок

**Развлечение:**

- `turtle` — графика и рисование 🐢
- `tkinter` — GUI приложения

---

## Самые полезные модули (которые ты будешь использовать часто)

### 1. `os` — работа с ОС и файлами

```python
import os

# Текущая директория
print(os.getcwd())  # /Users/ivan/projects

# Список файлов
files = os.listdir(".")
print(files)  # ['main.py', 'models.py', ...]

# Проверить, существует ли файл
if os.path.exists("data.json"):
    print("Файл есть")

# Удалить файл
os.remove("temp.txt")

# Переименовать файл
os.rename("old.txt", "new.txt")
```

### 2. `json` — JSON парсинг (частая работа!)

```python
import json

# Загрузить JSON из файла
with open("data.json", "r") as f:
    data = json.load(f)
    print(data)  # {'name': 'Иван', 'age': 25}

# Сохранить Python объект в JSON
user = {"name": "Мария", "age": 23}
with open("user.json", "w") as f:
    json.dump(user, f)

# Парсить JSON из строки
json_string = '{"key": "value"}'
parsed = json.loads(json_string)
print(parsed)  # {'key': 'value'}

# Конвертировать в JSON строку
python_dict = {"name": "Иван"}
json_string = json.dumps(python_dict)
print(json_string)  # '{"name": "Иван"}'
```

### 3. `random` — случайные числа (для игр, симуляций)

```python
import random

# Случайное число от a до b (включительно)
num = random.randint(1, 100)
print(num)  # 42 (каждый раз разное)

# Случайное число от 0.0 до 1.0
decimal = random.random()
print(decimal)  # 0.375

# Выбрать случайный элемент из списка
colors = ["красный", "зелёный", "синий"]
color = random.choice(colors)
print(color)  # "зелёный"

# Перемешать список
cards = ["♠", "♣", "♥", "♦"]
random.shuffle(cards)
print(cards)  # ['♥', '♠', '♦', '♣']
```

### 4. `time` и `datetime` — время (частая работа!)

```python
import time
from datetime import datetime, timedelta

# Текущее время (timestamp)
now = time.time()
print(now)  # 1735572000.123

# Текущие дата и время (удобнее!)
now = datetime.now()
print(now)  # 2024-12-30 16:20:15.123456

# Форматирование даты
formatted = now.strftime("%d.%m.%Y")
print(formatted)  # 30.12.2024

# Работа с датами
today = datetime.now()
tomorrow = today + timedelta(days=1)
print(tomorrow)  # 2024-12-31 16:20:15.123456

# Разница между датами
date1 = datetime(2024, 1, 1)
date2 = datetime(2024, 12, 31)
diff = date2 - date1
print(diff.days)  # 364

# Задержка (sleep)
print("Жди 2 секунды...")
time.sleep(2)
print("Готово!")
```

### 5. `math` — математика

```python
import math

# Константы
print(math.pi)      # 3.14159...
print(math.e)       # 2.71828...

# Функции
print(math.sqrt(16))     # 4.0
print(math.pow(2, 3))    # 8.0
print(math.floor(3.7))   # 3 (вниз)
print(math.ceil(3.2))    # 4 (вверх)
print(math.fabs(-5))     # 5.0 (модуль)

# Тригонометрия
print(math.sin(math.pi / 2))  # 1.0
print(math.cos(0))            # 1.0
```

### 6. `re` — регулярные выражения (для парсинга!)

```python
import re

# Найти email в тексте
text = "Напиши на ivan@mail.com или anna@gmail.com"
emails = re.findall(r"[\w\.-]+@[\w\.-]+\.\w+", text)
print(emails)  # ['ivan@mail.com', 'anna@gmail.com']

# Проверить, соответствует ли формату
email = "test@mail.com"
if re.match(r"^[\w\.-]+@[\w\.-]+\.\w+$", email):
    print("✓ Верный email")

# Заменить по шаблону
text = "Телефон: 89991234567"
text = re.sub(r"(\d{1})(\d{3})(\d{3})(\d{2})(\d{2})", 
              r"+\1 (\2) \3-\4-\5", text)
print(text)  # +8 (999) 123-45-67
```

### 7. `logging` — логирование (вместо print!)

```python
import logging

# Настройка логирования
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(levelname)s - %(message)s"
)

# Логирование
logging.debug("Это дебаг")        # не показывается
logging.info("Это инфо")          # показывается
logging.warning("Это предупреждение")
logging.error("Это ошибка")

# Результат:
# 2024-12-30 16:20:15 - INFO - Это инфо
# 2024-12-30 16:20:15 - WARNING - Это предупреждение
# 2024-12-30 16:20:15 - ERROR - Это ошибка
```

### 8. `csv` — работа с CSV таблицами

```python
import csv

# Чтение CSV
with open("data.csv", "r") as f:
    reader = csv.reader(f)
    for row in reader:
        print(row)  # ['Иван', '25', 'разработчик']

# Чтение как словари (с заголовками)
with open("data.csv", "r") as f:
    reader = csv.DictReader(f)
    for row in reader:
        print(row)  # {'name': 'Иван', 'age': '25', 'job': 'разработчик'}

# Запись CSV
data = [
    ["Имя", "Возраст"],
    ["Иван", 25],
    ["Мария", 23]
]
with open("output.csv", "w") as f:
    writer = csv.writer(f)
    writer.writerows(data)
```

---

## Turtle — рисование! 🐢

После бесконечной консоли, вот что-то визуальное:

### Простой квадрат

```python
import turtle

# Создать окно
window = turtle.Screen()
window.title("Мой первый рисунок")

# Создать черепаху
t = turtle.Turtle()
t.speed(1)  # скорость рисования

# Нарисовать квадрат
for i in range(4):
    t.forward(100)  # вперёд на 100
    t.right(90)     # повернуть на 90 градусов

# Закончить
window.done()
```

**Результат:** Маленькое окно с нарисованным квадратом! 📦

### Звезда

```python
import turtle

t = turtle.Turtle()
t.speed(0)

# Звезда
for i in range(5):
    t.forward(100)
    t.right(144)  # угол звезды

turtle.done()
```

**Результат:** Красивая звезда! ⭐

### Спираль

```python
import turtle

t = turtle.Turtle()
t.speed(0)

# Спираль
for i in range(100):
    t.forward(i * 2)
    t.right(45)

turtle.done()
```

**Результат:** Красивая спираль! 🌀

### Много рандомных линий

```python
import turtle
import random

window = turtle.Screen()
t = turtle.Turtle()
t.speed(0)

colors = ["red", "blue", "green", "yellow", "purple"]

for i in range(50):
    t.pencolor(random.choice(colors))
    t.forward(random.randint(10, 100))
    t.right(random.randint(10, 180))

window.done()
```

**Результат:** Красивый хаос! 🎨

---

## Что если использовать только внешние пакеты?

### ❌ Плохая идея

```python
# Использовать пакет для всего
import some_library

# Все функции из пакета
data = some_library.read_file("data.txt")
some_library.process(data)
some_library.save(data)
some_library.send_email(data)
```

**Проблемы:**

- 😵 Ты не учишься программировать
- 🐛 Если пакет сломан, ты растерян
- 📦 Зависимость от чужого кода
- ⚡ Медленнее, чем оптимизированный код

### ✅ Хороший баланс

```python
# Используй встроенное для логики
# Используй пакеты для сложного
import json
from datetime import datetime

# Основная логика сам
data = json.load(open("data.json"))
for item in data:
    if item["date"] > datetime.now():
        print(item)
```

---

## Когда изобретать велосипед, а когда нет?

### ✅ ИСПОЛЬЗУЙ ВСТРОЕННОЕ, если:

- 📦 Есть в стандартной библиотеке
- ⚡ Встроенное быстрее
- 🔒 Встроенное надёжнее
- 📚 Ты разбираешься в технологии

```python
# ✅ ИСПОЛЬЗУЙ встроенное
import json
data = json.load(open("data.json"))

# ❌ НЕ ПИШИ сам JSON парсер!
# Это долго и сложно
```

### ✅ ИСПОЛЬЗУЙ ВНЕШНИЕ ПАКЕТЫ, если:

- 🔧 Сложная функциональность
- 🌐 Интеграция с сервисами
- 📊 Специализированная работа

```python
# ✅ Используй для сложного
import requests
response = requests.get("https://api.example.com")

# ❌ НЕ ПИШИ сам HTTP клиент!
# Слишком сложно
```

### ✅ ПИШИ САМ, если:

- 🎓 Это учебная задача
- 🎯 Уникальная логика твоего проекта
- 🔍 Нужна максимальная производительность

```python
# ✅ ПИШИ сам
def validate_task_title(title):
    return len(title) > 0 and len(title) < 100

# Это уникально для твоего проекта!
```

---

## Практика: Используем встроенное

### Практика 1: Работа с JSON и датами

```python
import json
from datetime import datetime

# Загруженные задачи
tasks = [
    {"title": "Купить продукты", "deadline": "2024-12-31"},
    {"title": "Написать отчёт", "deadline": "2024-12-25"}
]

# Сохранить в JSON
with open("tasks.json", "w") as f:
    json.dump(tasks, f)

# Загрузить из JSON
with open("tasks.json", "r") as f:
    loaded_tasks = json.load(f)

# Отсортировать по deadline
for task in loaded_tasks:
    deadline = datetime.strptime(task["deadline"], "%Y-%m-%d")
    print(f"{task['title']}: {deadline.strftime('%d.%m.%Y')}")
```

### Практика 2: Случайный выбор

```python
import random

# Игра: угадай число
secret = random.randint(1, 100)
attempts = 0

while True:
    guess = int(input("Угадай число (1-100): "))
    attempts += 1
    
    if guess < secret:
        print("Больше!")
    elif guess > secret:
        print("Меньше!")
    else:
        print(f"✓ Угадал за {attempts} попыток!")
        break
```

### Практика 3: Логирование вместо print

```python
import logging

# Вместо этого:
# print("DEBUG: user logged in")
# print("ERROR: file not found")

# Пиши это:
logging.basicConfig(level=logging.INFO)

def login_user(username):
    logging.info(f"User {username} logged in")
    
def read_file(filename):
    try:
        with open(filename) as f:
            return f.read()
    except FileNotFoundError:
        logging.error(f"File {filename} not found")
        return None
```

---

## Вопросы для самопроверки

1. **Что такое стандартная библиотека?**

2. **Почему она "стандартная"?**

3. **Какой модуль использовать для работы с JSON?**

4. **Зачем turtle, если можно просто print?**

5. **Когда нужны внешние пакеты?**

---

## Практическое задание

### Задание: Приложение "Мои дела"

**Требования:**

1. Сохраняй задачи в JSON файл
2. Используй `logging` для логирования
3. Используй `datetime` для работы с датами
4. Используй `random` для выбора случайной мотивирующей фразы

```python
import json
import logging
from datetime import datetime
import random

logging.basicConfig(level=logging.INFO)

MOTIVATION = [
    "Ты можешь!",
    "Давай, это просто!",
    "Не давай сдачу!",
    "Почти готово!"
]

class TaskApp:
    def __init__(self, filename="tasks.json"):
        self.filename = filename
        self.tasks = self._load_tasks()
    
    def _load_tasks(self):
        try:
            with open(self.filename, "r") as f:
                tasks = json.load(f)
                logging.info(f"Загружено {len(tasks)} задач")
                return tasks
        except FileNotFoundError:
            logging.info("Файл не найден, создаю новый")
            return []
    
    def add_task(self, title, deadline=None):
        task = {
            "title": title,
            "deadline": deadline,
            "created": datetime.now().isoformat(),
            "completed": False
        }
        self.tasks.append(task)
        self._save_tasks()
        logging.info(f"Задача добавлена: {title}")
    
    def complete_task(self, index):
        if 0 <= index < len(self.tasks):
            self.tasks[index]["completed"] = True
            self._save_tasks()
            print(random.choice(MOTIVATION))
            logging.info(f"Задача выполнена: {self.tasks[index]['title']}")
    
    def _save_tasks(self):
        with open(self.filename, "w") as f:
            json.dump(self.tasks, f)
        logging.info("Задачи сохранены")
    
    def show_all(self):
        for i, task in enumerate(self.tasks):
            status = "✓" if task["completed"] else "○"
            print(f"{i}. {status} {task['title']}")

# Использование
app = TaskApp()
app.add_task("Купить продукты")
app.add_task("Написать отчёт")
app.show_all()
app.complete_task(0)  # Выведет мотивирующую фразу!
```

---

## 🎯 Связь со сквозным проектом

**Менеджер задач с встроенной библиотекой!**

```python
import json
import logging
from datetime import datetime

logging.basicConfig(level=logging.INFO)

class TaskManager:
    def __init__(self):
        self.tasks = []
        self._load_from_file()
    
    def add_task(self, title):
        task = {
            "title": title,
            "created": datetime.now().isoformat(),
            "completed": False
        }
        self.tasks.append(task)
        self._save_to_file()
        logging.info(f"Добавлена задача: {title}")
    
    def _save_to_file(self):
        with open("tasks.json", "w") as f:
            json.dump(self.tasks, f)
    
    def _load_from_file(self):
        try:
            with open("tasks.json") as f:
                self.tasks = json.load(f)
        except FileNotFoundError:
            pass

# Вот и всё!
manager = TaskManager()
manager.add_task("Привет, мир!")
```

---

## 💡 Важный совет: Изучай стандартную библиотеку

**Первые годы программирования:**

- 📖 Учи основы Python (переменные, функции, классы) ← Модули 0-3
- 📚 Учи стандартную библиотеку ← Модуль 3.5 (эта глава!)
- 🔧 Потом внешние пакеты (requests, pandas, django)

**Профессионалы:**

- 80% кода = встроенная + стандартная библиотека
- 15% кода = проверенные внешние пакеты
- 5% кода = свой собственный код

**Правило:** Сначала посмотри в стандартную библиотеку! 📚

---

## Резюме: Три главных вывода

✅ **Python поставляется с 200+ встроенными модулями** (не нужно писать самому)

✅ **Используй встроенное вместо того, чтобы изобретать велосипед** (быстро и надёжно)

✅ **Turtle и другие визуальные модули — это весело!** (не просто консоль)

---

## Что дальше?

→ **Модуль 4** (Работа с данными): JSON, CSV, файлы

→ **Модуль 5** (Внешние пакеты): requests, pandas, и другие

→ **После этого:** Реальные проекты с использованием библиотек!

---

## 🌟 ГЛАВНАЯ ИДЕЯ

**"Не пиши то, что уже написано!"** 📚

Python поставляется с ПОТРЯСАЮЩЕЙ стандартной библиотекой.

**Каждый час, потраченный на изучение её — это сэкономленная неделя кода!** ⏰

---

**Поздравляем!** Ты теперь знаешь про стандартную библиотеку Python! 🎉

**Модуль 3 (Организация кода) полностью завершён!** 🏆

**Следующий: Модуль 4 (Работа с данными)!** 🚀✨