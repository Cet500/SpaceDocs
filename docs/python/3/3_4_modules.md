# 3.4 Модули: Организуй файлы в пакеты

## Введение: От файлов к организации

В предыдущей главе (3.3) мы научились делить код на файлы:

```
project/
├── main.py
├── models.py
├── validators.py
└── database.py
```

Это работает для маленьких проектов (3-5 файлов).

**Но что если проект растёт?**

```
project/
├── main.py
├── models.py
├── validators.py
├── database.py
├── email_service.py
├── report_generator.py
├── config.py
├── logger.py
├── cache.py
├── settings.py
├── ...
```

Теперь **20+ файлов в корне** — это беспорядок!

**Решение: Организуй файлы в папки (модули)!**

```
project/
├── main.py
├── models/
│   ├── __init__.py
│   ├── task.py
│   └── user.py
├── services/
│   ├── __init__.py
│   ├── database.py
│   └── email_service.py
└── utils/
    ├── __init__.py
    ├── validators.py
    └── logger.py
```

**В этой главе:**

1. Что такое модуль (и чем он отличается от папки)
2. Структура модуля (`__init__.py`)
3. Импорты (как они работают)
4. Примеры структур
5. Best practices

**Результат:** Твой проект будет **структурирован, масштабируем и понятен**. 🎯

---

## Живой пример: Файлы vs Модули

### ❌ ПРОСТО ФАЙЛЫ (20 файлов в корне)

```
project/
├── main.py
├── task_model.py
├── user_model.py
├── task_validator.py
├── user_validator.py
├── database.py
├── cache.py
├── email_service.py
├── sms_service.py
├── logger.py
├── config.py
├── settings.py
├── utils.py
├── helpers.py
├── handlers.py
├── processors.py
├── transformers.py
├── adapters.py
├── converters.py
└── ...
```

**Проблема:** 😵 Невозможно ориентироваться!

### ✅ МОДУЛИ (организованная структура)

```
project/
├── main.py
├── models/              # Модуль моделей
│   ├── __init__.py
│   ├── task.py
│   └── user.py
├── services/            # Модуль сервисов
│   ├── __init__.py
│   ├── database.py
│   ├── email.py
│   └── sms.py
└── utils/               # Модуль утилит
    ├── __init__.py
    ├── validators.py
    └── logger.py
```

**Результат:** ✅ Логично организовано!

---

## Что такое модуль?

### Определение (просто!)

**Модуль** — это **папка с Python-файлами**, которая становится единой структурой благодаря **`__init__.py` файлу**.

**Проще:**

- 📄 Обычный файл (file.py) — это `file`
- 📁 Папка с `__init__.py` (folder/) — это модуль, который можно импортировать

### Три компонента модуля:

1. **Папка** — контейнер
2. **`__init__.py`** — "паспорт" модуля (может быть пустой!)
3. **Файлы внутри** — функции, классы, переменные

```
models/                 # Папка = контейнер
├── __init__.py         # "Паспорт" (может быть пустой)
├── task.py             # Файл с кодом
└── user.py             # Файл с кодом
```

---

## `__init__.py` — что это?

### Самая простая версия:

`__init__.py` может быть **пустым файлом**! Его задача просто **сказать Python: "Это модуль, а не просто папка"**.

```python
# models/__init__.py
# Может быть совсем пустой!
```

### Но можно сделать его полезным:

`__init__.py` может **экспортировать удобные импорты**:

```python
# models/__init__.py
from .task import Task
from .user import User

# Теперь можно писать:
# from models import Task, User
# Вместо:
# from models.task import Task
# from models.user import User
```

---

## Импорты: Как они работают?

### Основы: `import` vs `from...import`

**`import` — импортируй весь модуль**

```python
import models

# Используй как models.Task
task = models.Task("купить продукты")
```

**`from...import` — импортируй конкретное из модуля**

```python
from models import Task, User

# Используй напрямую
task = Task("купить продукты")
user = User("Иван")
```

### Относительные импорты (`.` и `..`)

**Точка (`.`) означает "текущая папка"**

```python
# services/database.py
from .validators import validate_email  # из текущей папки (services)
```

**Две точки (`..`) означают "папка выше"**

```python
# services/database.py
from ..models import Task  # из папки выше (models)
```

### Примеры импортов:

```python
# main.py в корне проекта

# Импорт из модуля
from models import Task, User
from services import Database
from utils import validate_email

# Импорт конкретного файла из модуля
from models.task import Task
from services.database import Database

# Импорт всего модуля
import models
models.Task(...)  # используй как models.Task
```

---

## Структура: Файлы vs Модули

### Маленький проект (до 5 файлов) — просто файлы

```
project/
├── main.py
├── models.py          # Один файл со всеми классами
├── validators.py
└── database.py
```

**Импорты просто:**

```python
from models import Task, User
from validators import validate_email
```

### Растущий проект (10+ файлов) — модули

```
project/
├── main.py
├── models/                    # Модуль (папка)
│   ├── __init__.py
│   ├── task.py               # Task класс
│   └── user.py               # User класс
├── services/                  # Модуль (папка)
│   ├── __init__.py
│   ├── database.py
│   ├── email.py
│   └── cache.py
└── utils/                     # Модуль (папка)
    ├── __init__.py
    ├── validators.py
    └── logger.py
```

**Импорты:**

```python
# Вариант 1: импортируй модули
from models import Task, User
from services import Database
from utils import validate_email

# Вариант 2: импортируй файлы из модулей
from models.task import Task
from services.database import Database
from utils.validators import validate_email
```

### Очень большой проект (100+ файлов) — подмодули

```
project/
├── main.py
├── app/
│   ├── __init__.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── task.py
│   │   └── user.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── database.py
│   │   └── email.py
│   └── utils/
│       ├── __init__.py
│       └── validators.py
├── tests/
│   ├── __init__.py
│   ├── test_models.py
│   └── test_services.py
└── config.py
```

**Импорты:**

```python
from app.models import Task
from app.services import Database
```

---

## Практика: Как организовать проект

### Пример 1: Менеджер задач (растущий проект)

**Структура:**

```
task_manager/
├── main.py                    # главная программа
├── models/                    # модуль с классами
│   ├── __init__.py
│   ├── task.py
│   └── user.py
├── services/                  # модуль с логикой
│   ├── __init__.py
│   └── task_service.py
└── utils/                     # модуль с утилитами
    ├── __init__.py
    ├── validators.py
    └── logger.py
```

**Содержание файлов:**

```python
# models/__init__.py
from .task import Task
from .user import User

# models/task.py
class Task:
    def __init__(self, title):
        self.title = title
        self.completed = False

# models/user.py
class User:
    def __init__(self, name):
        self.name = name
        self.tasks = []

# services/__init__.py
from .task_service import TaskService

# services/task_service.py
from ..models import Task

class TaskService:
    @staticmethod
    def add_task(user, title):
        task = Task(title)
        user.tasks.append(task)
        return task

# utils/__init__.py
from .validators import validate_email
from .logger import log_message

# utils/validators.py
def validate_email(email):
    return "@" in email

# utils/logger.py
def log_message(message):
    print(f"[LOG] {message}")

# main.py
from models import Task, User
from services import TaskService
from utils import validate_email, log_message

def main():
    user = User("Иван")
    
    if validate_email("ivan@mail.com"):
        task = TaskService.add_task(user, "Купить продукты")
        log_message(f"Задача добавлена: {task.title}")

if __name__ == "__main__":
    main()
```

---

### Пример 2: Веб-приложение (большой проект)

```
web_app/
├── main.py
├── app/
│   ├── __init__.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   └── post.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── auth.py
│   │   └── email.py
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── user_routes.py
│   │   └── post_routes.py
│   └── utils/
│       ├── __init__.py
│       ├── validators.py
│       └── decorators.py
├── tests/
│   ├── __init__.py
│   ├── test_models.py
│   └── test_services.py
├── config.py
└── requirements.txt
```

---

## Когда реально нужен модуль?

### ✅ НУЖЕН модуль, если:

- 📦 **Много файлов** (10+) в одной категории
- 👥 **Команда разработчиков** (разные люди работают на разными частями)
- 🔄 **Переиспользуемая логика** (хочешь использовать в других проектах)
- 🧪 **Нужно тестировать** отдельные части
- 🚀 **Проект растёт** (сегодня 5 файлов, завтра 20)

### ❌ НЕ НУЖЕН модуль, если:

- 📄 **5 и меньше файлов** в проекте
- 👤 **Один разработчик** на маленький проект
- 🚀 **Один раз пишешь скрипт** (не переиспользуется)
- ⏰ **Быстро нужно что-то написать** (не до архитектуры)

---

## Best Practices: Как писать хорошие модули

### 1. Один модуль = одна ответственность

**✅ ХОРОШО:**

```
models/           # только классы
services/         # только логика
utils/            # только вспомогательное
```

**❌ ПЛОХО:**

```
everything/       # всё вместе
mixed/            # классы, логика, утилиты
```

### 2. Понятные импорты через `__init__.py`

**✅ ХОРОШО:**

```python
# models/__init__.py
from .task import Task
from .user import User

# main.py
from models import Task, User  # просто и понятно
```

**❌ ПЛОХО:**

```python
# main.py
from models.task import Task
from models.user import User  # долгие импорты везде
```

### 3. Не делай циклические импорты!

**❌ ЦИКЛИЧЕСКИЙ ИМПОРТ (плохо!):**

```python
# models/task.py
from services import save_task

# services/database.py
from models import Task
# Циклический импорт! Task → save_task → Task
```

**✅ РЕШЕНИЕ:**

```python
# models/task.py
# Не импортируй сервисы в модели!

# services/database.py
from models import Task  # только в одну сторону
```

### 4. Используй относительные импорты внутри модуля

**✅ ХОРОШО (внутри модуля):**

```python
# services/database.py
from .cache import get_cache      # относительный импорт
from ..models import Task          # импорт из другого модуля
```

**❌ ПЛОХО:**

```python
# services/database.py
import sys
sys.path.insert(0, '/absolute/path')  # хардкодим пути
```

### 5. Используй абсолютные импорты из main.py

**✅ ХОРОШО (в главной программе):**

```python
# main.py
from models import Task
from services import Database
```

**❌ ПЛОХО:**

```python
# main.py
from ..models import Task  # в главной программе не нужны относительные
```

---

## Примеры импортов в реальном коде

### Внутри модуля (используй `.`)

```python
# services/database.py (внутри модуля services)
from .cache import Cache        # из текущей папки
from ..models import Task       # из папки выше
from ..utils import log_message # из другого модуля
```

### В главной программе (используй абсолютные)

```python
# main.py (в корне проекта)
from models import Task
from services import Database
from utils import validate_email
```

### При импорте модуля (используй `__init__.py`)

```python
# models/__init__.py
from .task import Task
from .user import User

# main.py может просто сделать:
from models import Task, User
```

---

## Вопросы для самопроверки

1. **Чем модуль отличается от папки?**

2. **Что такое `__init__.py` и зачем он нужен?**

3. **Когда использовать `from models import Task` vs `from models.task import Task`?**

4. **В чём разница между `.` и `..` в импортах?**

5. **Когда проект готов к переходу на модули?**

---

## Практическое задание

### Задание: Переделай проект на модули

**У тебя есть проект с файлами:**

```
project/
├── main.py              (30 строк)
├── user.py              (20 строк)
├── task.py              (20 строк)
├── validators.py        (30 строк)
├── database.py          (40 строк)
├── email_service.py     (25 строк)
└── logger.py            (15 строк)
```

**Переделай на модули:**

```
project/
├── main.py
├── models/
│   ├── __init__.py      # экспортируй User и Task
│   ├── user.py
│   └── task.py
├── services/
│   ├── __init__.py      # экспортируй Database и EmailService
│   ├── database.py
│   └── email_service.py
└── utils/
    ├── __init__.py      # экспортируй validate и log
    ├── validators.py
    └── logger.py
```

**Требования:**

1. Создай папки с `__init__.py`
2. Раздели файлы по папкам
3. Напиши экспорты в `__init__.py`
4. Обнови импорты в main.py
5. Используй относительные импорты внутри модулей

**Пример содержания:**

```python
# models/__init__.py
from .user import User
from .task import Task

# services/__init__.py
from .database import Database
from .email_service import EmailService

# utils/__init__.py
from .validators import validate_email, validate_task
from .logger import log_message

# main.py
from models import User, Task
from services import Database, EmailService
from utils import validate_email, log_message

def main():
    user = User("Иван")
    if validate_email("ivan@mail.com"):
        task = Task("Купить продукты")
        log_message(f"Пользователь {user.name} создал задачу")

if __name__ == "__main__":
    main()
```

---

## 🎯 Связь со сквозным проектом

**Менеджер задач переходит на модули!**

```
task_manager/
├── main.py
├── models/
│   ├── __init__.py
│   ├── task.py
│   └── user.py
├── services/
│   ├── __init__.py
│   ├── task_service.py
│   └── storage.py
└── utils/
    ├── __init__.py
    ├── validators.py
    └── logger.py
```

Теперь можно добавлять новые функции без беспорядка! 📦✨

---

## 💡 Важный совет: Не переусложняй!

**Стадии роста проекта:**

**Стадия 1: Новичок (1-2 файла)**

```
main.py
```

**Стадия 2: Растёт (3-5 файлов)**

```
main.py
models.py
services.py
utils.py
```

**Стадия 3: Большой (10+ файлов)**

```
main.py
models/
services/
utils/
```

**Стадия 4: Очень большой (100+ файлов)**

```
main.py
app/
  models/
  services/
  utils/
tests/
```

**Правило:** Переходи на модули только когда это становится необходимым! 🌱

---

## Резюме: Три главных вывода

✅ **Модуль = папка + `__init__.py`** (паспорт модуля)

✅ **`__init__.py` экспортирует удобные импорты** (делает импорты проще)

✅ **Используй модули когда много файлов** (10+), не раньше

---

## Что дальше?

→ **Модуль 4** (Работа с данными): Файлы, JSON, CSV

→ **Позже:** встроенные модули (os, json, requests)

---

## 🌟 ГЛАВНАЯ ИДЕЯ

**Модули = организация больших проектов** 📦

Когда проект растёт:

- 3-5 файлов → просто файлы ✓
- 10-20 файлов → модули ✓
- 100+ файлов → подмодули ✓

Не спеши переходить на модули. Растёшь? Переходи. 🌱

---

**Поздравляем!** Ты теперь знаешь про модули и можешь организовать большой проект! 🎉

**Ты готов к модулю 4 (работа с данными)!** 🚀✨