# 1.6 Строковые методы: Работаем с текстом

## Введение

До сих пор мы работали с текстом как с неизменяемым объектом. Но часто нам нужно:

- Изменить все буквы на заглавные
- Найти слово в строке
- Удалить пробелы в начале/конце
- Заменить одно слово на другое

Для этого используются **методы** — встроенные функции, которые принадлежат определённому типу данных.

В этой главе ты научишься:

1. Вызывать методы строк
2. Использовать 10+ практических методов
3. Комбинировать методы вместе
4. Применять методы в реальном коде

**Результат:** Ты сможешь **обрабатывать и преобразовывать текст**.

---

## Живой пример: Методы в действии

Запусти этот код:

```python
# Обработка текста методами
text = "  Hello, World!  "

print(text.upper())           # "  HELLO, WORLD!  "
print(text.lower())           # "  hello, world!  "
print(text.strip())           # "Hello, World!"
print(text.replace("World", "Python"))  # "  Hello, Python!  "

# Проверка наличия
if "World" in text:
    print("Слово 'World' найдено!")

# Поиск индекса
index = text.find("World")
print(f"Индекс слова 'World': {index}")  # 9
```

**Результат:**

```
  HELLO, WORLD!
  hello, world!
Hello, World!
  Hello, Python!
Слово 'World' найдено!
Индекс слова 'World': 9
```

**Видишь?** Методы преобразуют текст. Это методы! 🎯

---

## Теория: Что такое метод?

### Определение

**Метод** — это встроенная функция, которая **принадлежит определённому типу данных** и работает с этими данными.

**Синтаксис:**

```python
переменная.метод(аргументы)
```

**Разбор:**

- `переменная` — переменная (строка, список и т.д.)
- `.` — точка (разделитель)
- `метод()` — встроенная функция этого типа

**Примеры:**

```python
text = "hello"
text.upper()      # Метод upper() работает со строками

numbers = [3, 1, 2]
numbers.sort()    # Метод sort() работает со списками
```

---

## Важная особенность: Методы возвращают, но не меняют

**Это критично!** Строковые методы **возвращают новое значение**, но **не меняют исходную строку**:

```python
text = "hello"

# Вызов метода (результат выводится)
text.upper()      # Вернёт "HELLO"
print(text)       # Но сама переменная всё ещё "hello"!

# Чтобы сохранить результат, нужно присвоить
text = text.upper()
print(text)       # Теперь "HELLO"
```

**Почему?** Строки в Python **неизменяемы** (immutable). Методы создают новую строку вместо изменения старой.

---

## Основные строковые методы

### 1️⃣ `upper()` и `lower()`

Преобразуют все буквы в заглавные/строчные:

```python
text = "Hello World"

print(text.upper())   # "HELLO WORLD"
print(text.lower())   # "hello world"

# Пример: нормализация для сравнения
user_input = "HeLLo"
if user_input.lower() == "hello":
    print("Привет!")
```

---

### 2️⃣ `capitalize()` и `title()`

**`capitalize()`** — первая буква заглавная, остальные строчные:

```python
"hello world".capitalize()    # "Hello world"
"HELLO WORLD".capitalize()    # "Hello world"
```

**`title()`** — первая буква каждого слова заглавная:

```python
"hello world".title()         # "Hello World"
"hello-world".title()         # "Hello-World"
```

---

### 3️⃣ `strip()`, `lstrip()`, `rstrip()`

Удаляют пробелы (и другие символы) из строки:

```python
text = "  hello  "

print(text.strip())    # "hello" (с обоих сторон)
print(text.lstrip())   # "hello  " (слева)
print(text.rstrip())   # "  hello" (справа)

# Удалить конкретные символы
"###hello###".strip("#")    # "hello"
"xxxhelloxxx".strip("x")    # "hello"
```

**Используется часто для очистки пользовательского ввода:**

```python
user_input = input("Введи своё имя: ").strip()
# Если пользователь добавит пробелы, они будут удалены
```

---

### 4️⃣ `find()` и `index()`

Находят индекс подстроки:

```python
text = "hello world"

print(text.find("world"))      # 6
print(text.find("python"))     # -1 (не найдено)

# Пример: найти положение символа
email = "user@example.com"
at_position = email.find("@")  # 4
```

**Разница между `find()` и `index()`:**

- `find()` возвращает -1, если не найдено
- `index()` выкидывает ошибку

```python
text = "hello"
text.find("x")    # -1 (безопасно)
text.index("x")   # ValueError (ошибка!)
```

---

### 5️⃣ `replace()`

Заменяет подстроку на другую:

```python
text = "hello world"

print(text.replace("world", "python"))     # "hello python"
print(text.replace("l", "L"))              # "heLLo worLd"

# Заменить только первые N случаев
text = "ha ha ha ha"
print(text.replace("ha", "ho", 2))        # "ho ho ha ha"
```

---

### 6️⃣ `count()`

Считает количество раз подстроки в строке:

```python
text = "hello world hello"

print(text.count("hello"))      # 2
print(text.count("l"))          # 3
print(text.count("python"))     # 0

# Пример: узнать количество строк в тексте
text = "apple,banana,orange,grape"
items_count = text.count(",") + 1
print(f"Количество товаров: {items_count}")  # 4
```

---

### 7️⃣ `split()` — разделить строку

Разделяет строку на список по разделителю:

```python
text = "apple,banana,orange"

items = text.split(",")
print(items)   # ["apple", "banana", "orange"]

# По умолчанию разделяет по пробелам
sentence = "hello world python"
words = sentence.split()
print(words)   # ["hello", "world", "python"]

# Практический пример
csv_line = "Миша,25,Москва"
name, age, city = csv_line.split(",")
print(f"{name} ({age} лет) из {city}")
```

---

### 8️⃣ `join()` — объединить строки

Объединяет список строк с разделителем:

```python
words = ["hello", "world", "python"]

text = " ".join(words)
print(text)   # "hello world python"

# С другим разделителем
items = ["apple", "banana", "orange"]
csv = ",".join(items)
print(csv)    # "apple,banana,orange"

# Пример: создание строки из списка
tags = ["python", "programming", "tutorial"]
hashtags = "#" + " #".join(tags)
print(hashtags)   # "#python #programming #tutorial"
```

---

### 9️⃣ `startswith()` и `endswith()`

Проверяют начало и конец строки:

```python
text = "hello.txt"

print(text.startswith("hello"))    # True
print(text.startswith("world"))    # False

print(text.endswith(".txt"))       # True
print(text.endswith(".pdf"))       # False

# Практический пример: проверка расширения файла
filename = "document.pdf"
if filename.endswith(".pdf"):
    print("Это PDF файл")
else:
    print("Это не PDF файл")
```

---

### 🔟 `in` — проверка наличия

Проверяет, есть ли подстрока в строке:

```python
text = "hello world"

print("world" in text)     # True
print("python" in text)    # False

# Практический пример
email = "user@example.com"
if "@" in email and "." in email:
    print("Похоже на валидный email")
```

---

## Практика: Обработка текста

### Практика: Нормализация данных

**Код:**

```python
# Пользователь вводит данные (с ошибками)
name = "  JoHn DoE  "
email = "JOHN@EXAMPLE.COM"
city = "new york"

# Нормализуем
name = name.strip().title()           # "John Doe"
email = email.lower().strip()         # "john@example.com"
city = city.title()                   # "New York"

# Выводим
print(f"Имя: {name}")
print(f"Email: {email}")
print(f"Город: {city}")
```

**Результат:**

```
Имя: John Doe
Email: john@example.com
Город: New York
```

---

### Практика: Парсинг CSV

```python
# Строка данных из CSV
csv_line = "Миша,25,Москва,misha@email.com"

# Разделяем по запятой
parts = csv_line.split(",")

# Очищаем пробелы и присваиваем
name = parts[0].strip()
age = parts[1].strip()
city = parts[2].strip()
email = parts[3].strip()

# Выводим структурированные данные
print(f"Имя: {name}")
print(f"Возраст: {age}")
print(f"Город: {city}")
print(f"Email: {email}")
```

**Результат:**

```
Имя: Миша
Возраст: 25
Город: Москва
Email: misha@email.com
```

---

## Вопросы для самопроверки

1. **Что выведет этот код?**
   ```python
   text = "hello"
   text.upper()
   print(text)
   ```

2. **Как разделить строку "a,b,c" на список?**

3. **Как объединить список ["x", "y", "z"] в строку "x-y-z"?**

4. **Что вернёт `"hello world".find("world")`?**

5. **Какая разница между `strip()`, `lstrip()` и `rstrip()`?**

---

## Практическое задание

### Задание: Обработчик данных пользователя

**Что нужно:**

Создай программу для обработки и валидации данных:

```python
# Данные пользователя (как из формы)
raw_name = "  john smith  "
raw_email = "JOHN.SMITH@EXAMPLE.COM"
raw_phone = "(123) 456-7890"
raw_bio = "I love   Python!  Programming  is cool."

print("=== ИСХОДНЫЕ ДАННЫЕ ===")
print(f"Имя: '{raw_name}'")
print(f"Email: '{raw_email}'")
print(f"Телефон: '{raw_phone}'")
print(f"Описание: '{raw_bio}'")

# Обработка
name = raw_name.strip().title()
email = raw_email.lower().strip()

# Удаляем скобки и пробелы из телефона
phone = raw_phone.replace("(", "").replace(")", "").replace(" ", "")

# Нормализуем описание
bio = " ".join(raw_bio.split())  # Удаляет лишние пробелы

print("\n=== ОБРАБОТАННЫЕ ДАННЫЕ ===")
print(f"Имя: {name}")
print(f"Email: {email}")
print(f"Телефон: {phone}")
print(f"Описание: {bio}")

# Валидация
print("\n=== ПРОВЕРКА ===")
if "@" in email and "." in email:
    print("✓ Email выглядит корректно")
else:
    print("✗ Email некорректен")

if len(phone) == 10 and phone.isdigit():
    print("✓ Телефон корректен (10 цифр)")
else:
    print("✗ Телефон некорректен")

if name.count(" ") >= 1:
    print("✓ Имя состоит из имени и фамилии")
else:
    print("✗ Требуется имя и фамилия")
```

**Результат должен быть примерно:**

```
=== ИСХОДНЫЕ ДАННЫЕ ===
Имя: '  john smith  '
Email: 'JOHN.SMITH@EXAMPLE.COM'
Телефон: '(123) 456-7890'
Описание: 'I love   Python!  Programming  is cool.'

=== ОБРАБОТАННЫЕ ДАННЫЕ ===
Имя: John Smith
Email: john.smith@example.com
Телефон: 1234567890
Описание: I love Python! Programming is cool.

=== ПРОВЕРКА ===
✓ Email выглядит корректно
✓ Телефон корректен (10 цифр)
✓ Имя состоит из имени и фамилии
```

**Требования:**

- Используй `.strip()` для очистки пробелов
- Используй `.lower()` / `.upper()` для нормализации
- Используй `.replace()` для удаления символов
- Используй `.split()` / `.join()` для работы с пробелами
- Используй `in` для проверки наличия символа

---

## Связь со сквозным проектом 🎯

**Консольный менеджер задач обрабатывает текст!**

```python
# Обработка ввода пользователя
task_input = "  BUY GROCERIES  "

# Нормализуем
task = task_input.strip().lower().capitalize()
print(f"Задача добавлена: '{task}'")  # "Buy groceries"

# Если пользователь ищет задачу
search = "BUY"
for task in tasks:
    if search.lower() in task.lower():
        print(f"Найдена: {task}")
```

На этом этапе:

- 📝 Ты учишься обрабатывать текст
- 🔜 Потом научишься читать/писать файлы
- 🔜 Потом работать с данными из API
- 🔜 Потом создавать веб-приложения

---

## Резюме: Три главных вывода

✅ **Методы преобразуют текст, но создают новую строку.** Помни: `text = text.upper()`, не просто `text.upper()`.

✅ **Самые полезные методы:**

- `strip()` — очистить пробелы
- `split()` — разделить на список
- `join()` — объединить в строку
- `replace()` — заменить текст
- `find()` — найти индекс

✅ **Методы часто используются вместе.** `text.strip().lower().replace()` — это нормально!

---

## Что дальше?

→ **Модуль 2.1** (Условия): Как принимать решения (if/else/elif)

→ **Модуль 2.2** (Циклы): Как повторять код (for/while)

→ **Модуль 2.3** (Функции): Как организовывать код

---

## 💡 Важный совет: Метод цепочки

Методы можно вызывать по цепочке (тут это работает, потому что каждый метод возвращает строку):

```python
# Без цепочки
text = "  HELLO WORLD  "
text = text.strip()
text = text.lower()
text = text.replace("world", "python")
print(text)   # "hello python"

# С цепочкой (одна строка!)
text = "  HELLO WORLD  ".strip().lower().replace("world", "python")
print(text)   # "hello python"
```

**Правило:** Методы выполняются слева направо. Сначала `strip()`, потом `lower()`, потом `replace()`.

---

**Модуль 1 завершён!** Ты знаешь переменные, списки, словари, операторы и методы. Этого достаточно для большинства
простых программ! 🎉

**Модуль 2 — впереди:** Условия, циклы, функции. Это сделает твои программы намного мощнее! 🚀✨